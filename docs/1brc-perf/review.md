# Review ledger — 1brcPerf

One line per group, written by the cycle that built it after it audited its own diff. This file is what this codebase remembers about what it gets wrong; read it before you build.

- reviewed idle-gate-sigpipe 9e2fbcc, busiest-stamp 32e3e60: REPAIRED — both instrument fixes are correct and both self-test cases were mutate-verified this cycle (`awk … exit` → `FAIL (status 141 on a call within 40)`; deleted `busiest:` echo → `FAIL: named no busiest process on a quiet machine`), but `busiest-stamp` had landed with NO ledger line, NO handoff block and NO review line, and this file did not exist at all. The defect was bookkeeping, not code: a member whose code is committed but unledgered is invisible to `loop-next.sh`, which greps `built.md` by literal id, so the ticket would have been re-dispatched and re-implemented. Added the `built.md` line, the handoff block and this file.
- reviewed lanes-requiet (STILL OPEN, no `built.md` line) d0836ef: REPAIRED — audited E-38 (`857911b`), which a second writer landed mid-cycle for this ticket. Its verdict is sound and its refusal to promote `lanes` is correct: wall -3.57% inside the pre-registered 2-5% band against a 0.000% incumbent bracket, user CPU -5.16% against E-37's -5.28%, ranges NOT disjoint (`lanes` max 1.309 s vs incumbent minima 1.227/1.220 s), so the `-fold` default rightly did not move. The defect was provenance, not arithmetic: the row cited `856215f` while its own linked artifact's header says `fb198d0` with a DIRTY WORKING TREE, measured from the `research` checkout on `main` rather than this branch, and that header carries no `busiest:` line because that checkout predates the `busiest-stamp` fix. Verified the number survives — `git diff fb198d0 856215f -- 1brc/code/go` is empty, so either commit builds the same binary — and rewrote the citation plus a provenance bullet to say so. The ticket stays open because E-38's own verdict defers promotion to a quiet-machine run, and that bracket is in flight detached, waiting on operator idle.
