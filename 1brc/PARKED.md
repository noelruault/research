# Parked ideas — 1brc

Convention and required fields: [`../PARKED.md`](../PARKED.md). **Re-read this file whenever any baseline in this study moves** — that is exactly when a parked entry can silently become valuable and exactly when nobody thinks to look.

Live state and the active hypothesis queue are in [`07-experiment-ledger.md`](07-experiment-ledger.md). This file is only for things *not* being worked on.

The baseline entries P-01 to P-05 were measured against, so a future reader can see when the comparison goes stale: **staged SWAR at 11.225 ns/row on the 413-station corpus and 13.760 on the 10k long-name corpus**, both PROVISIONAL (battery), both from `04-asm-kernels-data.txt`.

**The baseline P-06 to P-09 are measured against is the end-to-end binary, not a kernel microbenchmark**: `-workers 20 -buf 1024 -parse word -fold ptr -table combined -split static -io pread -nocache=true` at **1.233 s ± 0.010 s** on `measurements-1b.txt`, storage state **uncached (`F_NOCACHE`, 20 parallel `pread`s of 1 MiB)**, on AC with the preflight green (E-27). Its compute floor is **0.9771 s** (E-32's three-round mean; the rounds themselves are 0.9793 / 0.9760 / 0.9760) and **0.9812 s** as E-27 measured it — two figures from two invocations, not a range — and the target is 1.000 s.

**Round-3 close: every entry above was re-read against those numbers, and the sweep is recorded here rather than left implied** (`go-opt-round-3-park`). Two triggers fired and neither revived, and the two entries carry the numbers inline: **P-01** (fired, and its third trigger is falsified by its own data — `CORRECTIONS.md` C12) and **P-03** (fired on `memequal` at 8.81-9.87% of compute, stays parked). Three did not fire, with the number or the compiler error each was tested against: **P-02** — the transfer floor has not moved on arm64, and the check is below rather than assumed; **P-04** — the trigger is the Go path within ~15% of the 754.4 ms read floor (~870 ms) and the best measured wall is **1.233 s, +63.44% above it**; **P-05** — the trigger is a worker-wall spread above ~1.15 and E-32 measured **1.008-1.013** on the shipped default.

**The intrinsic trigger P-01 and P-02 share was tested against the installed toolchain, and it fired on the wrong architecture** [MEASURED, `07-experiment-ledger-data.txt` §U]. go1.27.0 **does** now ship `simd` and `simd/archsimd` with arm64 support (`$GOROOT/src/simd/archsimd/ops_arm64.go`, `midway_arm64.go`), behind `//go:build goexperiment.simd` and off by default (`go env GOEXPERIMENT` is empty; without the experiment the package's "build constraints exclude all Go files"). The op both entries wait on — a mask into a general register, arm64's missing `PMOVMSKB` — is **amd64-only**: `archsimd/types_amd64.go:264` declares `func (x Mask8x16) ToBits() uint16`, the arm64 mask type declares only `And`, `Or` and `Not` (`archsimd/ops_arm64.go:2140-2144`) plus `String` (`archsimd/maskmerge_gen_arm64.go:142`), and a two-line probe that calls `ToBits()` on `archsimd.Mask8x16` fails to compile at `GOARCH=arm64` (`m.ToBits undefined (type archsimd.Mask8x16 has no field or method ToBits)`) while the identical file builds clean at `GOARCH=amd64` and the same file calling `Not().String()` builds clean at `GOARCH=arm64` — two controls, so the failure is the missing method and not the import or the experiment flag. So the trigger's premise is now *half* satisfied and the half that matters here is not: a Go release exposed vector intrinsics without exposing the one that would delete P-01's measured 1.080 ns/row. **What this register does NOT hold is the study's remaining gap.** Reaching 1.000 s needs **−17.58% of user CPU** (E-32), the only pot that size is the separator scan at 41.57-43.64% of non-syscall compute, and the board has spent both of its mechanisms on it — item 1 CLOSED not-adopted at +0.81% (E-27) and item 2 KILLED at +9.8%/+10.4% (E-10). Nothing is parked against that pot because no third mechanism has been named, and inventing one to fill the register is what `spec.md:61` forbids.

---

## P-01 — Per-row NEON separator scan · `parked`

**What it is.** Find `;` and `\n` with a 16-byte NEON compare per row — `VLD1` the window, `CMEQ` against the needle, `SHRN #4` to pack the byte lanes into a nibble mask, `VMOV` the mask into a general register, count trailing zeros. Built and correct: `neon_arm64.s`, driven by `TokenizeStagedNEON`, which differs from `TokenizeStagedSWAR` in exactly one line.

**Why parked — with the number.** It loses to 8-byte SWAR by **+1.830 ns/row (+16.3%)** on the 413 corpus and by **+3.185 ns/row (+23.1%)** on the 10k corpus, against the baseline above. The cause is measured rather than argued: `neonTransferProbe` isolates one vector-to-general round trip at **1.080 ns/row** (413) and **0.825** (10k), and that transfer sits in the loop's critical path because the loop branches on it. arm64 has no `PMOVMSKB` and no branch-on-vector instruction to avoid it with. The margin *grows* on the corpus where the wider window should help most, which says the transfer is a per-window cost in the dependency chain, not a startup cost being amortised.

**Depends on.** (a) One transfer per row — the entire loss is a per-row transfer that a batched loop would pay once per ~2.3 rows. (b) The 1.080 ns/row transfer figure, which is transfer-*plus*-non-inlinable-call and has never been separated (Go has no intrinsic for the narrow-and-move). (c) The official key set's 8.0-byte mean name length, which is what keeps the 8-byte SWAR window sufficient 65.9% of the time. (d) Go's inlining of the SWAR path, which the assembly call cannot have.

**Revive when.** Any of: a Go release exposes the vector-to-general move as an intrinsic, or `//go:noescape` assembly becomes inlinable, either of which deletes the call half of the measured cost; ~~or a kernel is needed for an input whose names average over ~32 bytes, where the window advantage is largest~~ [**FALSIFIED, `CORRECTIONS.md` C12** — that input was already measured when this clause was written and it is the one where NEON loses by MORE: `04-asm-kernels.md:31` and `:92` record the 10k corpus at a 51.1-byte mean name with 68.9% of names over 32 bytes, and per-row NEON loses by **23.1%** there against **16.3%** on the 8.0-byte official set. Long names are this entry's worst case, not its rescue]; or the batch shape (`TokenizeBatch`) is rejected end-to-end in `go-v1-parallel` and a per-row scan is the shape being optimised again.

> **This last trigger FIRED, the entry was re-read, and it stays parked** (`go-v2-kernels`, `07-experiment-ledger.md` E-10). The batch shape lost end to end by 9.8-10.4% at 1b and the per-row scan IS the shape being optimised again — but P-01's own number is a comparison *between* per-row scans, and per-row NEON lost to per-row SWAR by 16.3-23.1%. The trigger fired into an entry whose number still says no. Firing a trigger is not the same as reviving, and recording the difference is the point of the register.

**Cost to revive.** Low. The kernel exists, is correct, is mutation-tested and has a page-guard test; reviving is re-running the benchmark, not rebuilding.

**Where.** `1brc/code/asm/neon_arm64.s`, `neon_arm64.go`, `TokenizeStagedNEON` in `tokenize.go`; benchmarks `BenchmarkTokenize/staged-neon` and `BenchmarkNEONTransferFloor`; numbers in `04-asm-kernels-data.txt`; commits `20aa0d3`, `e29141f`, `a4a49df`.

> **Not killed.** The mechanism is not wrong; it is priced wrong at one transfer per row. Its own rescue lived in the batch kernel, which pays the same transfer once per window — and E-10 has now measured that rescue losing 10.4% end to end at 1b, so P-01 has one fewer route back than it had when it was written.

---

## P-02 — NEON temperature parse · `parked`

**What it is.** Parse the fixed-point temperature field with a vector compare instead of merykitty's SWAR-style branchless arithmetic — the third variant `03-technique-recon.md` named as H3's test, alongside the scalar digit loop and the branchless parse. **Never built.**

**Why parked — with the number.** Parked on a measured bound rather than on a measurement of the thing itself, and that distinction is the point of this entry. Any per-row NEON kernel pays **1.080 ns/row** (413) before doing useful work, measured bare by `neonTransferProbe`. The *entire* spread between the two parses that were built is **1.735 ns/row** (413, scalar wins) and **3.295** (10k, branchless wins). So a NEON parse would have to beat the better of the two existing parses by more than the whole measured spread between them just to break even on its own transfer.

**Depends on.** (a) The 1.080 ns/row per-row transfer floor — the same figure P-01 depends on, so both entries go stale together. (b) The parse staying a per-row operation; the bound assumes one transfer per row and says nothing about a parse inside a batched loop. (c) The 1.735/3.295 ns/row spread between the scalar and branchless parses, which is itself input-dependent and was measured with the separator scan held fixed at SWAR.

**Revive when.** ~~The batch shape wins end-to-end in `go-v1-parallel`.~~ **This trigger is now SPENT: `go-v2-kernels` measured both batch arms losing at 1b by 9.8% and 10.4%, disjoint (`07-experiment-ledger.md` E-10), so the batched loop this entry was waiting for is not the loop the binary runs.** What remains is either P-01 trigger (a) — an intrinsic or an inlinable call, which moves the floor for both entries, and which `07-experiment-ledger-data.txt` §U.1 has now tested against go1.27.0: it fired on the wrong architecture, the mask-to-general-register move being amd64-only — or an input whose names average over ~32 bytes, ~~where E-10's revive trigger and this one coincide~~ [**CORRECTED, `CORRECTIONS.md` C12**: E-10's version of that trigger is falsified, so the two no longer coincide. **This entry's own long-name route survives, and on better numbers than the one it borrowed.** A parse does not care how long the name is; what the 10k corpus changes is the two figures this entry's bound is made of, and both move in its favour — the per-row transfer floor is **0.825 ns/row** there against 1.080 on the 413 set (`04-asm-kernels.md:15`) while the spread between the two built parses is **3.295 ns/row** against 1.735 (`:43`), so the headroom a NEON parse must clear goes from **0.655 ns/row** to **2.470**, a ratio of 3.99 against 1.61]. Without one of those, the 1.080 ns/row per-row transfer bound stands and this entry moves toward `killed`.

**Cost to revive.** Moderate. Unlike P-01 nothing exists: a kernel, a scalar-reference correctness check over all 1999 legal temperatures at every alignment, and a benchmark. Perhaps a day.

**Where.** Nothing built. The argument and its numbers are `04-asm-kernels.md` H3; the transfer floor is `BenchmarkNEONTransferFloor` in `1brc/code/asm/bench_test.go`; the hypothesis as originally stated is H3 in `03-technique-recon.md`.

> **This is a deviation from the recon's stated test for H3, recorded as one.** H3 named three variants and two were built. The third is parked on a number, which is the bar `spec.md:37` sets for not building something — but it remains a bound, not a measurement, and the entry says so.

---

## P-03 — Learned perfect hashing for the station table · `parked`

**What it is.** Replace the open-addressing probe with a perfect hash built at run time: learn the key set from the first buffer a worker reads, build a collision-free (ideally minimal) hash over those keys, then run the remaining rows through a table lookup with no probing and no key compare, keeping a fallback path for a key the sample missed. The construction is the compiler world's — `gperf` and CHD (Belazzougui, Botelho, Dietzfelbinger, ESA 2009) — applied to a key set that is discovered rather than declared, so nothing about the station list is hardcoded and the challenge's rules are not bent.

**Why parked — with the number.** It optimizes a step nothing has shown to be hot. 413 stations in 131,072 buckets is a **0.3% load factor**, so almost every probe resolves in its first slot and the `bytes.Equal` that follows is perfectly predicted. **Predicted under 2%** at 413 stations, against a two-phase table, a fallback path and a per-worker build. `05-go-techniques.md` measured the ADJACENT thing — a prefix-hashed open-addressing table beats Go's map by 15.8% on the 413 set — so the probe is already the cheap part of the table.

**Depends on.** (a) The 0.3% load factor, which is `-bits 17` (E-07's KEEP) against 413 stations; a 10,000-station file makes it 7.6% and a smaller table makes it worse still. (b) ~~Nothing having attributed measurable time to `table.update`'s probe — queue item 3's profile has not been run.~~ **Spent: the profile has now been run three times (E-21, E-26, E-32) and it attributes `(*table).update` 12.52-14.05% and `runtime.memequal` 8.81-9.87% of non-syscall compute — but no profile separates the probe from the key compare from the update arithmetic, so what this dependency now rests on is that unseparated share.** (c) The 413-station key set being what the headline is measured on.

**Revive when.** Either: the profile from queue item 3 attributes a measurable share of cycles to the probe or to `bytes.Equal`; or a 1b 10,000-station file (queue item 8) exists and the probe is measurably hot on it. ~~or H-13 (the quotiented entry) wins, which would show the bucket layout is worth spending on at all~~ — **this third trigger FIRED NEGATIVE: E-28 measured the quotiented entry at +5.34% of wall, so the bucket layout is not worth spending on at 413 stations and only the 10k regime can revive this.**

> **Round-3 close: trigger (a) FIRED and the entry stays parked, which is the P-01 pattern again.** E-32's profile of the shipped default puts `runtime.memequal` at **8.81-9.87%** of non-syscall compute (1.29-1.45 s of 14.6567 s) and `(*table).update` at **12.52-14.05%**, so "a measurable share" is measured. What stops it being a revival is that the share is not attributable to the probe: E-28 built the one arm that attacks the layout `memequal` sits behind and measured **+5.86%/+4.83% of wall**, and this entry's own prediction is **under 2%** at 413 stations. Trigger (b) is re-pointed rather than fired — the 1b-row 10k file was REFUSED at 57.09 GB (board item 8) and `measurements-10k-stations-242m.txt` replaced it, and on that file E-33 measured the 10k win coming from allocation (120.0 MiB, system CPU −5.48% on an identical I/O path) rather than from the probe, which is evidence against this entry rather than for it. The next thing that could move it is P-09.

**Cost to revive.** Moderate, about a day. The build is only 413 keys, so the construction is not the risk; the fallback path is, because it is the branch that never runs on clean data and therefore the branch no byte-compare will ever exercise — the same shape as the three first-vs-last-`;` bugs this study has already shipped.

**Where.** Nothing built. The argument is `06-cross-disciplinary-transfer.md` C4; the table it would replace is `1brc/code/go/table.go`; the load-factor and hash numbers are `05-go-techniques.md` and `07-experiment-ledger.md` E-07.

---

## P-04 — Metal GPU offload · `parked`

**What it is.** Tokenize and aggregate on the GPU. Apple silicon's unified memory means a buffer the CPU filled is one the GPU reads with no copy; this machine reports 16 GPU cores. Byte ranges are embarrassingly parallel and per-station reduction is the standard atomics-into-threadgroup-memory pattern. The CPU would do the reading and the GPU the folding.

**Why parked — NOT killed, and the number is what stops it being killed.** The read floor is **754.4 ms ± 8.8 ms** (`02-baseline.md`), and no accelerator lowers it. But the target is **1.000 s**, so a perfect offload — floor plus launch plus reduction — lands *under target*. An idea whose ceiling clears the goal cannot be killed on numbers. What parks it is scope: `spec.md:5` asks for a Go implementation, so a Metal kernel behind cgo is a measured comparison and not the answer, and standing it up means a new kernel, a cgo boundary, a cross-core reduction and the whole correctness gate re-established on a second path.

**Depends on.** (a) The 754.4 ms floor staying the floor — if a faster read path is found, the GPU's ceiling drops with it and the case gets *stronger*, not weaker. (b) `spec.md:5`'s "a Go implementation", which is a scope constraint the operator can change and is the only expiring half of this entry. (c) The Go path still being over target; if Go reaches 1.0 s there is nothing left for a GPU to win.

**Revive when.** The Go path is measured within ~15% of the 754 ms floor (roughly 870 ms) and is still over target — at that point every remaining millisecond is compute, which is the only thing a GPU can attack. Or the operator widens `spec.md:5` to allow a non-Go comparison arm.

**Cost to revive.** High, several days: a Metal compute kernel in MSL, a cgo bridge, a reduction across 16 GPU cores, and the correctness gate re-derived on the new path. This is a fork of the study, not a round of it.

**Where.** Nothing built. The argument is `06-cross-disciplinary-transfer.md` C6; the floor it is measured against is `02-baseline.md` / `02-baseline-data.txt`; the GPU core count is `06-cross-disciplinary-transfer-data.txt` §1.

## P-05 — H-11, the core-class-weighted split · `subsumed`

**What it is.** Split the file by byte range in proportion to core throughput rather than equally: 5 "Super" cores with 16 MiB of L2 and 10 "Performance" cores with 8 MiB (`06-cross-disciplinary-transfer-data.txt` §1) get shares weighted by a measured ratio `r`, instead of `reader.go` handing every worker the same number of bytes. Borrowed from morsel-driven parallelism, minus the dynamic dispatch E-02 already killed here.

**Why subsumed — and the number, with the baseline it was measured against.** The mechanism is finish-time imbalance: an equal split makes the fast cores idle while the slow ones finish, and weighting reclaims that tail. E-20 measured the tail directly with `-phases` on the current default (20 workers, 4 MiB buffers, `F_NOCACHE`, static split, 1b rows, AC power, load 4.20): **worker wall spread is 1.029-1.038 max/min over three runs**, so there are at most **3.8%** of imbalance to collect, against a prediction of 3.3% to 16.7% as a function of `r`. E-17's oversubscription to `NumCPU()*4/3` = 20 workers is what collects it: with more goroutines than cores the scheduler refills a core that finishes early, which is the same balancing a weighted split would buy by arithmetic. The prediction was not wrong about the mechanism, it was written against a shape the binary no longer has.

**Depends on.** ~~The default staying oversubscribed. The 3.8% ceiling is a property of 20 workers on 15 cores, not of the split; at one worker per core the imbalance E-20 cannot see would be back, and `r` would matter again.~~ **FALSIFIED by E-31, which ran the revive test rather than reasoning about it.** Measured at one worker per core on the shipped binary: worker wall spread is **1.021-1.025** with `-fill off -workers 15` and **1.022-1.027** with `-fill ahead -workers 15`, against 1.014-1.018 for the 20-worker default in the same window. Dropping oversubscription moves the spread by less than one point and leaves it **12.3 points below the 1.15 trigger** (2.7% of imbalance measured against the 15% the trigger asks for), so this entry does not depend on the default staying oversubscribed at all: there is at most **2.7%** of finish-time imbalance to collect in the very regime that was supposed to bring it back. What oversubscription buys is read overlap across workers (E-31 prices removing it at +14.09% of wall), not finish-time balance — so the two are not the same tail and only one of them was ever this entry's pot.

**Revive when.** A `-phases` run on some future configuration shows worker wall spread above ~1.15. That is now the ONLY trigger: the "default returns to one worker per core" half is spent, because E-31 measured that configuration directly and the spread did not move. Pinning workers to cores for cache-locality reasons still qualifies, but only through the spread it produces, not by assumption.

**Cost to revive.** Low, under an hour: `r` on a resident buffer, then a weighted `staticRanges` behind a flag. The arithmetic is already written in the ledger's item 11, and the boundary-trap tests the range splitter already carries (`TestARangeShorterThanARowOwnsNothing` and its siblings) cover a weighted split unchanged, because they sweep worker counts rather than assuming equal shares.

**Where.** Nothing built. The hypothesis is `06-cross-disciplinary-transfer.md` H-11 and the ledger's queue item 11; the measurement that subsumed it is E-20 / [`bench/2026-09-01T192513Z-queue-item-3-where-the-idle-cores-go.txt`](bench/2026-09-01T192513Z-queue-item-3-where-the-idle-cores-go.txt), and the measurement that spent its second revive trigger is E-31 (`07-experiment-ledger-data.txt` §S, the `-phases` rounds). The claim it finally tested is `02-baseline.md:15`'s "a uniform 15-way split is close to optimal on this chip", which is now measured and true **at 15 workers as well as at 20** — E-31 is the first run to test it in the configuration the sentence literally describes.

---

## P-06 — Queue item 18, more readers than 20 · `parked`

**What it is.** Raise `-workers` above the current default of 20 (`NumCPU()*4/3` = 20 on 15 cores) and sweep it, because the binary does not have enough runnable goroutines to fill the machine. Each worker both reads and folds, so a worker blocked in `pread` is a core with nothing to run; adding workers adds runnable ones. No code — `-workers` is a flag on the shipped binary (`main.go:21`).

**Why parked — with the number.** It is the round's ONLY runnable candidate and **its ceiling does not reach the goal**, so it could never have closed the round. E-32 instrumented the shipped default and the gap decomposes as an identity rather than a model: `wall × 15 cores` = 18.1995 core-seconds of capacity = **user 80.53% + system 7.99% + idle 11.48%**, reproduced to the digit in all three rounds. The idle term is **1.722 cores of 15**, traced to a mean of **12.85 workers-with-work of 20** (`20 − read/wall`). Perfect packing — every idle core filled, nothing else changed — is `(user + sys)/15` = **1.0740 s**: **−11.48%** of the 1.2133 s instrument wall and still **+7.40% over the 1.000 s target**. It was not built because the round's split (`docs/1brc/backlog.md:26`) gave the windows after `go-opt-round-3-gap` to `go-opt-round-3-10k` and then to this ticket, which closes the round rather than opening another arm; an arm whose ceiling misses the target is a candidate for the gap, never for the goal.

**Depends on.** (a) The slope, which is a **two-point line and is labelled DERIVED at its source**: blocked read is 5.097 s at 15 readers (E-31) and 8.673 s at 20 (E-32), so workers-with-work went 11.35 → 12.85, **0.300 per added worker**, extrapolating to **27.2 workers** for 15 runnable — and one of those two points is another invocation's arm. (b) Blocked-read time staying a non-constant in the reader count: E-32 measures **11.37 GB/s aggregate, 62.2%** of `02-baseline.md`'s 18.29 GB/s at 15 uncached `pread`s, so there is device headroom now, and this entry dies the moment there is not. (c) The 1 MiB buffer and `F_NOCACHE` staying the default — both set the blocked-read term this item acts on, and E-24 swept `-buf` to its minimum against a 4 MiB incumbent, not against 24 workers. (d) E-12 having measured 20 against 30 without separating them, which does NOT rule the item out: that invocation carries the slot bias E-16/E-18 priced and its binary is three arms old.

**Revive when.** It is runnable now, so the trigger is a decision and not a wait — and **the test is the one the queue already pre-registered, quoted rather than restated, because a second version of it in a second file is how a pre-registered bar gets quietly moved**: `-a 'default.a=' -a 'w24=-workers 24' -a 'w28=-workers 28' -a 'default.b='`, four slots at 1b, backgrounded, incumbent first and last, preflight green on AC. Its bar is **"anything over 5% would be a surprise"** and its second channel is **parallel efficiency, which must rise from 80.5% if the mechanism is real**. Two things this entry adds and labels as additions rather than corrections: a fifth slot `-a 'w36=-workers 36'` is worth having because the extrapolated optimum is 27.2 workers and the pre-registered arms straddle it without bracketing the far side, so `w36` is what distinguishes a plateau from a peak; and an arm that moves USER CPU is not doing what this item claims, so user CPU flat across slots is a precondition for reading the wall clock at all (E-16's rule applied to this arm).

**And the trigger only discriminates if it settles the competing reading the queue names, which is the reason to run it at all.** Idle cores are equally consistent with *too few runnable goroutines* (this item's reading, which more workers fix) and with *the scheduler not waking a P fast enough* for goroutines that are already runnable (which more workers do not fix, and which the 0.63 workers' worth descheduled in the same runs also fits). Nothing in E-32 separates them. So the mechanism check is `-phases` on the winning arm: workers-with-work must MOVE, and if it does not while the wall clock does, the win is not this item's mechanism. It is KILLED if the best arm sits inside the bracket spread, or if workers-with-work is unmoved.

**Cost to revive.** Low, one quiet window. No code. Five arms at 1b is `5 × 6 × 21.7 s` ≈ **11 minutes**, so it must be BACKGROUNDED — a 600 s foreground cap kills the run on hyperfine's `--prepare` sleep and aborts the whole invocation — plus `check-correctness.sh` per arm.

**Where.** Nothing to build. The mechanism, the decomposition and every number are `07-experiment-ledger.md` E-32 and its board item 18; the raw output is `07-experiment-ledger-data.txt` §T; the 15-reader blocked-read point is E-31 (§S); the flag is `-workers` in `1brc/code/go/main.go:21` and `defaultWorkers()`.

---

## P-07 — Queue item 12, H-12's vectorized table probe · `parked`

**What it is.** Probe several buckets in one operation instead of one at a time: load N bucket hashes into a vector, compare all N against the row's hash with a single NEON compare, and branch once — SIMD predicate filtering, which is how a columnar database evaluates a filter, applied to the open-addressed station table. Behind a `-table vec` flag alongside `combined`, `split`, `quot` and `map`.

**Why parked — with the number.** Its own pre-registered prediction is **under 3% at 413 stations** and over 10% at 10,000, and both halves now have evidence against them. At 413 stations the load factor is **0.315%** (413 live of 131,072 buckets at `-bits 17`), so almost every probe resolves in its first slot and there is nothing to vectorize away. At 10,000 stations — the regime the item was really aimed at, which now has a file — **E-33 measured the cost NOT to be in the probe**: `-table map` won by 12.81% of wall and 14.51% of user CPU while allocating **120.0 MiB less** (20 tables × 131,072 buckets × 48 B holding 10,000 live entries each), and system CPU fell **5.48%** on an identical I/O path, which is the signature of allocation and which a probe hypothesis cannot explain at all. On top of that, every per-row NEON kernel on this machine pays a measured **1.080 ns/row** vector-to-general transfer (P-01's `neonTransferProbe`), and a probe is per row.

**Depends on.** (a) The probe being a separable, measurable share — E-32 puts `(*table).update` at 12.52-14.05% of non-syscall compute and `runtime.memequal` at 8.81-9.87%, but **no profile in this study separates the probe from the key compare from the update arithmetic**, so the item's pot has never been isolated. (b) The 1.080 ns/row transfer floor, which it shares with P-01 and P-02 — all three go stale together, and §U's toolchain check is the shared test. (c) The 413-station key set being what the headline is measured on. (d) The 10k regime's incumbent, which E-33 moved: a vectorized ARRAY probe there is now measured against `-table map`, not against the array it was designed to beat.

**Revive when.** Either: a profile that SEPARATES the probe from the key compare attributes over ~5% of non-syscall compute to the probe alone — the test is `go tool pprof -peek` naming the probe loop inside `(*table).update`, not the function's total, because the function's total is what has always been quoted and it is not the item's pot; or P-09's `-table quot` arm measures the bucket layout back into play in the 10,000-station regime, which would be the first evidence that layout is worth spending on at all. Absent one of those this entry moves toward `killed`, because the two regimes it was written for have each produced a number pointing elsewhere.

**Cost to revive.** Moderate, most of a day. Nothing exists: a Plan 9 NEON kernel or its SWAR-in-Go equivalent behind `-table vec`, a differential test against `-table combined` over both key sets, the `;`-in-name case against a sibling implementation that this study's three shipped divergences make mandatory, and mutation testing at the probe's width boundary (the value AT the split, not one past it).

**Where.** Nothing built. The hypothesis is H-12 in `06-cross-disciplinary-transfer.md` and the ledger's board item 12; the table it would change is `1brc/code/go/table.go` and the flag is `-table` (`main.go:27`); the transfer floor is `BenchmarkNEONTransferFloor` in `1brc/code/asm/bench_test.go`; the evidence against it is E-33.

---

## P-08 — Queue item 10's other half, the merge and the output at 10,000 stations · `parked`

**What it is.** Price the cross-shard merge and the output formatting when the table holds 10,000 stations instead of 413: **24.21× the entries to merge** (20 shards × 10,000 = 200,000 entry merges against 20 × 413 = 8,260) and 24.21× the lines to sort and format. It is a `-phases` run on `measurements-10k-stations-242m.txt` — no code, and the file exists.

**Why parked — with the number.** The 413-station half is CLOSED and it is nothing: E-20 measured the merge at **5.1 ms, 0.34% of wall**, and E-32 instrumented it directly at **2.28-3.16 ms**. The 10k half is runnable now and was left open by `go-opt-round-3-10k` rather than run, because its ceiling is small and its arithmetic says so: the merge is per-ENTRY while the fold is per-row, so a linear 24.21× of the measured 3.16 ms is **76.5 ms**, **6.0% of that file's own 1.276 s default wall** (E-33) — and that is an upper bound on a step whose 413-station cost is already at `/usr/bin/time`'s resolution. It is parked as a small, cheap, genuinely open measurement, not as a candidate.

**Depends on.** (a) The merge staying per-entry rather than per-row — if it were per-row the 413 measurement would already have shown it. (b) 20 shards, which is `-workers 20`; P-06 would change the entry count this item scales with, so the two interact and P-06 should run first. (c) The 24.21× being entries and not bytes: the 10k file's rows are **57.09 B** against the 1b file's 13.80, so it holds BYTES constant at 241,600,000 rows and the per-row work is a different total. (d) `-phases` not perturbing what it measures — untested for this flag, though E-32 measured `-cpuprofile` costing +13.74% of wall and 0% of the work, so the instrument's own tax is a known hazard here.

**Revive when.** Runnable now; the test is three `-phases` rounds on the 242m 10k file with round 0 discarded as the first-touch outlier, and it revives into real work only if the merge exceeds **~2% of that file's wall (25.5 ms)**. Below that it is closed at BOTH key sets and queue item 10 dies outright, which is the outcome the arithmetic predicts and would be a result worth having.

**Cost to revive.** Very low, under ten minutes. No code, an existing flag on an existing file, and no correctness risk: `-phases` writes to stderr and does not change the output, so the byte-compare is unaffected.

**Where.** Nothing to build. Board item 10 in `07-experiment-ledger.md`; the 413 measurement is E-20 and the direct instrumentation is E-32 (§T); the file is `$ASSETS/measurements-10k-stations-242m.txt` (241,600,000 rows, 13,798,576,606 B, sha256 recorded in `02-baseline-data.txt`, third pass); the flag is `-phases` (`main.go:35`).

---

## P-09 — The quotiented bucket layout in the 10,000-station regime · `parked`

**What it is.** Run `-table quot` — the quotiented 32-byte bucket built and gated by `go-opt-round-2-item13`, an 8-byte name prefix inline in the entry with full names in a side array — on `measurements-10k-stations-242m.txt`. E-28 killed it at 413 stations and wrote its own escape clause: bucket layout re-opens only in a measured 10,000-station regime. That regime now has a file, the arm is already a flag, and **E-33 states plainly that no `quot` arm has been run on it**.

**Why parked — with the number.** At 413 stations it is KILLED-on-numbers: **+5.86%/+4.83% of wall and +6.98%/+6.80% of user CPU**, disjoint from both bracket slots in a 0.98% bracket, against a prediction of 0-4% faster, and its compute floor of 1.0456 s puts the floor back ABOVE the target E-27 had taken it 18.8 ms under. What keeps the 10k half parked rather than killed with it is that the kill was measured where the mechanism is absent: the layout exists to make key compares cheap, and at 413 stations the load factor is **0.315%** so there are almost none, while at 10,000 stations it is **7.63%** (10,000 of 131,072). The pot is real — `runtime.memequal` is **8.81-9.87%** of non-syscall compute (E-32) — but it was measured on the key set where the layout has nothing to do.

**Depends on.** (a) The 10k incumbent, which E-33 MOVED: `-table map` beats the array there by 12.81% of wall, so a `quot` arm now has to beat the map and beating the array is no longer enough. (b) The 7.63% load factor at `-bits 17`; `-bits` is inert for `-table map` (`TestMapLayoutGrowsPastTheBucketCount` pins that) but not for `quot`, so the two arms do not respond to the same knob. (c) E-33's measurement that the 10k win came from allocation — 120.0 MiB, system CPU −5.48% on an identical I/O path — which is evidence that the layout is not the lever in that regime either, and is the reason this entry is parked instead of scheduled.

**Revive when.** One bracketed **mechanism-only** invocation on the 242m 10k file, incumbent first and last: `-a 'default.a=' -a 'quot=-table quot' -a 'map=-table map' -a 'default.b='`, which `experiment.sh -f` stamps `NOT A VERDICT` per E-09 — correctly, because this file ranks mechanisms in the 10k regime and re-ranks nothing about the 413-station defaults. It revives only if `quot` beats `-table map`, disjoint from both bracket slots; beating only `default` closes it instead, because the array is not what that regime would run. Anything it keeps must then be re-measured at 1b before it can become a default.

**Cost to revive.** Low, one short window. The arm exists, is gated and is mutation-tested (`TestHashPathsAgree` covers the quotiented layout so `hashWord` and `hashName` cannot split a station across two buckets). Four arms on the 242m file is `4 × 6 × 21.7 s` ≈ 9 minutes, backgrounded, plus a correctness pass per arm on the 10k expected output.

**Where.** Built: `-table quot` in `1brc/code/go/table.go`, the flag at `main.go:27`. The kill and its escape clause are E-28; the untried-ness is stated in E-33's "What this does NOT say" bullet; the file is as in P-08.
