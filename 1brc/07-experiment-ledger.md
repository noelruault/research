# 07 — Experiment ledger

Append-only. One row per experiment, each with the idea, the prediction made BEFORE the run, the measured result, and a verdict. A row is never edited once its verdict is written; a superseded number gets a new row and a `CORRECTIONS.md` entry. Raw output and every command: [`07-experiment-ledger-data.txt`](07-experiment-ledger-data.txt).

**Verdict vocabulary** (`PARKED.md`'s, plus the two that keep code): `KEEP` (the default now), `KILLED-on-numbers` (records the number AND the baseline it was measured against, because numbers move), `PARKED` (not disproved, no number yet), `SPLIT` (the answer depends on the input and both arms stay).

**The measurement rule this ledger enforces**: a delta is only ever taken between two arms of the SAME hyperfine invocation, because the identical binary measured 1.667 s in one invocation and 1.717 s in another. Cross-invocation numbers are recorded and never subtracted. **[CORRECTED, C6: necessary, not sufficient.** E-16 ran eight IDENTICAL arms in one invocation and they rose monotonically by 21.08%. A 1b invocation must also **sleep 20 s before every timed run** (`experiment.sh -c`, now the default there) and **name the incumbent first and last**, or its arms are ranked by their slots. Rows E-02 to E-08 predate the fix; E-18 prices what that did to them.**]**

## The loop this ledger runs — and where it came from

The optimization rounds run an autoresearch loop: **hypothesis queue → one experiment → one metric → keep/kill → ledger row → the queue is re-ranked**. The adaptation is [`1brc/scripts/experiment.sh`](scripts/experiment.sh), which runs exactly one experiment and refuses the runs that this study has already learned produce uninterpretable numbers.

The primary sources are this repo's own prior art, because it has run this loop before and the traps it walked into are recorded: [`shapes-image-file-format/AUTORESEARCH.md`](../shapes-image-file-format/AUTORESEARCH.md) (the running log plus a ranked bottleneck queue, both in one handover file), [`shapes-image-file-format/METHODOLOGY.md`](../shapes-image-file-format/METHODOLOGY.md) (a pre-run and a post-run checklist), [`shapes-image-file-format/07-method-what-worked.md`](../shapes-image-file-format/07-method-what-worked.md) (the practices that caught real errors) and [`quantization/00-methodology.md`](../quantization/00-methodology.md) (piece-level against integration-level benchmarking). The secondary source is https://github.com/karpathy/autoresearch, fetched 2026-09-01 (see the data companion): an agent edits one file, `train.py`, trains for exactly five minutes, scores one metric, `val_bpb`, and keeps or discards.

**Four things transplant, and each one is a check in the harness.**

- **A fixed experiment budget** — karpathy's is five minutes of training per attempt, which buys ~12 attempts an hour. Ours is one hyperfine invocation on the 1b file: N arms at five runs plus a warmup, so an N-arm experiment costs about `N × 6 × 1.75 s` of timing — 31.5 s for three arms — plus a per-arm correctness pass. **[CORRECTED, C6: the 20 s cooldown is now mandatory at 1b and it dominates the budget — `N × 6 × 21.75 s`, so 130 s for three arms and 17 minutes for eight. The incumbent also appears twice, first and last, so an N-knob experiment costs N+2 arms.]** That is the same trade for the same reason, and it lands on this ledger's binding rule for free, because all the arms are in one invocation.
- **One edit surface.** karpathy's agent only edits `train.py`. Every hypothesis here has instead become a **flag on one binary** — `-split`, `-table`, `-io`, `-parse`, `-kernel` — which is `METHODOLOGY.md` §1.1's "add a pricing function; never replace the baseline" in the form this problem takes. The incumbent is arm 1 of every invocation, so `AUTORESEARCH.md`'s "reproduce the baseline before changing anything" happens inside the run rather than against a published number. **[CORRECTED, C6: arm 1 AND the last arm. Being first is the most flattered slot there is, so an incumbent measured only there is the one arm the invocation cannot judge.]**
- **A predicted number before the run.** `07-method-what-worked.md`: "An investigation that predicts 5 KB and measures 21 KB has learned something; one that only measures has produced a number nobody can interpret." `experiment.sh -p` is mandatory and must contain a digit; a knob-turn with no mechanism is allowed only as `-p 'sweep: <why>'`, which is E-06's precedent and labels the row honestly.
- **A clean negative is a result.** `AUTORESEARCH.md`: "If it does not: add the log entry anyway with the number that killed it." Three of the eleven rows here are `KILLED-on-numbers` and a fourth, E-03, is a `SPLIT` whose 413 half is killed; E-11 is a method rule extracted from E-10's kill, and E-09 from the seven arms that disagreed across two file sizes.

**Two things deliberately do NOT transplant.**

- **The automatic keep/discard.** karpathy's loop compares one scalar and decides. Ours cannot: the verdict vocabulary above has four outcomes and `SPLIT` exists because E-03 and E-05 both depend on the input, so a scalar comparison would have thrown away a flag that is still the right default on one of the two key sets. `experiment.sh` measures, gates and pre-fills the row; **a human or an agent writes the verdict**, and `- **Verdict:**` is emitted blank on purpose.
- **Iterating on a cheaper proxy.** The obvious speedup is to run the loop on the 100m file at a tenth the cost. E-09 measured that it does not rank arms — seven arms, seven disagreements — so `experiment.sh` refuses any file but 1b unless `--mechanism-only` is passed, and then stamps `NOT A VERDICT` on the output. Mechanism evidence at 100m is welcome; verdicts from it are not.

One rule from `quantization/00-methodology.md` is already load-bearing here and needs no new machinery: piece-level and integration-level are **two different measurements**, and E-11 is what happens when a piece-level number is quoted as an integration-level one.

## Where the binary stands

| | measured | source |
|---|---|---|
| 1,000,000,000 rows, current default (20 workers, 1 MiB buffer, `-parse word`, `-fold ptr`) | **1.233 s ± 0.010 s** (range 1.220-1.248, 5 runs, 20 s cooldown, bracket 0.072%) — on AC, preflight green, **not provisional**, and disjoint from both bracket arms. **Reproduced across invocations by E-28, which measured the same configuration at 1.229 s and 1.241 s in its two bracket slots (−0.32% / +0.65%) with user CPU within 0.39%, and again by E-29 at 1.225 s and 1.213 s (−0.65% / −1.62%) with user CPU 14.664 s and 14.647 s, within 0.48% of this row's 14.718 s — three invocations, five slots, a 2.31% total spread and no drift in either direction** **[CORRECTED, C13: within-session only.** E-35 and E-36 re-measured the byte-identical binary the next day, on AC with the preflight green, at 1.281-1.292 s and 1.257 s in two valid brackets, both disjoint from the 1.213-1.241 s band. The reading stands as the best measured wall clock; the spread does not bound session-to-session variation.**]** | E-27, [`bench/2026-09-01T220012Z-…`](bench/2026-09-01T220012Z-queue-items-1-and-5-the-name-hash-fused-into-the-separator-scan-and-the-fold-walked-with-unsafe-Add.txt) |
| 1,000,000,000 rows, the slice-walk default this replaces | 1.424 s ± 0.009 s (range 1.414-1.438, bracket 0.067%) — **superseded by E-27, which measured the same configuration at 1.398 s and 1.399 s in its two bracket slots**; the 1.9% gap between the two readings is cross-invocation and is not a delta | E-25, [`bench/2026-09-01T212026Z-queue-item-16.txt`](bench/2026-09-01T212026Z-queue-item-16.txt) |
| 1,000,000,000 rows, the byte-check default this replaces | 1.510 s ± 0.005 s (range 1.504-1.519, bracket 1.86%) — on AC, preflight green, not provisional; **superseded by E-25, which measured the same configuration at 1.498 s and 1.499 s in its two bracket slots, reproducing this reading to 0.79%** | E-24, [`bench/2026-09-01T204544Z-…`](bench/2026-09-01T204544Z-queue-item-15-follow-up-the-read-buffer-swept-down-confirming-the-one-disjoint-reversal.txt) |
| 1,000,000,000 rows, the 4 MiB default this replaces | 1.613 s ± 0.047 s (range 1.568-1.659, 4 runs, 20 s cooldown) — **superseded by E-24, which measured 4 MiB at +6.56% against 1 MiB in one bracketed invocation**; PROVISIONAL, it was measured on battery | [`bench/2026-09-01T164052Z-E-17-…`](bench/2026-09-01T164052Z-E-17-the-two-knobs-re-measured-with-a-20-s-cooldown-incumbent-at-both-ends.txt) |
| the same invocation's incumbent (15 workers), for the honest delta | 1.729 s and 1.773 s in slots 1 and 4, **−7.49% against the slot-2 interpolation** | same |
| 1,000,000,000 rows, v1 as published by `go-v1-parallel` | 1.742 s ± 0.019 s (range 1.712-1.760, 5 runs) — **`bench.sh`, one command per invocation, so it has no arm slots and carries no slot bias**; what it does carry is its own 5 runs' 2.8% spread, uncorrected | [`bench/2026-09-01T152951Z-v1-parallel.txt`](bench/2026-09-01T152951Z-v1-parallel.txt) |
| 100,000,000 rows | 161.0 ms ± 4.8 ms (15 workers, not re-measured) | same |
| 10,000,000 rows | 27.8 ms ± 0.9 ms (15 workers, not re-measured) | same |
| the skeleton it replaces | 2.607 s at 100m and 260.5 ms at 10m, 26.1 ns/row | `bench/2026-09-01T125649Z-skeleton.txt` |
| target | 1.000 s | `spec.md:5` |
| **the compute floor: 14.718 s of user CPU over 15 cores at perfect parallelism** | **0.9812 s — BELOW the 1.000 s target, for the first time in the study** | E-27, [`bench/2026-09-01T220012Z-…`](bench/2026-09-01T220012Z-queue-items-1-and-5-the-name-hash-fused-into-the-separator-scan-and-the-fold-walked-with-unsafe-Add.txt) |
| the same floor, re-derived from E-29's two incumbent slots | **0.9776 s and 0.9765 s** (14.664 s and 14.647 s of user CPU over 15 cores), reproducing the row above to 0.48% in a second invocation. The wall left above it is **19.85%** and E-29 killed the only board item that claimed a mechanism for it | E-29, [`bench/2026-09-01T230802Z-…`](bench/2026-09-01T230802Z-H-14-board-item-14-double-buffered-workers.txt) |
| the compute floor before E-27, for comparison | 1.152 s from 17.279 s of user CPU (E-25), and 1.235 s from 18.53 s before that (E-20) — **E-27 is the arm that took it under the target**, by 13.49% | E-25 and E-20 |
| the read floor, for comparison | 0.754 s, still BELOW the compute floor, by 227 ms rather than 398 | `02-baseline.md` |
| 1,000,000,000 rows, the 4 MiB default, on AC with the preflight green | 1.50 s and 1.58 s (two runs; a third at 1.91 s is the first-touch outlier) — **not slot-biased and not provisional, but two runs of one configuration, so it was a sanity check on 1.613 s, not a replacement for it** | E-20, same file |

So the binary is **1.23× over target**, and the headline is **no longer provisional**: E-27 measured it on AC with the preflight green, in a bracketed invocation whose two incumbent slots agree to 0.072%. Correctness gate green on both 10m files (413 and 10,000 stations) before every number above, per arm.

**The 1.742 → 1.613 comparison is the weakest number in this table and it is kept only for continuity.** Those are two invocations under two different rules, and neither this ledger nor `spec.md:35` permits subtracting across invocations: E-13's incumbent moved 11.17% between two invocations eight minutes apart with no code change at all. The delta that carries is the **7.49% inside invocation K**, against a bracket interpolated to the same slot.

Derived, from E-27's winning arm: 14.718 s of user CPU for 1.233 s of wall clock is **11.94 cores of 15 busy, 79.6% parallel efficiency**, and **14.72 ns of CPU per row against the 15.00 ns a 1.000 s wall buys — 1.9% UNDER budget, where E-25 was 15.2% over.** The I/O floor measured in `02-baseline.md` is 754 ms, so **61.2% of the current wall clock is the unavoidable read**, up from 53.0%, because the wall fell while the floor did not. (E-25's figures, superseded: 17.279 s for 1.424 s, 12.13 cores, 80.9%, 17.3 ns/row, 53.0%.)

**The two mechanisms measured so far move different quantities, and the table above is what separates them.** E-24 took 6.4% of wall clock without touching user CPU (18.541 s at 4 MiB, 18.604 s at 1 MiB): it bought parallel efficiency, 77% → 82.1%, and closed none of the compute gap. E-25 took 4.97% of wall clock by removing 6.33% of user CPU and parallel efficiency went DOWN, 82.1% → 80.9%, because the same read floor now hides under less compute. Only the second kind of gain lowers the floor the target sits under, which is why the board is ordered by CPU share.

**E-20 corrects the reading of that last sentence, not its arithmetic.** "47% of the wall clock is the unavoidable read" invites the conclusion that the other 53% is the target's problem; the measured compute floor says the read is not the constraint at all. 18.53 s of CPU over 15 cores is 1.235 s of unavoidable *compute*, which is 480 ms MORE than the read floor it would hide behind, so at perfect overlap the read costs nothing and the binary still misses 1.000 s. The remaining rounds have to attack CPU per row — 18.53 ns against a budget of 15.00 — and the queue is now ordered by measured CPU share for that reason.

**E-25 is the first arm to move that floor, and it does not clear it.** 17.279 s over 15 cores is 1.152 s, so 152 ms of per-core compute still has to go before the target is even reachable at perfect parallelism, and the read floor is 398 ms below it rather than 480. 17.28 ns of CPU per row against the 15.00 ns budget: the shape of the problem is unchanged, the margin is 15.2% instead of 23.5%.

**E-27 clears it, and that inverts the shape of the problem the three paragraphs above describe.** 14.718 s over 15 cores is **0.9812 s**, so the compute floor is **18.8 ms BELOW the 1.000 s target** and the per-row CPU is **14.72 ns against the 15.00 ns budget, 1.9% under**. Every sentence in this ledger of the form "the target sits below the compute floor" was true when written and is now superseded: the binary spends little enough CPU per row to finish in under a second at perfect parallelism, and it does not, because it runs at **79.6% efficiency**. **The lever is no longer CPU per row; it is the 20.4% of wall clock between 1.233 s and the floor**, which is scheduling — item 14's territory — and it is the first time that item's ceiling lands under the target rather than on it. The margin is thin enough to say plainly: 18.8 ms of headroom on a 5-run reading is not a promise that 1.000 s is reachable, only that nothing measured now forbids it.

## Round 3 — the verdict, and the honest statement of the gap

Written by `go-opt-round-3-park`, which closes the round. `spec.md:61` makes an honest miss with the ledger intact a valid research outcome and an unmeasured claim not one, so this section is written in those terms: **the target was not reached, by a measured margin, and the reason is named.**

**The best measured wall clock: 1.233 s ± 0.010 s**, range 1.220-1.248 over 5 runs with a 20 s cooldown between them, on `measurements-1b.txt` (1,000,000,000 rows, 13,795,610,267 B, 13.80 B/row). Source E-27, the winning arm of a bracketed invocation whose two incumbent slots agree to **0.072%**.

- **Storage state: uncached.** `F_NOCACHE` set with darwin `fcntl(fd, 48, 1)`, so the reads bypass the page cache. It is NOT "cold": `purge` needs sudo and this loop is unattended, so a genuinely cold-cache number does not exist in this study and the word is never used for one (`02-baseline.md`, and `docs/1brc/lessons.md` carries it as a standing rule).
- **Reader configuration: `-workers 20 -buf 1024 -split static -io pread -nocache=true`** — 20 workers on 15 cores (`NumCPU()*4/3`), each reading its own static byte range with 1 MiB `pread`s. The rest of the shipped default, for reproduction: `-parse word -fold ptr -table combined -bits 17 -kernel row -fill off`.
- **Machine state: AC power, preflight green** — orphan check clear (`pgrep -fl 'hyperfine|experiment.sh'`), measurement lock held, `require_quiet` satisfied. **Not provisional.**

**It reproduced in five independent invocations, which is what the round's verdict actually rests on.** Wall clock, one reading per slot: E-27's arm **1.233 s**; E-28 **1.229 / 1.241 s**; E-29 **1.225 / 1.213 s**; E-31 **1.220 / 1.229 s** (published as their 1.2245 s bracket mean, slots in the row's table); E-34 **1.226 / 1.222 s** — **nine readings, one arm and eight bracket slots**, inside a **2.31%** total spread (1.213-1.241 s) with no drift in either direction. User CPU, over the seven readings that publish it in absolute terms: **14.718** (E-27), **14.664 / 14.647** (E-29), **14.678 / 14.747** (E-31), **14.648 / 14.659** (E-34) — a **0.68%** spread, 14.647-14.747 s, into which E-32's unbracketed three-round mean of 14.6567 s also falls. E-28 publishes its two only as a spread (within 0.39%), which is why it contributes wall readings here and no CPU ones. A bracket slot is a control and never a headline (E-31's rule, and E-32 applies it with more force to an unbracketed instrument run), so the published figure stays E-27's arm; what the slots buy is the right to say it does not drift. **[CORRECTED, C13: it does not drift WITHIN a session.** All nine readings above were taken on 2026-09-01/02 between 22:00Z and 01:12Z. E-35 and E-36 re-measured the same binary at 01:58Z and 02:04Z, in brackets spreading 0.859% and 0.000%, at 1.2865 s and 1.2570 s bracket means, both disjoint from the band. The best measured wall clock is unchanged and the target is missed on either figure, by 23.3% or by 25.7%.**]**

**The gap: +0.233 s, +23.3% over the 1.000 s target. NOT REACHED.**

**The bottleneck, profile-backed and decomposed exactly (E-32).** `wall × 15 cores` is an identity, not a model, so the wall above the compute floor splits with no residual: capacity is **18.1995 core-seconds** = **user 80.53% + system 7.99% + idle 11.48%**, reproduced to the digit in all three instrument rounds. That retires the study's long-standing "19.85% unexplained" (`CORRECTIONS.md` C11) and replaces it with two named halves.

- **8.0 points are system CPU, and they are CLOSED rather than open.** 1.4533 s of kernel time copying 13,795,610,267 B out of the `F_NOCACHE` `pread` into the worker buffers — **9.49 GB/s** [DERIVED; a plausible memcpy rate on this machine, not independently measured]. It scales with bytes and not with call count (13,160 calls), so no buffer size touches it, and the only mechanism that removes a copy is not copying: `-io mmap` is KILLED-on-numbers at **5.6× end-to-end** (H7/E-04, 16 KiB pages giving 842k faults, `MADV_WILLNEED` making it worse). Recovering these points means reversing a kill that has a number on it.
- **11.5 points are idle cores — 1.722 of 15 — and they are open, with a ceiling that misses the target.** The mean number of workers with work to do is **12.85 of 20** against 15 cores. Perfect packing is `(user + sys)/15` = **1.0740 s**: −11.48% of wall and still **+7.40% over target**. That is queue item 18, now parked as **P-06**.
- **Under 0.5% of wall is outside the parallel section.** `wall − max(worker wall)` is −0.6 / +6.0 / −0.8 ms, inside `/usr/bin/time`'s 10 ms resolution, and the cross-shard merge is instrumented directly at **2.28-3.16 ms**. There is no serial tail to attack.
- **The CPU profile, on the shipped default, ranked by share of the NON-syscall samples** (E-26's rule: the syscall bucket holds 55.03-56.50% of samples against 1.45-1.52 s of measured system time, so at most a fifth of it is time in the kernel): `indexDelimAt` **41.57-43.64%** (6.09-6.40 s of 14.6567 s), `parseTempWordFrom` 17.84-19.73%, `(*table).update` 12.52-14.05%, `runtime.memequal` 8.81-9.87%, `foldRowsPtr`'s own flat time 7.39-9.96%, per-row `merge` 1.92-2.59%. `-fold ptr` removed 13.5% of user CPU and left the RANK of all six identical while moving four shares 0.59-1.65 points, so the ordering key survived the arm that invalidated everything else derived from it. Shares rank; pprof's seconds do not (it under-counts total CPU by ~25%).

**What reaching 1.000 s would take, with both bounds and their assumptions named.** Holding today's overhead STRUCTURE intact — the system and idle shares unchanged — user CPU has to fall to **12.080 s, −17.58%** (E-32). Holding system CPU absolute and assuming P-06 delivered PERFECT packing, idle to zero, user CPU would still have to fall to **13.547 s, −7.57%** [DERIVED this cycle from E-32's means; the assumption is a scheduling fix that gives up nothing, which nothing has demonstrated]. Both bounds land on the same conclusion: **scheduling alone cannot get there and neither can any single mechanism now on the board.** The only pot of the required size is the separator scan at 41.57-43.64% of compute, and the board has spent both of its mechanisms on it — queue item 1's fuse is CLOSED not-adopted at +0.81% (E-27) and item 2's batch tokenizer is KILLED at +9.8%/+10.4% (E-10). **No third mechanism is invented here**, which is the same refusal `go-opt-round-3-gap` made and the reason there is no P-entry against that pot.

**What the round did settle.** Four rows — two verdicts, one instrument pass, one stamped NOT A VERDICT — and the round inherited its opening question from E-29, which is a round-2 row. **E-31** answered whether the two overlap mechanisms are substitutes (yes: `-fill off -workers 15` costs +14.09% and `ahead-w15` recovers 72.5% of it) and spent `PARKED.md` P-05's second revive trigger by running it instead of reasoning about it. **E-32** decomposed the gap into system CPU and idle cores, retiring C11 and boarding item 18 with its ceiling. **E-33** measured Go's runtime map at **−12.81%** in the 10,000-station regime, on a byte-constant 241,600,000-row file built for the purpose, mechanism-only. **E-34** killed the same arm at **+52.20%** on the official 413-station set, so the default does not move and **the key set inverts that choice with a 65-point swing**. Queue items **17** and **9** closed here, and item **8** was answered by refusing it (a 1b-row 10k file is 57.09 GB at 2.22× RAM, with a 3.12 s read floor). Of the board's **18 numbered items** — the table also carries one unnumbered row, the kernel's copy, which is a component of the gap and never was a hypothesis — the final state is **13 closed, 1 priced (item 4), 1 subsumed (item 11), 2 parked (items 18 and 12), and 1 split** (item 10, closed at 413 stations and parked for its 10k half). The 13 includes item 8, whose cell reads ANSWERED-by-refusing rather than CLOSED: it is closed in the sense that no arm will ever be built for it on this machine, and the cell says why in bytes. The parked survivors are `PARKED.md` **P-06** (item 18), **P-07** (item 12) and **P-08** (item 10's 10k half), joined by **P-09**, which is not a board item but E-28's own untried escape clause.

**The honest statement, in one paragraph.** The binary reads 13.8 GB and aggregates a billion rows in **1.233 s**, 1.23× the target, at **79.6% parallel efficiency** with **61.2% of the wall clock** being the measured 754.4 ms read floor. Its compute floor is **0.9771 s** (E-32's three-round mean, rounds 0.9793 / 0.9760 / 0.9760) and **0.9812 s** as E-27 measured it — two invocations agreeing to 0.42%, not a range — below the target since E-27, so the target is not forbidden by CPU per row; it is missed on the read, in two forms, two-thirds of which is a kill with a number on it. The remaining open mechanism has a ceiling of 1.0740 s. What would close the gap is a separator scan that costs materially less than 41.6-43.6% of compute, and this study has measured two attempts at it and kept neither. That is the result: a measured miss with every number re-derivable, four survivors parked with tests rather than wishes, and no invented mechanism standing in for the part nobody solved.

## Rows

### E-01 — 8 bytes of station name are enough to hash on (H4)

- **Idea:** hash the first 8 bytes of the name, masked at the separator, and let a full `bytes.Equal` resolve collisions.
- **Prediction** (`03-technique-recon.md`, H4): zero unresolved collisions on the 413 set, manageable on the 10k set.
- **Measured** (`05-go-techniques.md`, a static computation over both key sets, no benchmark): 412 distinct 8-byte prefixes for 413 names — one collision, `Alexandra`/`Alexandria` — and 413 distinct 16-byte prefixes. 10,000 distinct 8-byte prefixes for the 10k set.
- **Verdict: KEEP.** Shipped as `kernel.go:hashWord`. The one collision costs one extra probe on one station.

### E-02 — a shared cursor over 2 MiB segments beats a static equal split (H1)

- **Idea:** hand each worker 2 MiB at a time from an atomic cursor, so the Performance/Efficiency core asymmetry cannot leave the static split's slowest shard setting the wall clock.
- **Prediction** (H1): dynamic wins by **≥5%**; the static split's last shard idles the fast cores.
- **Measured, invocation B (1b):** static **1.667 s** [1.643-1.685], cursor **2.017 s** [1.976-2.058]. Cursor is **21.0% SLOWER**, disjoint over five runs each. At 100m (invocation A) the two are indistinguishable: 152.9 ms [150.9-155.5] against 153.6 ms [152.3-154.3], overlapping ranges.
- **Verdict: KILLED-on-numbers** against static-split-at-1b = 1.667 s in invocation B. **[CORRECTED, C8: RE-OPENED. E-23 re-measured `-split cursor` slot-corrected at −0.56% to +0.06%, overlapping the incumbent: the 21% is gone entirely, and so is E-18's derived +19.18%. Cursor is no longer killed — it is indistinguishable at 5 runs, which is not the same as harmless, so the flag stays and the arm goes back on the queue.]** The prediction was not just wrong in size, it was wrong in direction. The mechanism is not established; the measured symptom is that cursor mode does 21% more wall clock for the same 19 s of user CPU, which points at 15 workers re-entering the same file at unpredictable offsets instead of streaming one contiguous range each. **Revive trigger:** a run where the static split's shards demonstrably finish more than 5% apart, which nothing here measures yet.

### E-03 — splitting the hash array from the entry array (H5)

- **Idea:** probe an array of 8-byte hashes instead of 48-byte entries [CORRECTED, C4: published as 32-byte], so a miss touches a sixth as much memory [CORRECTED, C4: published as "a quarter"].
- **Prediction** (H5): **≥10%** better on the 10k-station file, **≈0%** on the 413 one. `05-go-techniques.md` sharpened the mechanism: at 1<<17 buckets the entry array is 6.00 MiB touched at random [CORRECTED, C4: published as 4 MiB] and the hash array is 1 MiB.
- **Measured, invocation B (1b, 413 stations):** combined **1.667 s** [1.643-1.685], split **1.743 s** [1.726-1.769]. Split is **4.6% slower**, disjoint. At 100m the ranges overlap.
- **Verdict: SPLIT — the 413 half is KILLED-on-numbers** (against 1.667 s) **[CORRECTED, C8: the 413 half is RE-OPENED — E-23 measures `-table split` at +0.87% to +1.49%, overlapping, so the 4.6% that killed it is gone. E-18's derived +0.94% was close here and nowhere else.]**, **the 10k half is PARKED and untested**, because there is no 1-billion-row 10,000-station file: the 10k stressor is a 10m file, where nothing is I/O bound and no wall-clock verdict is meaningful. The flag stays (`-table split`). **Revive trigger:** generate a 1b 10k-station file (the generator does 725 MB/s, so ~20 s) [CORRECTED, C5: 57.09 GB and ~79 s, and at 2.22× RAM it is a different regime — the queue board argues for an equal-BYTES 241.6M-row file instead] and re-run; that is also the only condition under which `05-go-techniques.md`'s working-set hypothesis can be tested end to end.

### E-04 — mmap the whole file (H7)

- **Idea:** the universal choice in the public 1BRC corpus — map 13.8 GB and let the page-fault path do the reading.
- **Prediction** (H7, written after `02-baseline.md` measured mmap losing 5-9× on a *scan*): parallel `pread` stays ahead once aggregation runs too, and `madvise(MADV_WILLNEED)` is the rescue that might change it.
- **Measured, invocation C (1b, own anchor):** anchor `pread` **1.717 s** [1.687-1.759], mmap **9.693 s** [9.619-9.827], mmap **+ `MADV_WILLNEED` 10.782 s** [10.702-10.893]. mmap is **5.6× slower** and the named rescue makes it **11% worse still**. System time tells the story: 18.4 s for mmap against 2.3 s for `pread`. At 100m — a 1.4 GB file that fits in RAM — mmap is *marginally faster* than `pread` (151.2 ms against 152.9 ms, overlapping) and `madvise` is already 18% worse than the default arm (19.6% worse than mmap without it).
- **Verdict: KILLED-on-numbers** against `pread`-at-1b = 1.717 s in invocation C. This is the largest divergence between the public corpus's design and ours, and it is now measured end-to-end rather than on a scan, which is what `spec.md:37` required before anything could be discarded. macOS's 16 KiB pages mean ~842k faults on a path that does not parallelise. **Revive trigger:** a machine with a different page size or a `MAP_POPULATE`-equivalent; `madvise` was the rescue and it is spent.

### E-05 — the branchless temperature parse against a branchy scalar one (H3)

- **Idea:** merykitty's magic-multiply parse, no data-dependent branch.
- **Prediction** (`04-asm-kernels.md`, H3 SPLIT): the winner is set by branch predictability, so only the real 15-shard loop settles it. The microbenchmark had the branchless parse **15.2% SLOWER** than scalar on the 413 corpus.
- **Measured, invocation B (1b):** branchless **1.667 s** [1.643-1.685], scalar **1.857 s** [1.760-2.202]. The scalar arm costs **11.4% more** than the branchless default it replaces, and the scalar arm is by far the noisiest in the invocation (σ 0.193 s against 0.017 s). At 100m the ordering **inverts**: scalar 146.4 ms [143.5-149.7] beats branchless 152.9 ms [150.9-155.5], disjoint.
- **Verdict: KEEP branchless as the default, and the FLAG STAYS. [CORRECTED, C8: this row no longer supports the default. E-23 measures `-parse scalar` at −1.12% to −1.73%, overlapping the incumbent, so the 11.4% handicap this row rests on is gone and branchless is kept only because nothing has beaten it. E-21's finding that `validTemp` costs 18.2% of CPU makes the choice a live question, and item 16 is where it gets answered.]** H3's "it depends on the input" is confirmed and sharpened: it depends on the *scale* too, on the same input. The microbenchmark ranked it backwards, and so did the 100m end-to-end run. Note the branchless arm carries a validation tax the scalar arm does not (`kernel.go:validTemp`, six byte compares, needed because the branchless parse cannot reject a malformed field), so the 11.4% is measured *including* that handicap.

### E-06 — per-worker read buffer: 1 MiB against 4 MiB

- **Idea:** `automataIA/1brc-rs` uses one reusable 4 MiB buffer per worker; `02-baseline.md` only swept 1 MiB against 8 MiB, and benhoyt's Go entries use 1 MiB.
- **Prediction:** none recorded before the run — this was a sweep, not a hypothesis, and it is labelled as such.
- **Measured:** at 1b (invocation B) 4 MiB **1.667 s** beats 1 MiB **1.713 s** [1.700-1.720], disjoint, **2.8%**. At 100m (invocation A) 1 MiB **143.5 ms** [139.8-147.1] beats 4 MiB **152.9 ms**, disjoint, **6.1%** the other way.
- **Verdict: KEEP 4 MiB** (the 1b answer is the one that counts). **[CORRECTED, C8: REVERSED. E-23 and E-24 measure 1 MiB at −6.34% and −6.15%, disjoint from both bracket arms in two independent invocations agreeing to 0.27%, and E-24's sweep makes 1 MiB a minimum (512 KiB +3.97%, 2 MiB +4.44% against it). The default is now 1024 KiB.]** **[CORRECTED, C6: NOT DEFENSIBLE — this arm sat in slot 5 of invocation B and E-18 derives −4.49% once the slot bias is subtracted, i.e. 1 MiB may win. Re-measure is queue item 15. 4 MiB stays the default because E-17 beat 8 MiB with the cooldown on, not because of this row.]** 16 MiB is measured in E-13 and loses.

### E-07 — table size: 1<<14 against 1<<17 buckets

- **Idea:** `05-go-techniques.md` measured load factor mattering (8.5% on the 10k key set) and working-set size plausibly mattering the other way.
- **Prediction:** 1<<14 might win on the 413 set, where 413 entries make load factor irrelevant and a 768 KiB array beats a 6.00 MiB one [CORRECTED, C4: published as 512 KiB against 4 MiB].
- **Measured, invocation B (1b):** 1<<17 **1.667 s**, 1<<14 **1.787 s** [1.765-1.812]. The bigger table is **7.2% faster**, disjoint — against the prediction. At 100m the small table wins by 7.1%, disjoint. Another arm whose direction inverts with scale.
- **Verdict: KEEP 1<<17. [CORRECTED, C6: NOT DEFENSIBLE — slot 6 of invocation B, E-18 derives −1.86%; re-measure is queue item 15.] [CORRECTED, C8: re-measured in E-23 at −0.44% to −1.05%, overlapping. The 7.2% is gone; 1<<17 stays the default because nothing separated from it, not because this row supports it.]** The mechanism is not established. Hypothesis for a later round: with 15 shards live, more buckets means fewer probe collisions in *aggregate*, and the entry array's size stops mattering because only ~413 lines of it are ever hot.

### E-08 — `F_NOCACHE` against the page cache, end-to-end

- **Idea:** `02-baseline.md` measured 15 uncached preads reading 13.8 GB in 754 ms against 1.126 s page-cached, because the file is 53.5% of RAM.
- **Prediction:** the ordering survives once aggregation competes for the same bandwidth.
- **Measured, invocation B (1b):** `F_NOCACHE` **1.667 s**, page-cached **1.759 s** [1.720-1.787], disjoint, **5.5%**. At 100m — 1.4 GB, comfortably resident — the page cache wins by 3.5%, disjoint.
- **Verdict: KEEP `F_NOCACHE`. [CORRECTED, C8: re-measured in E-23 at −0.43% to +0.19%, straddling zero and overlapping — this row's 5.5% is gone end-to-end, and the decision now rests entirely on `02-baseline.md`'s separate 754 ms against 1.126 s, exactly as the C6 note below anticipated.] [CORRECTED, C6: NOT DEFENSIBLE at this margin — slot 7 of invocation B, the worst-biased slot in the study, and E-18 derives −5.35%, a sign change. The decision stands on `02-baseline.md`'s separate 754 ms against 1.126 s (49%), which no bias of this size explains; this row's 5.5% does not stand on its own. Re-measure is queue item 15.]** `02-baseline.md`'s ordering is confirmed end-to-end at 1b and confirmed to invert at 100m, which is exactly the file-size-against-RAM mechanism it named.

### E-09 — the meta-result: a 100m run does not rank a 1b run

- **Idea:** iterate on the 100m file, which is 10× cheaper, and promote the winners.
- **Measured:** **seven arms, seven disagreements.** Every arm above that separates at 1b either inverts at 100m (H3 parse, buffer size, table bits, `F_NOCACHE`) or vanishes into overlapping ranges (H1 cursor, H5 layout, H7 mmap, where mmap is 5.6× slower at 1b and *marginally faster* at 100m).
- **Verdict: KEEP as a binding rule for the remaining rounds.** Development iteration may use 100m; **no verdict may be taken from it**. The mechanism is not mysterious — at 1.4 GB the file is 5.8% of RAM and nothing is I/O bound, at 13.8 GB it is 53.5% and everything is — but the size of the effect is worth the rule. This is `spec.md:37` ("only end-to-end wall clock ranks solutions") biting one level deeper than it was written for: end-to-end at one tenth scale is still a microbenchmark.

### E-10 — the batch tokenizer, end to end (queue item 2)

- **Idea:** `04-asm-kernels.md`'s largest measured win. Scan a window, drain both needles from one compare, aggregate from the drain, rather than rescanning from each row's first byte. Built two ways so the assembly call could be priced: `-kernel batch-neon` calls `asm.DelimMask32` once per 32 bytes, `-kernel batch-swar` drains both needles from each 8-byte word in pure Go and makes no call at all. Neither materialises a token stream; both fold straight into the table, which is strictly less memory traffic than the benchmarked kernel did.
- **Prediction:** the report handed this forward as the shape to build, at **−40.4% / −40.7%** on the official key set, and named it "the single most load-bearing untested assumption in this report". `05-go-techniques.md`'s 1.93 ns Plan 9 call cost predicted `batch-neon` would beat `batch-swar`, the window being ~2.3 rows wide.
- **Measured, invocation D (1b, 413 stations, 5 runs, one hyperfine invocation):** per-row `row` **1.684 s** [1.659-1.713], `batch-swar` **1.849 s** [1.819-1.883], `batch-neon` **1.859 s** [1.805-1.897]. Percentages are relative to the arm being replaced, which here is the incumbent `row`: `batch-swar` costs **9.8% MORE** and `batch-neon` **10.4% MORE**, both disjoint (`row`'s slowest run, 1.713 s, beats either batch arm's fastest, 1.819 s and 1.805 s). The two batch arms do NOT separate from each other. User CPU from the same runs: **18.720 s / 20.706 s / 19.915 s**, so the batch arms genuinely execute more work rather than losing to a scheduling artifact — and the assembly arm does spend less CPU than the pure-Go batch arm, which is the one prediction that survived.
- **Verdict: KILLED-on-numbers** against `row` = 1.684 s in invocation D. Both flags stay for reproducibility, as `-io mmap` and `-table split` did. The default is unchanged; **v1's per-row scan is still the shape**, and the study's biggest microbenchmark win buys nothing where it counts. **Revive trigger:** ~~an input whose names average over ~32 bytes (where `04-asm-kernels.md` measured the window advantage to be largest)~~ [**FALSIFIED, `CORRECTIONS.md` C12** — the report measured the opposite, in this kernel: `04-asm-kernels.md:73` has the batch tokenizer LOSING on the 10k long-name corpus by **+13.8% and +12.1%** across two runs, because "names average 51.1 bytes, so a 32-byte window no longer spans a row; the amortisation inverts into overhead". Long names are this kernel's crossover into loss, not its rescue, and `go-opt-round-3-10k` has since built that input as a 13.8 GB file (mean name length re-measured at **51.1046 B**, `07-experiment-ledger-data.txt` §U.2)], or a drain loop that is not per-row-branchy — two or four windows per compare was already listed as untried, and it is now the row's ONLY live trigger.

### E-11 — why the microbenchmark pointed the wrong way, and a rule that follows

- **Idea:** E-10 is a 50-point swing from the microbenchmark that recommended it (−40% predicted, +10% measured). A verdict that large deserves its cause on the record, not a shrug.
- **Measured, invocation E (100m, mechanism evidence, NOT a verdict — E-09):** `row` **158.3 ms** [155.4-165.2], `batch-swar` **168.7 ms** [166.3-170.7], `batch-neon` **158.8 ms** [155.8-163.4]. At 100m `batch-neon` **ties** the per-row scan (ranges overlap) while `batch-swar` costs 6.6% more, disjoint; at 1b `batch-neon` costs 10.4% more. Per-row user CPU across the two invocations: `row` 18.4 ns at 100m and 18.7 ns at 1b (+1.8%), `batch-neon` 18.5 ns and 19.9 ns (+7.7%). The batch shape degrades roughly four times as much as the per-row shape when the file goes from 5.8% of RAM to 53.5%.
- **Two causes, one measured and one hypothesised.** MEASURED: the microbenchmark's baseline was `TokenizeStagedSWAR` — a *single*-needle scan plus a parse, writing 12 bytes of token per row into an array. This binary's per-row arm has never been that shape; `indexDelim` already scans both needles in one SWAR pass and the fold consumes the row in place. The −40.4% was a true measurement against a baseline the production code does not contain, so it never applied here. HYPOTHESIS, not established: the batch drain's extra per-row state is what makes it more sensitive to memory pressure at 1b.
- **Verdict: KEEP as a method rule.** `spec.md:37` already says only end-to-end wall clock ranks solutions; E-11 adds the part that bites in practice — **a microbenchmark's baseline must be the shape production actually runs, or its delta is a fact about two other programs.** This is E-09's twin: E-09 says a 10x-smaller file does not rank arms, E-11 says a differently-shaped baseline does not either.

### E-12 — more workers than cores (queue item 6)

- **Idea:** `reader.go` runs one goroutine per core, and each alternates `ReadAt` with a fold, so a worker waiting on its read leaves its core idle. `02-baseline.md` measured 11.3 of 15 cores busy. Oversubscribing the pool lets another worker's fold cover that stall without any code change — the cheapest item in the queue, three flag values.
- **Prediction:** if part of the idle 25% is read stall, oversubscription hides it: 3-8%.
- **Measured, invocation F (1b, 413 stations, 5 runs, one hyperfine invocation, [`bench/2026-09-01T163010Z-queue-item-6-more-workers-than-cores.txt`](bench/2026-09-01T163010Z-queue-item-6-more-workers-than-cores.txt)):** `workers15` **1.656 s** [1.638-1.708], `workers20` **1.490 s** [1.466-1.517], `workers30` **1.512 s** [1.498-1.535]. Relative to the arm being replaced, 20 workers is **10.02% faster** and 30 is **8.70% faster**; both are disjoint from the incumbent (its fastest run, 1.638 s, loses to either arm's slowest). **20 and 30 do not separate from each other** — 1.466-1.517 against 1.498-1.535 overlap — so the measurement supports "more than 15", not "exactly 20". User CPU is flat across the three arms (18.619 s / 18.551 s / 18.923 s), which is the tell that this buys overlap rather than cheaper work.
- **Verdict: SUPERSEDED by E-17 — this row's 10.02% is not a clean measurement of the flag.** E-16 measured a **+1.81% penalty on the second slot of an invocation and +5.12% on the third**, so an incumbent placed first is flattered by roughly the size of the effect this row claims. The direction survives re-measurement under the cooldown fix and the size does not; E-17 carries the verdict. Kept here as the raw run, because the flat user CPU across the three arms (18.619 s / 18.551 s / 18.923 s) is still evidence that oversubscription buys overlap rather than cheaper work.

### E-13 — buffer sizes above 4 MiB (queue item 7)

- **Idea:** each worker reads into a private buffer; 4 MiB beat 1 MiB by 2.8% at 1b in E-06 and the top of the range was never tried at that scale.
- **Prediction:** `sweep:` 4 MiB beat 1 MiB by 2.8% at 1b, 8 and 16 MiB untried at that scale. (E-06's rule: a knob-turn with no mechanism is labelled a sweep, not dressed up as a hypothesis.)
- **Measured, invocation G (1b, 413 stations, 5 runs, one hyperfine invocation, [`bench/2026-09-01T163101Z-queue-item-7-buffers-above-4-MiB.txt`](bench/2026-09-01T163101Z-queue-item-7-buffers-above-4-MiB.txt)):** `buf4M` **1.841 s** [1.757-1.911], `buf8M` **1.682 s** [1.649-1.729], `buf16M` **1.888 s** [1.763-2.040]. 8 MiB is **8.64% faster** than the incumbent and disjoint from it (1.729 < 1.757); 16 MiB is **2.55% slower** and its range overlaps the incumbent's, so 16 MiB is not separated from 4 MiB and only 8 MiB is a result. The optimum is interior, which a sweep can find and a mechanism-free extrapolation cannot.
- **Verdict: SUPERSEDED by E-17, and this row is where the round stopped believing itself.** The 8.64% here is the second slot beating the first, which E-16 shows should be **1.81% slower**, not faster — but E-14 then measured the same flag **12.01% SLOWER** in the third slot of another invocation, a 20.65-point swing on one knob. The re-measurement under the cooldown fix is the verdict. The cross-invocation swing this row originally flagged is real and worth keeping: the incumbent here is byte-identical to E-12's `workers15` and measured **1.841 s against 1.656 s**, an 11.17% difference eight minutes apart — but E-16 shows the cause is not the eight minutes, it is that this arm sat in a different slot.

### E-14 — the two kept knobs in combination, which inverted one of them

- **Idea:** E-12 and E-13 each won in its own invocation, and percentages may not be compared across invocations, so the pair had to be measured against the same incumbent in one call before either could ship as a default.
- **Prediction:** E-12 gave 10.02% and E-13 8.64% in separate invocations; if they are independent the pair is worth ~17.8% against the same-invocation incumbent.
- **Measured, invocation H (1b, 413 stations, 5 runs, one hyperfine invocation, [`bench/2026-09-01T163244Z-E-14-the-two-kept-knobs-in-combination.txt`](bench/2026-09-01T163244Z-E-14-the-two-kept-knobs-in-combination.txt)):** `default` **1.715 s**, `w20` **1.671 s** (−2.57%), `buf8M` **1.921 s** (+12.01%), `w20buf8M` **1.935 s** (+12.83%). The prediction is not merely missed, it is contradicted: `-buf 8192` won by 8.64% in E-13 and lost by 12.01% here, a 20.65-point swing on one flag, and the pair of "winners" is the worst arm in the invocation. User CPU rises monotonically with arm position (18.671 / 19.643 / 20.663 / 21.163 s).
- **Verdict: NO VERDICT — the experiment measured its own harness.** An arm's rank tracks the slot it was given, so E-15 re-ran the identical four arms in reversed order and E-16 removed the configuration difference entirely. This row's value is that it is the first place the study measured the same flag twice at 1b and got both signs.

### E-15 — the same four arms, reversed order

- **Idea:** the cheapest possible discriminator between "configuration" and "position": change nothing but the order the arms are named in.
- **Prediction:** E-13 and E-14 disagree by 20.6 points on `-buf 8192`; if arm ORDER within one hyperfine invocation is the confound, reversing it moves the winner by at least 8%.
- **Measured, invocation I (1b, 413 stations, 5 runs, one hyperfine invocation, [`bench/2026-09-01T163352Z-E-15-the-same-four-arms-reversed-order.txt`](bench/2026-09-01T163352Z-E-15-the-same-four-arms-reversed-order.txt)):** in the order `w20buf8M` **1.734 s**, `buf8M` **1.812 s**, `w20` **1.779 s**, `default` **2.024 s**. The winner and loser of E-14 have swapped ends: `w20buf8M` went from worst (1.935 s, slot 4) to best (1.734 s, slot 1), and `default` from best (1.715 s, slot 1) to worst (2.024 s, slot 4) — **an 18.02% swing on a binary with no flags at all**.
- **Verdict: CONFIRMED, and it invalidates every ranking taken this round.** Order explains the results better than configuration does. E-16 measures the effect with the configuration difference removed.

### E-16 — the null experiment: eight identical arms

- **Idea:** name the same binary with the same flags eight times and let hyperfine rank it against itself. Any spread is the harness, the machine, or the method; none of it can be the configuration.
- **Prediction:** E-14 and E-15 rank by slot, not by configuration; eight arms of the SAME binary with the SAME flags will spread by more than 8%, the size of every verdict in this queue.
- **Measured, invocation J (1b, 413 stations, 5 runs each, one hyperfine invocation, [`bench/2026-09-01T163525Z-E-16-the-null-experiment-eight-identical-arms.txt`](bench/2026-09-01T163525Z-E-16-the-null-experiment-eight-identical-arms.txt)):** **1.660, 1.690, 1.745, 1.772, 1.796, 1.811, 1.841, 2.010 s** — monotonically increasing across all eight slots, **+21.08% end to end**, +10.90% by slot 7 with a jump at slot 8, averaging **0.0302 s per slot** over slots 1-7. Per-slot penalties against slot 1: **+1.81%** at slot 2, **+5.12%** at slot 3. User CPU rises with it (18.816 s to 21.948 s, **+16.65%**) for provably identical work, which is the signature of a clock the machine is lowering, not of extra work being executed. ADJACENT slots mostly overlap, which is what a 0.030 s step against a 0.012-0.036 s σ must look like; slot 1 is disjoint from every slot from 3 onward (1.681 < 1.720). The result is the monotone trend over eight slots, not any one neighbouring pair.
- **The confound does not accumulate across the session, which is what makes it fixable.** Slot 1 measured **1.656 s** in invocation F at 32% battery and **1.660 s** here at 21%, five minutes and eleven battery points later — a 0.24% difference. The machine returns to full speed during the gap between invocations (a build, two correctness passes, idle) and degrades again under sustained load. `pmset -g therm` reports no thermal or performance warning level, and on this hardware it reports none at any time, so it is evidence of nothing; the cause is stated as **sustained-load throttling, correlated with load duration and not with battery charge — measured behaviour, unidentified mechanism.**
- **Verdict: KEEP as a binding rule, and E-17 is the fix.** **A 1b arm's measured time is a function of when in the invocation it ran.** Every verdict in this study taken at 1b on battery carries a slot bias of about +1.8% for the second arm and +5.1% for the third; the incumbent has always been named first, so the bias has always flattered it. Checked rather than assumed on the largest such row: E-10 killed the batch tokenizer at +9.80% and +10.39% in slots 2 and 3, which is **+8.00% and +6.81%** after subtracting 0.0302 s per slot — the kill stands, with a smaller margin, and its ranges were disjoint by 0.106 s. Rows under about 6% at 1b (E-06's 2.8% buffer result, E-07, E-08) cannot be defended this way and are marked in the board below.

### E-17 — the fix, and the round's only two verdicts

- **Idea:** if the degradation is caused by sustained load and recovers in the gaps between invocations (E-16), then paying that gap back inside the invocation should remove it. `hyperfine --prepare 'sleep N'` runs before every timed run, warmups included; `experiment.sh -c N` wires it up and defaults to **20 s at 1b**, 0 elsewhere, stamping any 0-cooldown 1b file `NOT SLOT-CORRECTED`. The incumbent is named **twice, first and last**, so the invocation carries its own null control and no separate validation run is needed.
- **Prediction:** E-16 measured 0.0302 s of drift per slot with no cooldown; if 20 s of idle restores the machine, the two default arms bracketing this invocation land within 1% of each other and the knobs can be ranked.
- **Measured, invocation K (1b, 413 stations, 4 runs, 20 s cooldown, one hyperfine invocation, [`bench/2026-09-01T164052Z-E-17-the-two-knobs-re-measured-with-a-20-s-cooldown-incumbent-at-both-ends.txt`](bench/2026-09-01T164052Z-E-17-the-two-knobs-re-measured-with-a-20-s-cooldown-incumbent-at-both-ends.txt)):** `default.a` **1.729 s** [1.681-1.812], `w20` **1.613 s** [1.568-1.659], `buf8M` **1.812 s** [1.777-1.844], `default.b` **1.773 s** [1.733-1.815].
- **The fix works, and the flat user CPU is the evidence, not the wall clock.** The two identical bracket arms differ by **2.54%** across four slots against **18.02%** across four slots without the cooldown, and their ranges overlap. **User CPU is 18.656 / 18.541 / 18.652 / 18.669 s — a 0.69% spread against 16.65% in E-16** for provably identical work, which is the mechanism: with the cooldown the machine is no longer lowering its clock mid-invocation. The prediction asked for the brackets within 1% and got 2.54%, so a residual remains and interior slots are still interpolated against it rather than compared raw. 4 runs, not the usual 5: with the cooldown each timed run costs ~21.7 s instead of ~1.7, the whole invocation took **7 m 17 s** measured, and the machine was at 19% battery.
- **Verdict, `-workers 20`: KEPT.** Against the bracket interpolated to its own slot (1.7437 s) it is **7.49% faster**, and its range is disjoint from both bracket arms (1.659 < 1.681). The default is now `runtime.NumCPU()*4/3` (`main.go:defaultWorkers`), 20 here, pinned by `TestTheDefaultOversubscribesTheCores`, which asserts the DECISION (oversubscribe) and not the number, because E-12 could not separate 20 from 30.
- **Verdict, `-buf 8192`: KILLED-on-numbers.** Against its interpolated slot (1.7583 s) it is **3.05% SLOWER**, and its range overlaps `default.b`'s, so it is not even separated in the losing direction. E-13's +8.64% was the second slot of an uncorrected invocation. The 4 MiB default stands and the flag stays for reproduction. **Revive trigger:** a reader that is no longer one blocking `ReadAt` per worker (queue item 14's double buffer changes what a buffer size means).
- **The new best measured wall clock is 1.613 s ± 0.047 s at 1,000,000,000 rows** **[SUPERSEDED, C8: E-24 measures 1.510 s ± 0.005 s on AC with the preflight green; this arm ran with the 4 MiB buffer E-24 reversed]** — PROVISIONAL, measured on battery, `spec.md:42` requires the headline on AC power. It is 7.41% under the published v1 figure of 1.742 s, but those two numbers come from different invocations under different rules and **the honest comparison is the 7.49% inside invocation K**.

### E-18 — what the slot bias does to invocation B, where six earlier verdicts came from

- **Idea:** E-16 makes every uncorrected 1b invocation suspect, and six ledger rows (E-02, E-03, E-05, E-06, E-07, E-08) were all read off **one** invocation with the incumbent in slot 1. Before round 2 builds on them, price the bias.
- **Measured (the evidence that invocation B has the same drift):** its user CPU rises monotonically with slot — **18.701, 18.979, 20.105, 19.979, 20.585, 20.705, 21.419 s, +14.53%** ([`07-experiment-ledger-data.txt`](07-experiment-ledger-data.txt) §invocation B) — against +16.65% in the null experiment and +0.69% with the cooldown on. That is the signature, in the raw data, of the same machine behaviour.
- **DERIVED, not measured:** subtracting E-16's 0.0302 s per slot from each arm of invocation B gives `H1 cursor` **+19.18%** (raw +21.00), `H5 split` **+0.94%** (raw +4.56), `H3 scalar` **+5.96%** (raw +11.40), `buf 1M` **−4.49%** (raw +2.76), `bits 14` **−1.86%** (raw +7.20), `page cache` **−5.35%** (raw +5.52). Applying one invocation's drift constant to another is an extrapolation and it is labelled as one: E-16 measured 8 arms of 5 runs, invocation B ran 7 arms of 5 runs, and nothing establishes that the constant transfers.
- **Verdict: three rows are NOT DEFENSIBLE and one kill nearly vanishes; no verdict is reversed here, because a derived correction cannot reverse a measurement.** E-02's kill of the shared cursor (+19.18%) and E-05's branchless parse (+5.96%) survive with smaller margins. E-03's split-table kill lands under 1% and is now indistinguishable from its incumbent. **E-06 (4 MiB buffer), E-07 (1<<17 buckets) and E-08 (`F_NOCACHE`) all sit inside the bias and two of them change sign under it** — they must be re-measured under the cooldown before anything is built on them, and that is now the top of the queue for `go-opt-round-2`. Independent support, and the reason this is a re-measure rather than a retraction: `02-baseline.md` measured `F_NOCACHE` against the page cache in a separate scan harness at 754 ms against 1.126 s, a 49% margin no slot bias of this size can explain, and E-17 has now re-measured 8 MiB against 4 MiB with the cooldown on and 4 MiB won.

### E-19 — two measurements voided in ten minutes, and the guard the harness was missing

- **Idea:** not an experiment. `go-opt-round-2` opened by trying to measure and failing twice, and what the failures share is worth a row, because the harness had already been taught the lesson's cousin (E-16, the cooldown) and this is the same class of defect.
- **Measured (the first void):** the cycle before this one was cut off mid-benchmark and left its 17-minute item-15 invocation running at **PPID 1**. The next cycle, not knowing, ran four 1b instrument runs into its arms 1-3 of 8, including the leading `default.a` that E-17's bracket interpolation subtracts against. Killed at 19:25 with six arms unrun ([`bench/2026-09-01T171803Z-*.txt`](bench/2026-09-01T171803Z-queue-item-15-invocation-B-re-measured-with-the-cooldown-on.txt), stamped void in place).
- **Measured (the second void):** `pmset` flipped to AC between attempts, and macOS answered the charger with `backupd`, Spotlight's `mdbulkimport` and a WorkflowKit / `siriactionsd` / ModelCatalogAgent burst at 56%, 23% and 24% CPU. One-minute load **12.79** on 15 cores; the same binary went **1.56 s → 4.54-5.06 s** and **18.56 s → 34.37-38.52 s of user CPU for byte-identical work** ([`bench/2026-09-01T173005Z-*.txt`](bench/2026-09-01T173005Z-round-2-instruments-phases-and-cpuprofile.txt)).
- **Verdict: the provenance header was recording exactly the state that invalidated the numbers, and recording is not refusing.** `lib-provenance.sh` now gates what it used to only describe: an exclusive lock (atomic `mkdir` holding the owner's pid, cleared when that pid is dead so a killed cycle cannot park every later one) makes a second measurement refuse with status 3 instead of overlapping, and `require_quiet` waits a busy machine out 15 s at a time up to 180 s before refusing with status 4, naming the busiest processes. `QUIET_FORCE=1` still measures, and then the header stamps `NOT QUIET`, joining `NOT A VERDICT` and `NOT SLOT-CORRECTED`. The lock is taken **before** the correctness gate, because those runs are 15-core load too. Thirteen mutants, all caught; two survived the first pass and both were real gaps (see the handoff).
- **The transferable rule:** `spec.md:42`'s "the final headline must be measured on AC power" has a trap in it — the *transition* to AC is the worst measuring window there is, and `power: AC Power` in a header does not mean the machine was ready. The headline re-measure must clear the preflight, not just the charger.

### E-20 — where the idle quarter of the machine goes (queue item 3)

- **Idea:** 11.5 of 15 cores busy at 77% parallel efficiency, and nothing measured whether the rest was I/O wait, the merge, or shard skew. The round is defined to start here, and `-phases` (built last cycle) times the two alternating call sites in `foldRange`, each worker goroutine, and the merge.
- **Measured, on AC at load 4.20 with the preflight green and the lock held** — the first number in this study that is neither on battery nor otherwise confounded (twelve of the fourteen files in `bench/` carry `PROVISIONAL`, and the two that do not are this one and the AC-storm file that is stamped void), three runs, run 1 excluded as the first-touch outlier every set here shows ([`bench/2026-09-01T192513Z-*.txt`](bench/2026-09-01T192513Z-queue-item-3-where-the-idle-cores-go.txt)): worker wall spread **1.029-1.038 max/min**, merge **5.1 ms, 0.34% of wall**, read **29.1-30.1% of each worker's wall**, **5.75 of 20 workers blocked in `pread` at any instant**, stream **9.20 GB/s** against the 18.3 GB/s of `02-baseline.md`.
- **Verdict: it is the read, and neither of the other two candidates.** Shard skew is under 4% and the merge is a third of a percent, so two of the three guesses in the queue item are dead on numbers. **Consequences for three queue items, all in this cycle's board:** item 10 (the merge) is CLOSED at 413 stations and open only for the 10k regime; H-11 (item 11, the core-class-weighted split) is SUBSUMED, because its whole mechanism is the finish-time imbalance an equal split leaves and there is at most 3.8% of imbalance to take — E-17's oversubscription to 20 workers already collects it, and `02-baseline.md:15`'s never-tested "a uniform 15-way split is close to optimal" is now tested and true *at 20 workers*; H-14 (item 14, double-buffering) keeps its place as the largest single mechanism but its ceiling is now **measured at 17.6%**, not the 25% derived in `06`.
- **The finding that outranks all of them: this binary is compute-bound at 1b, and the target is below its compute floor.** User CPU is **18.53 s** (the most stable number in the set: 18.53-18.83 s over five runs across three attempts, two of them contaminated) for **1.50 s** of wall, so the floor at perfect parallelism is **18.53/15 = 1.235 s**, while `02-baseline.md`'s read floor is 0.754 s — *below* it. A 1.000 s wall clock buys **15.00 ns of CPU per row** and this binary spends **18.53**, so **19.1% of the CPU has to go**, and no scheduling fix reaches the target: H-14 at its full 17.6% ceiling lands at 1.24 s. Round 3's lever is CPU per row, and that is the queue's new ordering.

### E-21 — what the CPU is actually spent on, and the correctness tax priced at last (queue item 4)

- **Idea:** with the gap now known to be a CPU gap, the profile decides which CPU. `-cpuprofile` on the same configuration, same window, same lock.
- **Measured (shares of 14.20 s of samples, 1.73 s duration, `-top`):** `indexDelim` **33.5%**, `validTemp` **18.2%**, `syscall.rawsyscalln` **15.7%**, `(*table).update` **15.9%**, `parseTempBranchless` **5.8%**, `binary.littleEndian.Uint64` 3.2%. [**CORRECTED, C9: every share on this line is a share of a total that includes the syscall bucket, and it was taken at `-buf 4096`. E-24 then made 1 MiB the default and that bucket went to 54.03-56.66%, so these numbers do not describe the shipped binary and must not be rescaled to it — E-26 re-profiles both settings. The shares that transfer are of the NON-syscall samples: `indexDelim` 39.74% here against 40.88% at 4 MiB and 40.90-44.29% at 1 MiB now.**] Two of those are nested and `-peek` says by how much: inside `update`'s 15.9% sit `bytes.Equal` → `runtime.memequal` at **5.8%** (36.4% of `update`) and `(*table).merge` at **1.7%** (10.7%), so the three do not add. pprof's own total is **24.6% short** of the measured user CPU (14.20 s against 18.83 s), so **its shares rank and its absolute seconds do not** — every figure derived from it below is a share applied to the measured 18.53 s.
- **`(*table).merge` is NOT the merge that queue item 10 is about, and the two must not be read together.** `table.go:81`'s `merge(i, v)` folds ONE measurement into an entry's min/max/sum/count, per row, inlined into `update`; the cross-shard merge item 10 asks about is the drain at `reader.go:208`, which `-phases` times separately at **5.1 ms, 0.34% of wall** (E-20). The 1.7% is per-row aggregation and says nothing about item 10 either way. Two functions named `merge` at two scales is a trap this ledger walked into once already, in the first draft of this row.
- **Cross-check, and the reason the contaminated attempt was kept rather than deleted:** the 19:21 profile, taken with a competing 13.8 GB reader on the machine, gives `indexDelim` 30.2%, `validTemp` 19.1%, the syscall 14.8%, `update` 17.7%, `memequal` 7.5%, parse 8.1%, merge 1.0% — the same order and the same magnitudes. One competitor moves the wall clock and leaves the CPU distribution alone; the AC storm tripled both. A contaminated profile still orders a queue.
- **Verdict on item 4, whose prediction is falsified: `validTemp` alone is 18.2% of CPU, against the item's predicted "3-8% of compute".** Free it and the compute floor is **1.010 s**; free `indexDelim` and it is 0.821 s. So the correctness tax is very nearly the size of the whole remaining gap, and **item 4's own escape clause has fired** — it said "if it is more, the shape of the check should change, not the contract". What `validTemp` costs that on is the FORMAT of the field, the `-?\d{1,2}\.\d\n` the branchless parse cannot check for itself; the numeric range is a separate test, `inRange`, which is already integer-only and already runs on the same row. So the cheaper check has to be a cheaper way to establish the same format, not a range test standing in for it — the difference matters enough that item 16 spells out the wrong version and why it fails. `CORRECTIONS.md` C7 carries the falsified prediction.
- **Why item 7's kill and this profile do not contradict each other, mechanism named:** `rawsyscalln`'s 15.7% is the kernel copying 13.8 GB into user buffers, which scales with **bytes, not with syscall count**, so halving the number of `pread` calls by doubling the buffer could not have bought it — which is exactly what E-17 measured when 8 MiB lost by 3.05%. Item 7 stays closed, and the 15.7% is a floor for any strategy that copies: not copying means `mmap`, which E-04 measured at 6.7 s on this machine.

### E-22 — the two invocations of queue item 15 that produced no verdict

- **Idea:** not an idea, a cost. Item 15 took four invocations to produce two readable ones, and the two failures fail in different ways that the harness now has to tell apart.
- **Measured, L ([`bench/2026-09-01T195758Z-…`](bench/2026-09-01T195758Z-queue-item-15-invocation-B-re-measured-with-the-cooldown-on.txt)):** no timings at all. hyperfine stopped at Benchmark 1 with `Error: The preparation command terminated with a non-zero exit code.` The preparation command is the 20 s cooldown `sleep`. It is not refused here — `hyperfine --prepare "sleep 1" --warmup 0 --runs 2 "true"` exits 0 both foregrounded and backgrounded — it dies when the cycle owning it dies, because the sleep is in the process group and hyperfine reads the signalled exit as a failed preparation. **An eight-arm 1b invocation costs about 1050 s and cannot run in the foreground of a 600 s tool call.**
- **Measured, M ([`bench/2026-09-01T200150Z-…`](bench/2026-09-01T200150Z-queue-item-15-invocation-B-re-measured-with-the-cooldown-on.txt)):** every arm timed, and **the bracket failed: 27.37%.** `default.a` **2.094 s** [1.872-2.531] against a byte-identical `default.b` **1.644 s** [1.608-1.687], all five of slot 1's runs above all five of slot 8's.
- **This is not E-16's drift, and the difference is why M is void rather than correctable.** E-16's signature is user CPU rising monotonically with slot; here user CPU is **flat and unordered** (3.97% max-min, no trend, slot 1 only +2.82% over slot 8) and the entire excess is **system time, +27.2%**. E-16's fix is a per-slot constant you can subtract; nothing here has a per-slot shape to subtract. M's arms 2-7 disagree with N's by up to **11.85 points** (pagecache, +12.04% against +0.19%), so the contamination was not confined to slot 1 either.
- **Verdict: KEEP the bracket as a REFUSAL, not a diagnostic.** E-17 introduced naming the incumbent first and last to make an invocation readable; this row is the first time it declared one unreadable, and it did so on the only invocation of the four whose numbers would otherwise have been believed. A bracket over 3% means no arm in that invocation may be quoted — M's numbers appear in this ledger only as the counter-example above. **Revive trigger for the mechanism:** unknown, and deliberately left unknown — nothing here identifies what made M's first arm cost 27% more system time, and a mechanism guessed from one occurrence is what E-11 warns against.

### E-23 — invocation B's six verdicts, re-measured slot-corrected (queue item 15, CLOSES it)

- **Idea:** six ledger rows (E-02, E-03, E-05, E-06, E-07, E-08) were read off one uncorrected invocation. E-18 priced the bias by DERIVATION and could not reverse anything, because a derived correction cannot reverse a measurement. This measures them.
- **Prediction:** E-18 derived −4.49% for buf 1M, −1.86% for bits 14 and −5.35% for the page cache; if that extrapolation holds, at least one of the three changes sign.
- **Measured, invocation N (1b, 5 runs, 20 s cooldown, lead-in plus bracket, [`bench/2026-09-01T202421Z-…`](bench/2026-09-01T202421Z-queue-item-15-invocation-B-re-measured-with-a-sacrificial-lead-in-arm-absorbing-slot-1.txt)), bracket 0.62%, user CPU flat at 3.29% and unordered.** Percentages are against the arm being replaced, quoted against both bracket arms because only their agreement licenses the reading:

  | arm | row it re-measures | invocation B said | E-18 derived | **measured now** | disjoint? |
  |:---|:---|---:|---:|---:|:---|
  | `-buf 1024` | E-06 KEEP 4 MiB | +2.76% | −4.49% | **−6.34% to −6.92%** | **yes, from both** |
  | `-bits 14` | E-07 KEEP 1<<17 | +7.20% | −1.86% | −0.44% to −1.05% | no |
  | `-nocache=false` | E-08 KEEP F_NOCACHE | +5.52% | −5.35% | −0.43% to +0.19% | no |
  | `-table split` | E-03 KILLED at 413 | +4.56% | +0.94% | +0.87% to +1.49% | no |
  | `-parse scalar` | E-05 KEEP branchless | +11.40% | +5.96% | −1.12% to −1.73% | no |
  | `-split cursor` | E-02 KILLED | +21.00% | +19.18% | −0.56% to +0.06% | no |

- **Verdict: ONE reversal, and five margins that do not reproduce.** `-buf 1024` reverses E-06 outright and is carried by E-24. The other five arms all land **inside their bracket's own spread** — they are now *indistinguishable from the incumbent*, which is a weaker and different claim than "equal": five runs of a 1.6 s program cannot separate a 1% effect, and nothing here says these knobs do nothing. What it does say is that **every margin invocation B reported is gone**, including a 21.00% kill (E-02) and an 11.40% keep (E-05).
- **E-18's derivation is itself falsified, and that is the sharper lesson.** It predicted cursor would survive at +19.18% and split at +0.94%; measured, cursor is **+0.06%** and split **+1.49%**. The split figure is close and the cursor figure is off by 19 points, so the linear per-slot model was not a conservative approximation of invocation B — it was the wrong shape for whatever went wrong there, exactly as M's failure was the wrong shape for it too. **A contaminated invocation cannot be repaired by arithmetic; it can only be re-run.** `CORRECTIONS.md` C8.
- **What this does NOT license.** E-02, E-03 and E-05 are re-opened, not re-decided: `-split cursor` and `-table split` are no longer killed on these numbers, and `-parse branchless` is no longer defended by them, but three overlapping arms are not grounds to change three defaults. They go back on the queue as measurable-at-need. `-nocache` keeps F_NOCACHE on `02-baseline.md`'s separate 754 ms against 1.126 s (49%), which was never this row's evidence and is untouched by it.

### E-24 — the read buffer swept to its minimum (queue item 15 follow-up)

- **Idea:** E-23's one disjoint result changes a default, and E-06 is a standing lesson in what changing a default on one invocation costs. Confirm it, and sweep the neighbours so the new default is a measured minimum rather than the one alternative that happened to be tried.
- **Prediction:** 1 MiB reproduces its −6.34% to within 2 points; if the mechanism is that smaller reads cost less kernel time, 512 KiB is at least as fast and 2 MiB lands between.
- **Measured, invocation O (1b, 5 runs, bracket 1.86%, [`bench/2026-09-01T204544Z-…`](bench/2026-09-01T204544Z-queue-item-15-follow-up-the-read-buffer-swept-down-confirming-the-one-disjoint-reversal.txt)):** `1 MiB` **1.510 s** [1.504-1.519], `512 KiB` 1.570 s [1.566-1.581], `2 MiB` 1.577 s [1.547-1.621], `4 MiB` 1.609 s [1.590-1.645] and 1.639 s [1.589-1.698]. Against 1 MiB: **4 MiB +6.56%, 2 MiB +4.44%, 512 KiB +3.97%**, and 1 MiB's range is **disjoint from every other arm in the invocation**. It reproduces N's reading to **0.27%** (1.506 → 1.510).
- **Verdict: KEEP 1 MiB. E-06 is REVERSED** — 4 MiB was kept on a slot-biased +2.76% and costs 6.56%. `defaultBufKiB` is now 1024, pinned by `TestTheDefaultReadBufferIsTheSweptMinimum`, and 1 MiB is a **minimum**: moving off it loses in both directions, so this is not "smaller is better" and a further sweep down is answered, not owed.
- **The prediction's mechanism is falsified even though its headline held.** 512 KiB was predicted at least as fast and is 3.97% slower, so "smaller reads cost less kernel time" is not it. What is measured is that the gain is **entirely kernel-side**: system time 1.559 s against 2.081 s (**−25.1%**) with user CPU flat. Why a 1 MiB `pread` with `F_NOCACHE` costs less system time than either 512 KiB or 2 MiB is **unestablished**, and it is worth naming as a live question because E-21 measured that same syscall at 15.7% of CPU and argued it scales with bytes rather than call count — this row says the call SHAPE matters too, which that argument did not predict.

### E-25 — the format check folded into the word the parse already loaded (queue item 16, CLOSES it)

- **Idea:** `validTemp` establishes the shape `-?\d{1,2}\.\d\n` by indexing `b[0]`..`b[5]` behind a three-way `switch` on `next`, and `next` varies per row, so the switch is unpredictable and the byte loads are dependent. `parseTempBranchless` already holds those bytes as one 64-bit word. `parseTempWord` folds the whole rejection into that word: the `.` and `\n` at their fixed offsets from the dot lane, a SWAR digit test on the two lanes bounding the digit run plus the lane above the dot, the sign byte when the parse read one, and the digit COUNT — one accumulator, `bad == 0`, and no branch inside the check. The one branch it keeps is `parseTempBranchless`'s own `dot > 28` guard, which is never taken on well-formed input; what left the row loop is the UNPREDICTABLE branch, the three-way switch on a `next` that varies per row. **The contract does not move**: `inRange` still runs, and the byte check is still what establishes the bytes were a temperature (C7).
- **Prediction:** the unpredictable switch and four to six dependent byte loads leave the row loop; **5-12% off the wall**, ceiling 18.2% of user CPU (E-21). The board's pre-registered keep bar was **8%**.
- **Measured, invocation 2026-09-01T212026Z (1b, 5 runs, 20 s cooldown, bracket 0.067%, AC, load 1.96, preflight green, [`bench/2026-09-01T212026Z-queue-item-16.txt`](bench/2026-09-01T212026Z-queue-item-16.txt)):** `incumbent` 1.498 s [1.489-1.510] user 18.414 s, **`word` 1.424 s [1.414-1.438] user 17.279 s**, `incumbent-last` 1.499 s [1.493-1.509] user 18.479 s. Against the bracket mean: **−4.97% of wall, −6.33% of user CPU**, and `word`'s range is **disjoint from both bracket arms**. The two incumbent slots agree to 0.067%, and they reproduce E-24's 1.510 s to 0.79%.
- **Verdict: KEEP, and the default moves to `-parse word`** (pinned by `TestTheDefaultParseFoldsTheCheckIntoTheWord`). The study's best is **1.424 s ± 0.009 s at 1,000,000,000 rows**, not provisional. [**SUPERSEDED by E-27: the best is 1.233 s and the default carries `-fold ptr` as well. This row's own numbers stand; only the "study's best" claim moved.**] Both replaced functions stay in the binary, because they are the arm this was measured against and E-11's rule needs that baseline to stay runnable.
- **The pre-registered 8% bar is MISSED and the arm is kept anyway, which is a judgement and is recorded as one.** 4.97% of wall and 6.33% of CPU are both under it. The bar was written to stop complexity being kept for noise, and this is not noise: it is disjoint inside a 0.067% bracket, it costs one tested function, the contract is unchanged, and the alternative is paying `validTemp`'s 18.2% of CPU for the rest of the study. What would have justified the bar is a marginal or overlapping result, and that is not what came back.
- **The prediction's mechanism is falsified even though its headline nearly held.** 4.97% lands 0.03 points under the 5-12% band, but the reasoning behind "~13%" was that a SWAR form would cost a quarter of the byte check. **Derived from the measured CPU, in percentage points inside one invocation so that no user-CPU figure is subtracted across two:** 18.2% − 6.33% = **11.87% of CPU is what the new check costs, 65.2% of the old one**, not 25%. The unpredictable switch was worth a third of that function, not three quarters, so the remaining 11.87% is now mostly the parse itself and this neighbourhood has **at most 11.9% of CPU left** in it.
- **This is the first arm on the board that removed CPU instead of buying parallel efficiency, and the two show up differently.** E-24 moved the wall 6.4% with user CPU flat and efficiency 77% → 82.1%; E-25 moved the wall 4.97% with user CPU −6.33% and efficiency 82.1% → **80.9%**, because the unchanged 754 ms read floor now hides under less compute. A falling efficiency number is the expected shape of a real CPU win here, not a regression, and reading it as one is the trap this row exists to name.
- **What it does NOT do:** clear the floor. 1.152 s of compute at perfect parallelism is still above 1.000 s, so the target remains unreachable without another CPU cut of at least 152 ms per core, and the largest pot on the board (`indexDelim`, 33.5% [**CORRECTED, C9: 40.9-44.3% of compute; the 33.5% was a share of a total that included the syscall bucket at a buffer size the binary no longer uses**]) is untouched.

### E-26 — the re-profile queue items 1 and 5 owed, which falsified the share it was asked to refresh

- **Idea:** the backlog line for items 1 and 5 says to re-profile before quoting a share, because E-21 was one arm stale. It is worse than stale: it was rescaled across a change to the reader.
- **Measured (three runs of the shipped default on 1b, AC power, load 1.59, raw output in `07-experiment-ledger-data.txt` §P):** user CPU **17.16 / 17.30 / 17.39 s**, mean **17.283 s** — which confirms E-25's *derived* incumbent of 17.279 s to **0.02%**, the first independent check of that derivation. Shares of samples on the shipped default: `syscall.rawsyscalln` **54.03% / 56.66%**, `indexDelim` **20.36% / 17.73%**, `parseTempWord` 8.13% / 9.41%, `(*table).update` 5.68% / 5.35%, `memequal` 3.31% / 3.76%, `binary.littleEndian.Uint64` 2.88% / 2.53%.
- **The board said `indexDelim` was 35.9%. It is 17.73-20.36%, and the binary did not change.** The same binary at `-buf 4096` — E-21's setting — profiles at `indexDelim` **31.27%** and the syscall **23.51%**, reproducing E-21. E-24 made 1 MiB the default between the two profiles, and the syscall bucket went 15.7% → 54-57%, compressing every other share against a denominator that is mostly not compute. `CORRECTIONS.md` C9.
- **The figure that transfers is the share of NON-syscall samples, and it is stable across both buffer settings:** `indexDelim` 39.74% (E-21, back-derived), 40.88% (4 MiB now), 44.29% and 40.90% (1 MiB now) — a 4.6-point spread across three profiles at two buffer sizes. Applied to 17.283 s of measured user CPU: **7.1-7.7 s**. The syscall bucket is excluded on evidence rather than convenience: it holds **7.51 s** of samples in a run whose entire measured **system** time is **1.52 s**, so at most a fifth of it can be time in the kernel. The mechanism (darwin's process-wide `setitimer(ITIMER_PROF)` delivering SIGPROF to a thread parked in `pread`) is a **hypothesis, not verified**, and is the same unidentified cause E-21 measured as a 24.6% under-count.
- **Item 1 is re-priced by a factor of about 25, and this is the row's real product.** The fuse cannot remove the scan; it removes the second load of the word the scan already had. `-peek` attributes that load exactly: of `littleEndian.Uint64`'s samples, `foldRows`'s own share is **0.11 s of 6.39 s of compute (1.72%)** at 1 MiB and **0.10 s of 9.37 s (1.07%)** at 4 MiB. Item 5's target, `foldRows`'s own flat time, is **6.62-8.76%** of compute over the three profiles.
- **Verdict: the board's ordering key becomes share of non-syscall samples, and item 1 goes to the invocation with a prediction of "expected not to separate" rather than one of "the biggest pot".** No arm is decided here. What this row buys is that E-27's prediction is made against a measurement instead of against a rescaled one, which is the whole reason the backlog line demanded it.

### E-27 — the row loop walked with `unsafe.Add`, and the hash fused into the scan (queue items 1 and 5, CLOSES both)

- **Idea:** the two items attack the same loop and overlap, so they run in one invocation with their combination, and the overlap is measured instead of assumed additive.
- **Prediction, made against E-26 rather than against the board:** `hash` **0-1.5% of wall and expected INSIDE the bracket** (its pot is the second load, 1.07-1.72% of compute); `ptr` **1-4%** against `foldRows`'s 6.6-8.8% flat; `both` sub-additive at **1-5%**. Pre-registered keep bar: **disjoint from BOTH bracket arms or it is not kept.**
- **Measured (1b, 5 runs, 20 s cooldown, AC, load 1.38, preflight green, bracket **0.072%**, user CPU unordered across slots, [`bench/2026-09-01T220012Z-…`](bench/2026-09-01T220012Z-queue-items-1-and-5-the-name-hash-fused-into-the-separator-scan-and-the-fold-walked-with-unsafe-Add.txt)):**

  | arm | wall | range | user CPU | vs incumbent.a / .b (wall) | vs .a / .b (user CPU) | disjoint from both? |
  |:---|---:|:---|---:|---:|---:|:---|
  | `incumbent.a` | 1.398 s | 1.391-1.404 | 17.013 s | — | — | — |
  | `hash` | 1.331 s | 1.310-1.371 | 15.945 s | **−4.79% / −4.86%** | −6.28% / −6.48% | **yes** |
  | `ptr` | **1.233 s** | 1.220-1.248 | **14.718 s** | **−11.80% / −11.87%** | **−13.49% / −13.67%** | **yes** |
  | `both` | 1.243 s | 1.227-1.285 | 14.702 s | −11.09% / −11.15% | −13.58% / −13.77% | **yes** |
  | `incumbent.b` | 1.399 s | 1.388-1.408 | 17.049 s | — | — | — |

- **Verdict: `ptr` KEPT and the default moves to `-fold ptr`** (pinned by `TestTheDefaultFoldIsThePointerWalk`). The study's best is **1.233 s at 1,000,000,000 rows**, and it beats `hash` disjointly too (−7.36%, ranges not overlapping). All three arms clear the pre-registered bar; `ptr` is the default because it is the fastest of them and `both` does not separate from it.
- **The prediction is falsified on the upside, by 3x on each arm, and the reason is a mechanism no board item named.** `hash` was predicted inside the bracket and measured −4.79% disjoint; `ptr` was predicted 1-4% and measured −11.80%. Building item 1's arm required a second copy of the scan (`indexDelimAt`, pointer arguments and a rotated loop that peels its first word), and THAT is where most of the win is, not in the fuse it was built for.
- **Item 1's fuse, isolated exactly: it does not separate.** `both` and `ptr` are the same function with one `bool`, so their difference is the fuse and nothing else: **+0.81% of wall and −0.11% of user CPU**, ranges overlapping, **the two channels disagreeing in SIGN**, which is what no effect looks like. That is E-26's 1.07-1.72% ceiling and item 1's own original prediction ("small, the hash already reads only one word") both confirmed. The board's "it is where the CPU is" was the falsified claim, and `CORRECTIONS.md` C9 carries it.
- **Derived decomposition, with its assumption named.** Taking the fuse at zero (measured on the pointer walk, ASSUMED to behave the same on the slice walk), the scan rewrite alone is `hash`'s **−6.28% of user CPU** and the pointer walk adds **−7.2 points** on top of it. So **40.6% of the wall-clock win (4.79 of 11.80 points) is reachable without unsafe pointer arithmetic in the row loop** — which is the board's own "half of that was reachable in safe Go" clause for item 5, measured, and slightly under half.
- **The finding that outranks the arms: the compute floor is now BELOW the target for the first time in the study.** 14.718 s of user CPU over 15 cores is **0.9812 s**, against 1.152 s at E-25 and 1.235 s at E-20. The 1.000 s target is **18.8 ms under the floor rather than 152 ms over it**, so it is no longer provably unreachable, and every "the target sits below the compute floor" statement in this ledger is superseded. Parallel efficiency 80.9% → **79.6%**, which is again the expected shape of a CPU win under an unchanged 754 ms read floor, not a regression.
- **What it does NOT do:** reach the target. 1.233 s of wall against a 0.9812 s floor is 20.4% of scheduling to recover, which is now exactly item 14's territory and is the first time that item's ceiling lands under 1.000 s. [**E-29 spent that territory and recovered none of it: item 14 is killed and the 19.85% still standing between wall and floor has no named mechanism. `CORRECTIONS.md` C10.**]

### E-28 — the quotiented 32-byte bucket (queue item 13, CLOSES it)

- **Idea:** replace the 48-byte bucket's 24-byte `[]byte` header with the name's first 8 bytes inline, put the full names in a dense side array, and let 65.9% of rows (272 of the 413 official names are 8 bytes or shorter) settle on a word compare that never leaves the bucket. Borrowed from k-mer count tables, and the last untried member of E-03's neighbourhood.
- **Prediction, pre-registered:** **0-4% of wall**, against `memequal` at 7.2-8.7% of compute and `(*table).update` at 12.4% (E-26). Keep bar: **disjoint from BOTH bracket arms or it is not kept.**
- **Measured (1b, 5 runs, 20 s cooldown, AC, load 2.76, preflight green, bracket **0.98%**, [`bench/2026-09-01T223508Z-…`](bench/2026-09-01T223508Z-queue-item-13-H-13-quotiented-32-byte-bucket.txt)):**

  | arm | wall | range | user CPU | vs incumbent.a / .b (wall) | vs .a / .b (user CPU) | disjoint from both? |
  |:---|---:|:---|---:|---:|---:|:---|
  | `incumbent.a` | 1.229 s | 1.226-1.238 | 14.660 s | — | — | — |
  | `quot` | 1.301 s | 1.290-1.310 | 15.684 s | **+5.86% / +4.83%** | **+6.98% / +6.80%** | **yes, and in the wrong direction** |
  | `incumbent.b` | 1.241 s | 1.239-1.243 | 14.686 s | — | — | — |

- **Verdict: KILLED-on-numbers, against the baseline above.** The arm is disjoint from both bracket slots and it is SLOWER: +5.34% against the bracket mean of 1.235 s, predicted 0-4% faster. Both channels agree in sign — wall +4.8 to +5.9%, user CPU +6.8 to +7.0% — which is what a real CPU regression looks like rather than a slot artifact. `-table quot` stays in the binary as the arm this was measured on, the way `-table split` and `-kernel batch-*` did.
- **The floor moves the wrong way too, and that is the sharpest way to say it.** 15.684 s of user CPU over 15 cores is a **1.0456 s compute floor**: this arm puts the floor back ABOVE the 1.000 s target that E-27 took it 18.8 ms under. An arm that raises the compute floor cannot reach the target at any parallel efficiency.
- **Why, DERIVED and not profiled — no profile was taken, and the two candidate mechanisms are not separated by this row.** At 413 stations in 131,072 buckets the load factor is 0.32%, so a probe touches ONE cache line per lookup whatever the bucket size: 48 → 32 bytes removes no cache line, because the buckets a shard actually touches are scattered one per line either way. What the layout does add is real: a second dependent load through `keys[ord]` for the 34.1% of rows whose names exceed 8 bytes, plus the `nlen` compare, against an incumbent whose `[]byte` header already sits in the line it just read. The shrink is a win only for a table dense enough to put two live buckets in one line, which 413 stations are not, and the 10,000-station regime that might be is queue item 8's file, which does not exist.
- **Two independent losses now bound the neighbourhood.** E-03 lost 4.6% splitting the hashes out of the entry; this loses 5.3% shrinking the entry. The board's own framing for this item was "if it loses too then the neighbourhood is exhausted rather than untried", and it did: **bucket layout is closed at 413 stations**, and only a measured 10k-station regime (item 8) can re-open it.
- **The control this invocation bought for free.** Its two incumbent slots reproduce E-27's headline ACROSS invocations — 1.229 s and 1.241 s against 1.233 s, −0.32% and +0.65%, user CPU within 0.39% — so the refactor this arm needed (the `update` signature carrying the masked word, the layout dispatch branch, `hashWord` split into `maskWord` and `mixWord`, all inlined) costs the incumbent nothing measurable. hyperfine flagged statistical outliers on `incumbent.a`; the bracket is 0.98%, well inside the 3% refusal threshold, so the invocation is quotable.

### E-29 — the double-buffered worker (queue item 14, H-14, CLOSES it)

- **Idea:** `foldRange` reads, waits, folds, alternating in one goroutine. Give each worker a fill-ahead goroutine so chunk N+1 is in flight while chunk N folds, and the 29-30% of worker wall E-20 measured blocked in `pread` stops being wall clock. Borrowed from DPDK's fill-while-you-process; the last and largest single mechanism on the board, and after E-27 the only one whose ceiling landed under 1.000 s.
- **Prediction, pre-registered:** **−11% to −20.4% of wall** against the incumbent's 1.233 s, landing **0.98-1.10 s**, with user CPU flat to +3% because the mechanism buys parallel efficiency (79.6% toward ~95%) and removes no compute. Keep bar: **disjoint from BOTH bracket arms or it is not kept.** The buffer is re-swept rather than inherited, because E-24's 1 MiB minimum was measured on the unstaged syscall shape.
- **Three arms, not two, and that is what makes the row readable.** `sync` is the same rewritten loop — fixed-size reads at fixed offsets into a buffer reserving `maxRow` at the front for the carry — reading INLINE, with the same two buffers. `ahead` is that loop with the reads in a goroutine. The pair differs by the goroutine alone, so the overlap is isolated from the read-shape rewrite it needed to exist at all (E-27's lesson, applied before the fact this time).
- **Measured (1b, 5 runs, 20 s cooldown, AC, load 1.95, preflight green, bracket **0.989%** on wall and **0.116%** on user CPU, [`bench/2026-09-01T230802Z-…`](bench/2026-09-01T230802Z-H-14-board-item-14-double-buffered-workers.txt)):**

  | arm | wall | range | user CPU | vs incumbent.a / .b (wall) | vs .a / .b (user CPU) | disjoint from both? |
  |:---|---:|:---|---:|---:|---:|:---|
  | `incumbent-a` | 1.225 s | 1.217-1.234 | 14.664 s | — | — | — |
  | `sync` | 1.229 s | 1.224-1.239 | 14.663 s | +0.33% / +1.32% | −0.01% / +0.11% | no, overlaps both |
  | `ahead` | 1.232 s | 1.222-1.244 | 14.653 s | **+0.57% / +1.57%** | **−0.08% / +0.04%** | **no, overlaps both** |
  | `ahead-buf512` | 1.303 s | 1.296-1.312 | 15.141 s | +6.37% / +7.42% | +3.25% / +3.37% | yes, and slower |
  | `ahead-buf2048` | 1.246 s | 1.192-1.322 | 14.578 s | +1.71% / +2.72% | −0.59% / −0.47% | no, σ 0.050 s |
  | `ahead-buf4096` | 1.313 s | 1.287-1.343 | 14.779 s | +7.18% / +8.24% | +0.78% / +0.90% | yes, and slower |
  | `ahead-w15` | 1.212 s | 1.178-1.234 | 14.545 s | −1.06% / −0.08% | −0.81% / −0.70% | no, overlaps both |
  | `incumbent-b` | 1.213 s | 1.192-1.236 | 14.647 s | — | — | — |

- **These arms were measured on a reader the review gate then repaired, and E-30 re-runs them.** Mutation-testing this cycle's own arm found that the `maxRow`-sized carry prefix made the staged reader reject rows between `maxRow` and the buffer size alignment-dependently. The fix touches the timed path, so **quote E-30 for the shipped binary**; it reproduces every number here (`ahead` −0.57% across invocations, the `ahead`/`sync` pair inside the bracket both times) and the verdict below is unchanged.
- **Verdict: KILLED-on-numbers, against the baseline above.** No arm clears the pre-registered bar, and the direction is wrong: `ahead` is +1.07% against the bracket mean of 1.219 s, having been predicted 11-20.4% faster. `-fill off | sync | ahead` stays in the binary as the arm this was measured on, the way `-table quot`, `-table split` and `-kernel batch-*` did, and `defaultFill` stays `off`.
- **The overlap measured ZERO, and the `sync`/`ahead` pair is what says so rather than the incumbent comparison.** Those two arms are one `bool` apart — same loop, same two buffers, same fixed-size reads — and they land **0.24% apart on wall and 0.07% apart on user CPU**, both inside the 0.989% bracket. The read-shape rewrite is likewise free and likewise worth nothing: `sync` is +0.82% against the bracket mean with user CPU flat to 0.11%. So this is not "the mechanism helped and something else cost it back". Nothing happened.
- **Parallel efficiency is the number that refutes the prediction outright.** It was supposed to go 79.6% toward ~95%. Measured: **79.8% and 80.5% for the two incumbent slots, 79.3% for `ahead`, 80.0% for `ahead-w15`** — a spread of 1.2 points across every arm in the invocation, in both directions. The 0.242 s of wall between the 1.219 s bracket mean and the **0.9770 s** compute floor (14.664 s and 14.647 s of user CPU over 15 cores, reproducing E-27's 0.9812 s to 0.4%) is still there, still **19.85% of wall**, and per-worker prefetch does not touch it.
- **Why, DERIVED and not profiled.** The read is ALREADY overlapped with folding — across workers rather than inside one. E-17 made the default 20 workers on 15 cores for exactly this reason ("a worker blocked in its read leaves its core to another's fold"), so the Go scheduler is already filling a blocked worker's core with someone else's compute, and a second overlap mechanism stacked on a saturated first one has nothing left to hide. E-20's "29-30% of worker wall is blocked in `pread`" is true and was never the same claim as "29-30% of WALL CLOCK is idle": it is per-worker wall on a machine running 20 workers on 15 cores, which is what oversubscription is for.
- **The one arm that would have TESTED that reading was not run, and the row says so rather than implying it.** `-fill off -workers 15` in this same invocation would isolate it: if the two overlap mechanisms are substitutes, the unstaged 15-worker arm should lose to `ahead-w15` by roughly what E-17 measured (7.5%), and if they are independent it should not. What the invocation does show is consistent with substitution and no more than consistent — `ahead-w15` is the fastest arm here (1.212 s) with the lowest user CPU (14.545 s), i.e. once each worker overlaps its own read, oversubscribing the cores stops paying — but it does not separate from `incumbent-b` (−0.08%, ranges overlapping), so it buys nothing either. E-17's 7.5% cannot be quoted forward against it: that number predates E-24, E-25 and E-27, which is E-11's rule.
- **E-24's 1 MiB survives the re-measure the ticket demanded, and that is the row's one positive result.** Under the staged reader's different syscall shape — constant-size reads at fixed offsets instead of reads shrunk by the carry — 512 KiB is **+5.76%** and 4 MiB **+6.57%** against `ahead` at 1 MiB, both disjoint from it, and 4 MiB carries the invocation's highest system time (2.473 s against 1.479-1.629 s). 2 MiB is +1.14% with σ 0.050 s, the only unstable arm in the set, so it is indeterminate rather than a loss. The default was not inherited; it was re-measured and it held.
- **Consequence for the board: item 14 was the last item with a derived path to 1.000 s, and it has none.** The gap to close is 19.85% of wall that is not compute and not read latency a prefetch can hide. `go-opt-round-3` inherits a board whose remaining candidates are `PARKED.md` P-05 (core-class-weighted split, subsumed from H-11) and the untried queue items, none of which has a ceiling derived against this baseline. E-20's shard-skew measurement (1.029-1.038) already excludes the obvious explanation for the gap, so round 3 starts by finding out what the 0.242 s IS, not by spending an arm on a guess.

### E-30 — the same arm after the review gate repaired the reader (confirms E-29)

- **Why it exists:** E-29's own cycle mutation-tested the arm it had just measured and found a defect in it. The staged reader reserved `maxRow` at the front of each buffer for the carry, so it rejected rows between `maxRow` and the buffer size **only when the chunk boundary happened to leave more than `maxRow` of one trailing** — the same file was accepted at one `-workers` and rejected at another, where `-fill off` accepted it throughout. The repair is three changes, all in the range-ownership arithmetic: the prefix is a whole chunk, the previous buffer is released after its carry has been copied out of it rather than at the end of its own iteration, and one `bytes.IndexByte` per chunk rejects an over-long row deterministically. That touches the timed path, so **E-29's numbers no longer described the shipped binary and the arm was re-run rather than quoted forward** (E-11).
- **Prediction, pre-registered:** the repair is confined to range ownership, so E-29 stands and `ahead` lands within 2% of the incumbent without separating from it. **A reversal here would mean E-29 had measured the bug and not the mechanism.**
- **Measured (1b, 5 runs, 20 s cooldown, AC, load 2.49, preflight green, bracket **0.905%** on wall and **0.307%** on user CPU, [`bench/2026-09-01T234120Z-…`](bench/2026-09-01T234120Z-H-14-re-measured-after-the-review-gate-repaired-the-staged-reader.txt)):**

  | arm | wall | range | user CPU | vs incumbent-a / -b (wall) | vs -a / -b (user CPU) | disjoint from both? |
  |:---|---:|:---|---:|---:|---:|:---|
  | `incumbent-a` | 1.215 s | 1.206-1.228 | 14.675 s | — | — | — |
  | `sync` | 1.235 s | 1.222-1.255 | 14.739 s | +1.65% / +0.73% | +0.44% / +0.13% | no, overlaps both |
  | `ahead` | 1.225 s | 1.215-1.237 | 14.733 s | **+0.82% / −0.08%** | **+0.40% / +0.09%** | **no, overlaps both** |
  | `incumbent-b` | 1.226 s | 1.212-1.237 | 14.720 s | — | — | — |

- **Verdict: E-29 CONFIRMED, H-14 stays killed.** `ahead` is +0.37% against the bracket mean of 1.221 s and its range covers both incumbent slots. The isolating pair reproduces: `ahead` against `sync` is **−0.81% on wall and −0.04% on user CPU**, inside the 0.905% bracket, sign flipped from E-29's +0.24% and both readings well under the noise — which is what "no effect" looks like measured twice. Parallel efficiency 80.5% and 80.0% for the incumbents against **80.2%** for `ahead`.
- **The repair cost the arm nothing, which is the second thing this row buys.** `ahead` measured 1.232 s at E-29 and **1.225 s** here, −0.57% across invocations, and `sync` 1.229 s against 1.235 s, +0.49%. So the answer to "did E-29 measure the bug or the mechanism" is the mechanism: the extra `IndexByte` per chunk and the doubled prefix allocation are both invisible at this resolution, and the earlier row's conclusion transfers intact.
- **The compute floor and the gap are unchanged.** 14.675 s and 14.720 s of user CPU over 15 cores are **0.9783 s and 0.9813 s**, a fourth and fifth independent reproduction of E-27's 0.9812 s. The wall above the floor is **19.72%**, against 19.85% at E-29.

### E-31 — are the two overlap mechanisms substitutes? (queue item 17, CLOSES it, and it answers E-29's open question)

- **Idea:** E-29 killed `-fill ahead` against the oversubscribed default and explained it DERIVED, not measured: E-17's 20-workers-on-15-cores is already overlapping the read across workers, so a per-worker prefetch has nothing left to hide. E-29's own row named the one arm that would test that and said it had not been run — `-fill off -workers 15`. This is the 2x2 that runs it: if the mechanisms are substitutes, removing oversubscription must cost real wall clock and adding the prefetch back must recover most of it; if they are independent, `ahead-w15` stays as far behind as `w15`.
- **Prediction, pre-registered:** `w15` loses **3-8%** against the 20-worker default and `ahead-w15` recovers most of it, landing **within ~2%** of default, while `ahead-w20` stays inside E-30's no-effect band (+0.82%/−0.08%).
- **Measured (1b, 5 runs, 20 s cooldown, AC, load 1.98, preflight green, bracket **0.738%** on wall and **0.470%** on user CPU, [`bench/2026-09-02T000710Z-queue-item-17.txt`](bench/2026-09-02T000710Z-queue-item-17.txt), arithmetic in `07-experiment-ledger-data.txt` §S):**

  | arm | wall | range | user CPU | vs default.a / .b (wall) | vs .a / .b (user CPU) | parallel efficiency |
  |:---|---:|:---|---:|---:|---:|---:|
  | `default.a` (`-fill off -workers 20`) | 1.220 s | 1.214-1.228 | 14.678 s | — | — | 80.2% |
  | `w15` (`-fill off -workers 15`) | 1.397 s | 1.380-1.441 | 14.598 s | **+14.51% / +13.67%** | −0.55% / −1.01% | **69.7%** |
  | `ahead-w15` | 1.272 s | 1.269-1.275 | 14.693 s | **+4.26% / +3.50%** | +0.10% / −0.37% | 77.0% |
  | `ahead-w20` | 1.231 s | 1.219-1.245 | 14.743 s | +0.90% / +0.16% | +0.44% / −0.03% | 79.8% |
  | `default.b` | 1.229 s | 1.216-1.240 | 14.747 s | — | — | 80.0% |

- **Verdict: SUBSTITUTES, and E-29's derived reading is CONFIRMED on both channels.** Dropping oversubscription costs **0.1725 s** against the 1.2245 s bracket mean (+14.09%, and `w15`'s 1.380-1.441 s range is disjoint from both slots). Putting the per-worker prefetch back recovers **0.1250 s of that 0.1725 s, 72.5%**, leaving `ahead-w15` +3.88% over the bracket mean — **also disjoint from both slots** (1.269-1.275 against maxima of 1.228 and 1.240), so it is a measured loss and not a slot artifact. Stacked on top of oversubscription the same prefetch recovers **nothing**: `ahead-w20` is +0.53% against the bracket mean with its range overlapping both slots' (1.219-1.245 against 1.214-1.228 and 1.216-1.240 — overlapping, not covering), reproducing E-29 (+1.07%) and E-30 (+0.37%) a third time. A mechanism worth 72.5% of a 14% gap on its own and 0% in the presence of another is the definition of a substitute.
- **Parallel efficiency is the second channel and it moves as a ladder, not as one arm.** 80.2% and 80.0% for the two incumbent slots, **69.7%** for `w15`, **77.0%** for `ahead-w15`, 79.8% for `ahead-w20`: removing oversubscription costs 10.4 points, the prefetch buys 7.3 of them back (70.2% of the drop, which is the 72.5% wall-clock recovery measured on a second channel), and adding the prefetch to the oversubscribed default lands **0.3 points under the 80.1% bracket mean against a 0.2-point spread between the two slots** — at the edge of the bracket's own noise rather than inside it, which is the honest reading of a null on this channel and matches its +0.53% of wall. `w15` is the only arm whose efficiency moves OUTSIDE that bracket in the losing direction; `ahead-w15` moves too, and by three points, which is exactly why it is 3.88% short rather than level.
- **The prediction was right in direction and wrong in size, on the half nobody had measured.** `w15` was predicted to lose 3-8% (E-17's 7.5%, which E-11 forbids quoting forward across E-24/E-25/E-27) and lost **14.09%** — oversubscription is worth roughly twice what the board thought. It is not ranked against `-fold ptr`'s 11.80% here and must not be: that number was measured as a win added to a 1.398 s baseline and this one as a loss removed from a 1.2245 s one, which is precisely the comparison E-11 forbids. `ahead-w15` was predicted within ~2% of default and landed at 3.88%, so "recovers most of it" holds and "nearly all of it" does not.
- **The `-phases` mechanism check says the same thing independently, and it is the reason this row is a verdict rather than two wall clocks.** Worker wall against the read+fold total is the discriminator, because `phaseRead` is accumulated wherever the `ReadAt` happens: **`off-w15` and `default-w20` both measure sum(worker wall)/(read+fold) = 1.0011**, i.e. every microsecond blocked in `pread` is on the worker's own critical path, while **`ahead-w15` measures 0.7368** — **6.768 s, 26.3% of the arm's read+fold, never appears in worker wall at all.** The unstaged identity is what licenses reading that as read time specifically: worker wall minus fold leaves **3.257 s** of read still visible out of the arm's 10.025 s, so **67.5% of the read is off the critical path** and a third of it is not. So the prefetch demonstrably does what it was built to do (worker wall sum 20.308 s → 18.946 s, −6.71%) and still loses 3.88% of wall clock, which is only possible if something else was already doing it.
- **Why a substitute and not a duplicate, DERIVED from the two configurations:** `-fill ahead -workers 15` is 15 workers **plus 15 fill goroutines** = 30 runnable goroutines on 15 cores, so it is oversubscription by another name. The count is exact for what was measured rather than approximate: under `-split static` (the default here) `work(w)` hands each worker exactly one range (`reader.go:162`), so `foldRangeFill` is called once per worker and launches one fill goroutine each — under `-split cursor` it would be one at a time in sequence instead, which is the same peak — the same slack, spent through a dedicated reader per worker instead of through a general extra worker. That predicts exactly the 72.5%-not-100% shape measured: a fill goroutine can only ever read for its own worker, where a 20th worker refills a core that stalls for any reason. It also predicts `ahead-w20`'s null (40 goroutines buys nothing over 20) and E-29's, without needing a new mechanism.
- **Per-worker read cost is not a constant and the board should stop treating it as one.** Blocked-read time per invocation is **5.097 s at 15 readers, 8.861 s at 20, and 10.025 s with 15 fill goroutines racing ahead** — the read gets slower per call as concurrency rises (25.1% of worker wall at `off-w15` against 36.2% at `default-w20`), so total blocked time growing while worker wall shrinks is the signature of overlap working, not of a regression. E-20's "29-30% of worker wall blocked in `pread`" was measured at 20 workers and 4 MiB; on the shipped 1 MiB default the same figure is **35.5-37.1%**.
- **P-05 is ANSWERED, and it stays subsumed on a measurement instead of a derivation.** Its revive test is worker wall spread above ~1.15, and its "depends on" clause predicted the imbalance would return at one worker per core. Measured at one worker per core: **skew 1.021-1.025 for `off-w15` and 1.022-1.027 for `ahead-w15`**, against 1.014-1.018 for the 20-worker default and E-20's 1.029-1.038 at 20 workers and 4 MiB. Dropping to 15 workers moves the spread by less than a point and leaves it **12.3 points below the trigger** (2.7% of imbalance against the 15% the trigger asks for), so the core-class-weighted split has at most **2.7%** to collect even in the regime that was supposed to bring it back. The clause was wrong: what oversubscription buys is not finish-time balance.
- **Consequence for the board: item 17 is closed and it hands `go-opt-round-3-gap` a sharper question, not a candidate.** The best arm here is the incumbent, so nothing in this row moves the study's standing best, which is **E-27's 1.233 s** — this invocation's two incumbent slots measured **1.220 s and 1.229 s**, consistent with E-29's 1.225/1.213 and E-30's 1.215/1.226, and none of those five slots was ever declared a new best because a bracket slot is a control, not a headline measurement. The wall above the compute floor is **19.79%/20.01%** (0.9785 s and 0.9831 s from 14.678 s and 14.747 s of user CPU over 15 cores — a sixth and seventh reproduction of E-27's 0.9812 s). What this row removes from the gap's suspect list is scheduling: two independent overlap mechanisms are now measured, they are substitutes, the better of them is already the default, and neither the read (26.3% hideable, already hidden) nor the skew (2.7% at worst) is where the 0.24 s lives. [**CORRECTED, C11: the last clause is false and the rest of the sentence stands.** E-32 decomposes the same gap into 8.0 points of system CPU — the kernel copying the file into the buffers, which IS the read and is not stall at all — and 11.5 points of idle cores that trace to 12.85 workers-with-work against 15 cores, which is the read blocking and NOT hidden. What E-31 established is that no third *overlap* mechanism can hide more of it; "already hidden" was true of `ahead-w15`'s 26.3% and never of the shipped `-fill off` default, whose own `sum(worker wall)/(read+fold)` of 1.0011 this row prints two bullets earlier.**]

### E-32 — where the wall above the compute floor actually goes (`go-opt-round-3-gap`, CLOSES it)

- **Idea:** the round's remaining question has had no mechanism attached to it since E-29, and every reading the board rests on predates two defaults — E-20 profiled a 1.50 s binary at 4 MiB, E-26 re-profiled before `-fold ptr` landed. So instrument the binary that ships today and let the measurement either name a mechanism or record that it names none. **No arm is built here**, which is the whole point: `spec.md:61` makes an unexplained gap recorded as unexplained a result, and a mechanism invented to fill it not one.
- **Measured (three unprofiled `-phases` rounds of the shipped default `-workers 20 -buf 1024 -parse word -fold ptr` on the 1b file, AC power, load 1.61 at the start, preflight green — orphan check clear, measurement lock held, `require_quiet` satisfied; round 0 discarded as the first-touch outlier every `-phases` set in this study has shown; raw output and every derivation in [`07-experiment-ledger-data.txt`](07-experiment-ledger-data.txt) §T):** wall **1.20 / 1.22 / 1.22 s**, user CPU **14.69 / 14.64 / 14.64 s**, system CPU **1.44 / 1.43 / 1.49 s**, blocked read 8.473 / 8.742 / 8.805 s, fold 15.430 / 15.394 / 15.422 s, worker-wall skew 1.008-1.013, merge 2.28-3.16 ms. Means: wall **1.2133 s**, user **14.6567 s**, sys **1.4533 s**, compute floor **0.9771 s**, wall above it **19.47%**. The three rounds' floors are 0.9793 / 0.9760 / 0.9760 s, so they are the **eighth, ninth and tenth** reproductions of E-27's 0.9812 s on E-31's counting (its two bracket slots were the sixth and seventh), against the figure the board carries as 19.85% / 20.42%.
- **The gap decomposes EXACTLY, because it is an identity rather than a model.** `wall × 15 cores` is **18.1995 core-seconds** of capacity and every one of them is user CPU, system CPU or idle: **user 80.53% + system 7.99% + idle 11.48%**, and `(sys + idle)/capacity` reproduces the 19.47% to the digit in every round (18.39% / 20.00% / 20.00%). So what this study has been calling "the wall above the compute floor" is **two** things, and only one of them is the machine standing still: **8.0 points are system CPU that the floor's own definition excludes**, and **11.5 points are idle cores** — 1.72 of 15 on average. Per round the system term is tight (7.81-8.14) and the idle term is 10.39-12.19; that spread is `/usr/bin/time`'s 10 ms wall resolution, which idle absorbs entirely. Nothing measurable happens outside the parallel section: `wall − max(worker wall)` is **−0.6 ms / +6.0 ms / −0.8 ms**, i.e. inside the same quantization, and the merge is instrumented directly at 2.28-3.16 ms.
- **The system half has no live mechanism, and that is the most useful thing this row says.** System time is the kernel copying 13,795,610,267 B out of the `F_NOCACHE` `pread` into the worker buffers; 1.4533 s implies **9.49 GB/s** [DERIVED — a plausible memcpy rate on this machine, not independently measured]. It scales with BYTES and not with call count (13,160 calls), so no buffer size touches it, and the only mechanism that removes a copy is not copying: `-io mmap` is KILLED-on-numbers at 5.6x end-to-end (H7, 16 KiB pages giving 842k faults, and `MADV_WILLNEED` making it worse). **8.0 of the 19.47 points are therefore closed rather than open** — recovering them means reversing a kill that has a number on it.
- **The idle half is a supply problem, and the instrument says so directly.** The mean number of workers with work to do is `20 − read/wall` = **12.85 of 20**, against 15 cores: on average the binary does not have enough runnable goroutines to fill the machine. It coexists with descheduling in the same runs — `fold/wall` = **12.71** workers inside a fold against `user/wall` = **12.08** cores executing user code, so **0.63** workers' worth is runnable-but-waiting (**4.92%** of fold wall) — and idle cores together with waiting goroutines in one run is what a runnable count that **fluctuates around** its mean looks like rather than one that sits at it. That is the quantity a worker count acts on, and it is the only quantity this measurement leaves open.
- **The ceiling on the idle half does NOT reach the target, so it is a candidate for the gap and not for the goal.** Perfect packing is `(user + sys)/15` = **1.0740 s**: **−11.48%** of today's wall and still **+7.40% over the 1.000 s target**. No scheduling fix of any kind gets there. To land at 1.000 s with today's overhead structure intact, user CPU has to fall to **12.080 s, −17.58%**.
- **The profile, refreshed on the shipped default at last** (three profiles, shares of NON-syscall samples per E-26, applied to the measured 14.6567 s of user CPU): `indexDelimAt` **41.57-43.64% (6.09-6.40 s)**, `parseTempWordFrom` 17.84-19.73% (2.61-2.89 s), `(*table).update` 12.52-14.05% (1.84-2.06 s), `runtime.memequal` 8.81-9.87% (1.29-1.45 s), `foldRowsPtr`'s own flat time 7.39-9.96% (1.08-1.46 s), per-row `merge` 1.92-2.59%. The syscall bucket reproduces E-26's shape a third time and is excluded on the same evidence: **55.03-56.50% of samples against 1.45-1.52 s of measured system time**, so at most a fifth of it can be time in the kernel.
- **`-fold ptr` removed 13.5% of user CPU and left the RANK ORDER of all six functions identical**, which is the result about the ordering key. The shares themselves moved and the row says by how much rather than claiming they did not: against E-26's `indexDelim` 40.9-44.3%, `parseTempWord` 17.7-21.7%, `update` 12.4%, `memequal` 7.2-8.7%, `foldRows`'s own 6.6-8.8%, `merge` 2.0%, **two of the six are inside E-26's bracket and four sit above its top by 0.59-1.65 points** (`update` +1.65, `memequal` +1.17, `foldRows` +1.16, `merge` +0.59) — every one of them in the same direction, which is what removing 13.5% from a smaller non-syscall denominator does to the small shares while the two big ones absorb it. **The board is ordered by rank, and the rank is unchanged**, so it survived the arm that invalidated everything else derived from it; a claim that the shares themselves were unmoved would not have survived the check, and the first draft of this row made it.
- **Only one function is large enough to be worth 17.58% by itself, and the ceiling is stated with its bad assumption named.** Removing `indexDelimAt` outright, holding system CPU and idle FIXED, lands at **0.787-0.807 s**; `parseTempWordFrom` lands at 1.021-1.039 s and nothing below it reaches the target at all. The held-fixed assumption is known to be wrong in the unhelpful direction — E-25 and E-27 both measured parallel efficiency FALLING as they removed CPU (82.1% → 80.9% → 79.6%), because the unchanged read hides under less compute — so these are ceilings, not forecasts, and unpriced ceiling arithmetic on a sound floor is exactly what `CORRECTIONS.md` C10 retired for H-14. **The board carries no un-attempted mechanism for that pot**: item 1's fuse is CLOSED not-adopted at +0.81% (E-27) and item 2's batch tokenizer is KILLED at +9.8%/+10.4% (E-10). This row does not invent a third.
- **The reader is not device-limited, which is why the worker count is the lever and the buffer is not.** 13.795 GB in 1.2133 s is **11.37 GB/s aggregate**, **62.2%** of `02-baseline.md`'s measured 18.29 GB/s (754.4 ms, 15 uncached `pread`s), with **7.15 of 20** workers blocked in `pread` at any instant (35.74% of worker wall, reproducing E-31's 35.5-37.1% at the same 1 MiB buffer).
- **The one mechanism the measurement admits is boarded as item 18, with a number.** Blocked-read time is a non-constant in the reader count (E-31: 5.097 s at 15 readers; 8.673 s at 20 here), so workers-with-work went **11.35 → 12.85** across those two points, **0.300 per added worker**, and a linear extrapolation puts 15 runnable at **27.2 workers** [DERIVED from two points, one of them another invocation's arm]. E-12 measured 20 against 30 and could not separate them, but that invocation carries the slot bias E-16/E-18 priced and its binary is three arms old, so it does not rule the item out.
- **A methodological by-product worth the four extra runs: `-cpuprofile` costs +13.74% of wall and 0% of the work.** The four profiled rounds run at 1.42 / 1.42 / 1.41 / 1.31 s against the `-phases` rounds' 1.2133 s mean while user CPU is flat at +0.32% and system at +1.83%; the whole difference is idle, **21.82% against 11.48%**. `/usr/bin/time -l`'s instruction counter settles it without any derivation: the profiled rounds retire **279,161,109,302** instructions against the unprofiled rounds' **279,120,176,115**, **+0.015%**, with all eight runs of the pass inside a **0.153%** spread. A profiled run's wall clock is therefore not the binary's, its parallel efficiency is not the binary's, and only its shares transfer — which is E-21's "shares rank, seconds do not" arriving from a second direction. The same counter gives the study a figure it did not have: **279.1 instructions per row**, measured, against the 15.0 ns per-core budget `05-go-techniques.md` derived.
- **The first-touch round corroborates the verdict from a direction nothing else covers, and it was found by mutating the exclusion rather than by reasoning about it.** Every `-phases` set in this study discards round 0; putting it back moves the headline gap **19.47% → 24.40%** while the compute floor moves **0.9771 → 0.9772 s** (user CPU 14.6567 → 14.6575 s) — all 4.93 points of damage land in idle, 11.48% → 16.79%. The outlier costs **0.31 s of wall and 0.001 s of work**, so it is I/O and not warm-up of anything the CPU does, which is the same conclusion the decomposition reaches by a different route.
- **Verdict: the gap is NAMED, and two-thirds of it is closed rather than open.** 8.0 of the 19.47 points are the kernel's copy, whose only mechanism is already killed; 11.5 points are idle cores whose ceiling is 1.0740 s, still 7.40% over target; under 0.5% of wall is outside the parallel section. The study's standing best is untouched at **E-27's 1.233 s** — these are single unbracketed instrument runs, and E-31's rule that a control is not a headline applies with more force to a run that was never bracketed at all. **The round now has exactly one runnable candidate (item 18) and it cannot reach the target**, so the honest statement of the remaining gap is: reaching 1.000 s needs −17.58% of user CPU, the only pot that size is the separator scan at 41.6-43.6% of compute, and the board has already spent both of its mechanisms on it.

### E-33 — Go's runtime map in the 10,000-station regime (queue item 9, CLOSES it)

- **Idea:** fold into `map[string]*entry` instead of the open-addressed array, on the file that finally makes the question askable. `05-go-techniques.md:39` measured Go's map **3.7% faster** than the best open-addressing variant on 10,000 names and **15.8% slower** on the official 413, single-threaded and never end-to-end; item 9 has sat on the board since, blocked on a file.
- **The file is the decision this row is really about, and it holds BYTES constant rather than rows.** A 1b-row 10k file is 57.09 GB, 2.22x this machine's RAM, with a 3.12 s read floor — three times over target before any compute. `measurements-10k-stations-242m.txt` is 241,600,000 rows at 13,798,576,606 B, **+0.0215% of `measurements-1b.txt`'s size and the same 53.5% of RAM**, so it holds the memory regime the item is about and changes only the key set (`02-baseline-data.txt`, third pass).
- **Prediction, pre-registered:** −1% to −4% of wall, from the 3.7% single-threaded delta damped by the table's share of the whole binary; ceiling near 11% of compute if the map halved the 21-24% that `(*table).update` and `memequal` take of non-syscall CPU (E-26, E-32). "A map that is merely equal end-to-end kills the item."
- **Measured (242m 10k-station file, 5 runs, 20 s cooldown, AC, load 3.72, preflight green, bracket **0.078%**, [`bench/2026-09-02T010530Z-…`](bench/2026-09-02T010530Z-queue-item-9-Go-runtime-map-at-10-000-stations.txt)):**

  | arm | wall | range | user CPU | sys CPU | vs bracket mean (wall) | vs bracket mean (user CPU) | disjoint from both? |
  |:---|---:|:---|---:|---:|---:|---:|:---|
  | `default.a` | 1.276 s | 1.256-1.313 | 13.860 s | 1.516 s | — | — | — |
  | `map` | 1.113 s | 1.097-1.125 | 11.850 s | 1.432 s | **−12.81%** | **−14.51%** | **yes** |
  | `default.b` | 1.277 s | 1.262-1.309 | 13.863 s | 1.514 s | — | — | — |

- **NOT A VERDICT, and the stamp is load-bearing.** E-09 forbids ranking arms on anything but the 1b file, so this row ranks a MECHANISM in the 10k regime and re-ranks nothing about the 413-station defaults. What it settles is item 9's own question — "does Go's map win at 10,000 stations end-to-end" — and the answer is **yes, by 12.81% of wall, disjoint from both bracket slots** (the map's slowest run, 1.125 s, beats the incumbent's fastest, 1.256 s). **E-34 is the 1b half**, and it is what decides whether the default moves.
- **The prediction was right in sign and wrong by 3x in magnitude, and the reason is that the arm removes more than the probe.** Predicted −1% to −4%, measured −12.81%. The single-threaded 3.7% was the probe alone; end-to-end the array arm also pays to CREATE 20 tables of 131,072 buckets × 48 B = **120.0 MiB** that hold 10,000 live entries each, and to fault that memory in. System CPU falls **5.48%** with an identical I/O path, which is the signature of that allocation rather than of the probe, and the probe cannot explain it at all.
- **The measured channel that carries the mechanism is user CPU, and it moves further than wall.** −14.51% of user CPU against −12.81% of wall: parallel efficiency falls 72.4% → 71.0%, so the arm removes CPU faster than the machine can convert it into wall clock, which is what an arm that is compute-bound-but-not-perfectly-parallel looks like. Per row the fold goes 5.284 ns → 4.607 ns.
- **The arm is honest against itself in the one direction that matters.** It still pays `maskWord` and `mixWord` on every row and then throws the hash away — removing that would need a per-row branch in the fold loop, which would charge every other layout for this arm's existence — so the 12.81% is measured on an arm carrying dead work, and the map's own cost is at most this. `-bits` is inert for it, which `TestMapLayoutGrowsPastTheBucketCount` pins.
- **What this does NOT say.** It does not re-open bucket layout at 10k: no `quot` arm ran on this file, and E-28's own escape clause ("only a measured 10k-station regime can re-open it") is now runnable and still untried — see the board. It does not measure item 10's merge at 10k or item 12's vectorized probe; both keep their state, now with a file to run on.

### E-34 — the same map arm on the official 413-station key set (queue item 9's other half)

- **Idea:** E-33 measured `-table map` 12.81% FASTER at 10,000 stations. The study's target is defined on the 413-station 1b file, so before anyone reads that as a default change, the same arm is priced where the default lives. This is a 1b run, so unlike E-33 it IS a verdict.
- **Prediction, pre-registered:** the map LOSES **5-16%**. The 10k win is a working-set effect; at 413 stations only 413 of 131,072 buckets are live, ~20 KB of hot entries against a 6 MiB array, so the mechanism is absent and `05-go-techniques.md:37` measured the array 15.8% faster single-threaded. "A win instead would mean the default is wrong."
- **Measured (1b, 5 runs, 20 s cooldown, AC, preflight green, bracket **0.33%**, [`bench/2026-09-02T011232Z-…`](bench/2026-09-02T011232Z-queue-item-9-at-413-stations-does-the-map-win-survive-the-official-key-set.txt)):**

  | arm | wall | range | user CPU | sys CPU | vs bracket mean (wall) | vs bracket mean (user CPU) | disjoint from both? |
  |:---|---:|:---|---:|---:|---:|---:|:---|
  | `default.a` | 1.226 s | 1.219-1.237 | 14.648 s | 1.533 s | — | — | — |
  | `map` | 1.863 s | 1.837-1.913 | 23.265 s | 1.417 s | **+52.20%** | **+58.77%** | **yes, and in the wrong direction** |
  | `default.b` | 1.222 s | 1.213-1.232 | 14.659 s | 1.520 s | — | — | — |

- **Verdict: KILLED-on-numbers at 413 stations, and the default does not move.** `-table combined` stays. The arm is disjoint from both bracket slots by a margin no slot bias can produce (E-16's worst measured drift is 21% over eight slots; this is 52% over three), and both channels agree in sign. Its compute floor is **23.265 s over 15 cores = 1.551 s**, so it puts the floor 55% above the 1.000 s target — E-27 had taken it 18.8 ms under.
- **The pre-registered magnitude was wrong by more than 3x in the same direction the 10k one was, and the pair is the finding.** Predicted −1% to −4% at 10k, measured −12.81%; predicted +5% to +16% at 413, measured +52.20%. Both errors come from treating the single-threaded probe delta as the whole arm. What the two rows together measure is that **the key set inverts this choice with a 65-point swing** (−12.81% to +52.20%), which is far larger than the 19.5-point swing 05-go-techniques.md's single-threaded probes predicted (−3.7% to +15.8%): running 20 shards multiplies whatever the table costs, in both directions.
- **The control is the strongest in the study so far.** Two incumbent slots at 1.226 s and 1.222 s, 0.33% apart, reproducing E-27's 1.233 s headline and E-29's 14.664 s of user CPU (14.648 s and 14.659 s, within 0.10% of each other) across a third independent invocation. Whatever else this row says, the baseline it says it against is stable.
- **`-table map` stays in the binary** as the arm these two rows were measured on, the way `-table split`, `-table quot` and `-kernel batch-*` did, and because it is the incumbent for any future 10k-regime work.

### E-35 — the `final-dod` headline re-measure, which did not reproduce

- **Idea:** not an optimization. `spec.md:66` requires the headline re-measured in a quiet window on AC before publishing. Three invocations were taken, and the first two disagreed with the published figure, so the row records all three and E-36 is the control that separates one cause from the rest.
- **The binary is byte-identical to the one E-34 timed.** `git log <E-34 commit>..HEAD -- 1brc/code/go` returns **zero commits**, so nothing below is a code change; both readings are of the same program on the same machine, 47 minutes apart.
- **Prediction, pre-registered:** the shipped default reproduces 1.233 s ± 0.010 s inside the nine readings' 2.31% spread, and `bench.sh`'s 1.345 s is the missing cooldown.
- **Measured, invocation R1 (`bench.sh`, unbracketed, one configuration, NO cooldown, 5 runs + warmup, AC, preflight green, [`bench/2026-09-02T015752Z-final-dod-headline.txt`](bench/2026-09-02T015752Z-final-dod-headline.txt)):** **1.345 s ± 0.011 s**, range 1.331-1.357, user 14.450 s, sys 1.243 s.
- **Measured, invocation R2 (`experiment.sh`, two identical no-flag arms, 20 s cooldown, AC, preflight green, [`bench/2026-09-02T015858Z-final-dod-headline-re-measure.txt`](bench/2026-09-02T015858Z-final-dod-headline-re-measure.txt)):** `default.a` **1.292 s** [1.284-1.300], user 14.311 s, sys 1.267 s; `default.b` **1.281 s** [1.271-1.286], user 14.310 s, sys 1.263 s. **Bracket spread 0.859%**, so the invocation is valid by the 3% bar and both slots are quotable.
- **The prediction is falsified in its second half and cannot be settled by its first.** R2 has the cooldown and still lands at a 1.2865 s bracket mean, disjoint from the nine published readings (R2's fastest run, 1.271 s, is slower than the published band's slowest, 1.213 s, and than E-34's slowest, 1.213 s). R1 against R2 and R2 against E-34 are both **cross-invocation**, which this ledger records and never subtracts, so no percentage here is a delta.
- **The channel that rules out a regression: CPU went DOWN while wall went UP.** Today's four bracketed arms spend **14.311 / 14.310 / 14.327 / 14.340 s** of user CPU against E-34's **14.648 / 14.659 s**, a **2.26%** drop, and system CPU **1.267 / 1.263 / 1.299 / 1.287 s** against **1.533 / 1.520 s**, a **16.21%** drop. A slower program spends more CPU, not less. The binary is doing the same work and waiting longer to be allowed to do it.
- **Verdict: the published headline does NOT reproduce a day later, and the study says so rather than re-labelling one of the readings.** `spec.md:36` makes the headline the best measured wall clock under a recorded storage state; **1.233 s (E-27) remains that number** and it keeps its nine-reading, five-invocation support. What E-35 removes is the word "no drift": that spread was measured inside ONE session, and `CORRECTIONS.md` C13 corrects the claim at every site. **The study's conclusion does not turn on which figure is taken** — 1.233 s misses the target by 23.3% and 1.257 s (E-36) by 25.7%.

### E-36 — the control: the same bracket without the 1b correctness pass

- **Idea:** `final-dod` committed `testdata/expected-1b.out` so the DoD's 1b byte-compare could run. `experiment.sh:101-102` then fills `CASES_EXTRA` from `-f` whenever a reference output for the timed file exists, so from that commit onward **every 1b arm is preceded by a 13.8 GB `F_NOCACHE` correctness read that no earlier 1b invocation paid**. That is a change to the measurement conditions introduced by a deliverable, and it is testable by removing the file.
- **Prediction, pre-registered:** with `expected-1b.out` absent the bracket reproduces E-34 inside its 1.213-1.237 s band rather than repeating 1.281-1.292 s.
- **Measured, invocation R3 (`experiment.sh`, two identical no-flag arms, 20 s cooldown, `expected-1b.out` moved aside, AC, preflight green, [`bench/2026-09-02T020455Z-…`](bench/2026-09-02T020455Z-final-dod-headline-re-measure-control-no-1b-correctness-pass-before-the-timing.txt)):** `default.a` **1.257 s** [1.247-1.266], user 14.327 s, sys 1.299 s; `default.b` **1.257 s** [1.244-1.270], user 14.340 s, sys 1.287 s. **Bracket spread 0.000%**, the tightest control in the study.
- **Verdict: the extra correctness read is real and it is not the whole gap.** The prediction is **half right**: R3 is 2.35% below R2's bracket mean, so the inserted read costs something, and R3 is still **+2.70% over E-34** with its fastest run (1.244 s) outside E-34's slowest (1.213 s), so it does not land in the predicted band. Both comparisons are cross-invocation and neither is quoted as a delta. **No mechanism is named for the residual**, in the same refusal `go-opt-round-3-gap` and the round-3 verdict made: the two channels that would identify one (user CPU 2.26% lower, system CPU 16.21% lower than E-34 on identical code) say the binary waits more and computes less, and nothing measured here says why.
- **The compute floor is unaffected and still below target.** R3's user CPU over 15 cores is **14.3335/15 = 0.9556 s**, below E-27's 0.9812 s and below the 1.000 s target. The decomposition of R3's wall is user **76.02%**, system **6.86%**, idle **17.12%**, against E-32's 80.53 / 7.99 / 11.48: the whole of the difference is idle cores, which is the same lever P-06 already holds and the same one whose measured ceiling (1.0740 s) misses the target.
- **Consequence for the harness, not for the default:** a committed reference output for a timed file silently re-prices every later experiment on that file. `expected-1b.out` is kept, because the DoD requires the 1b byte-compare and a correctness gate that reads the file being ranked is what `experiment.sh`'s own comment asks for; what changes is that **no 1b number taken after this commit is comparable to one taken before it** without the control this row provides.

### E-37 — two row cursors per worker (`-fold lanes`), a mechanism the board never carried

- **Idea:** a row's start is `pos + sep + 1 + next`, so it is not known until that row's own parse retires. One cursor therefore hands the core a single serial chain per row with no independent work to overlap the stalls, which is what leaves measured IPC near **4.5** (279.1 instructions/row over 14.66 s of user CPU at ~4.2 GHz) on a roughly 8-wide core. `foldRowsLanes` splits the buffer at a **row boundary** and advances two cursors in lockstep, both scans issuing before either result is consumed, so two chains sit in the out-of-order window at once. The aggregate is unchanged because each lane owns whole rows and min/max/sum/count commute. Not queue items 1, 2, 5 or 17: those change the work per row, this changes how much of it can be in flight.
- **Prediction, pre-registered:** the row loop is latency-bound on a serial per-row chain, so two interleaved cursors take **5-15%**.
- **Measured (`experiment.sh`, 3 arms with the incumbent first AND last, 20 s cooldown, 5 runs, AC, preflight waited out two load excursions, [`bench/2026-09-04T062826Z-…`](bench/2026-09-04T062826Z-lanes-two-row-cursors-per-worker.txt)):** `ptr.a` **1.312 s ± 0.052** [1.266-1.399], user 14.753 s; `lanes` **1.285 s ± 0.034** [1.261-1.342], user 13.977 s; `ptr.b` **1.318 s ± 0.030** [1.286-1.354], user 14.758 s.
- **The two channels disagree, and the quiet one is the trustworthy one here.** Wall bracket spread **0.457%**, so arms may be quoted; lanes is **−2.28%** against the bracket mean, but its range overlaps both incumbent slots, so the wall win is **not disjoint and not a verdict**. User CPU bracket spread is **0.034%** — the tightest control in the study — and lanes is **−5.28%** against it, roughly 155× that control's noise. `User` is this process's own CPU and does not absorb another process's cores, which is why it stayed clean while wall σ did not: the preflight had to wait out WebKit at 71-97% and the machine did not go quiet, it only dropped under the ceiling.
- **Verdict: the mechanism is CONFIRMED on the CPU channel and the wall clock does not follow it.** Prediction held in direction and at the bottom of its range on CPU, and failed on wall. The compute floor drops **0.9837 s → 0.9318 s**, further below the 1.000 s target, while wall-above-floor *rises* **33.7% → 37.9%** — removing compute makes the binary MORE gated by the thing E-32 already named, the read and the idle cores. This is the first row to cut user CPU since E-27 and the first to demonstrate that cutting it no longer buys proportional wall clock.
- **Kept as a flag, NOT promoted to default.** A default change needs a disjoint wall-clock win, and this is not one. What it needs is a re-measure on a genuinely quiet machine at higher `-r`, and it should be stacked with P-06 (workers above 20) rather than measured alone, because it enlarges exactly the idle share P-06 attacks.

### E-38 — `-fold lanes` re-measured at `-r 10`, and the wall clock follows after all

- **Idea:** E-37 left one question open. Two row cursors cut user CPU by 5.28% against a 0.034% control bracket, but its wall clock (−2.28%) had ranges overlapping both incumbent slots, on an evening when a browser held 71-97% of a core. Either the wall win was real and buried in noise, or there was no wall win. `-r 10` doubles the runs against the same bracket.
- **Prediction, pre-registered:** a real wall win lands **2-5% and disjoint** and promotes `lanes` to the `-fold` default; otherwise the arms overlap again and it stays a flag. The CPU channel is predicted to reproduce **−5.28%** either way, because core-stealing cannot touch a process's own user CPU.
- **Measured (`measure-when-idle.sh`, 3 arms with the incumbent first AND last, 10 runs, 20 s cooldown, AC, commit `856215f`, operator ACTIVE on the machine throughout, [`bench/2026-09-04T211808Z-…`](bench/2026-09-04T211808Z-lanes-requiet-settle-E-37-wall-clock-at-r-10.txt)):** `ptr.a` **1.287 s ± 0.047** [1.227-1.401], user 14.862 s; `lanes` **1.241 s ± 0.034** [1.199-1.309], user 14.108 s; `ptr.b` **1.287 s ± 0.051** [1.220-1.368], user 14.890 s.
- **The bracket is 0.000% on wall and 0.188% on user CPU** — the two incumbent slots landed on the same mean to the millisecond, which is the tightest wall-clock control this study has recorded, and it was taken on a *busy* machine. Wide per-arm σ and a tight bracket are not in tension: the cooldown spreads each arm's runs across the same noise, so the slots average to the same place while individual runs scatter.
- **Verdict: the wall win is REAL and the prediction is met — −3.57%, inside the 2-5% band — and `lanes` still does not become the default, because the ranges are NOT disjoint.** `lanes` tops out at 1.309 s against incumbent minima of 1.227 s and 1.220 s. The keep rule wants separation, not just a delta whose bracket is clean, and the operator's own activity put ~14% spread on the incumbent ranges. Promotion waits for a quiet-machine run; the mechanism does not.
- **The mechanism is settled across three invocations and two channels.** User CPU: −5.28% (E-37), −5.16% here, and 13.979 s in the invocation killed mid-run, all against ~14.8 s incumbents. The compute floor moves **0.9917 s → 0.9405 s**.
- **And it re-states the shape of the remaining gap rather than closing it.** Wall sits **32.0%** above the lanes floor against 29.8% above the incumbent's, so removing compute again made the non-compute share bind harder — the third row in a row to do so. What is left is the kernel's byte copy (`-io mmap` killed at 5.6x) and idle cores waiting on reads, whose perfect-packing ceiling is 1.0740 s. Cutting CPU cannot reach 1.000 s from here, and this row is evidence for that rather than against it.

## Hypothesis queue for `go-autoresearch-harness` and the optimization rounds

Ordered by the size of the gap each could close. The gap to shut is **0.742 s**, of which at most **0.988 s** is compute (1.742 measured minus the 0.754 s read floor). **E-20 supersedes that arithmetic:** measured against the current best, the gap is **0.424 s** (E-25's 1.424 s minus the 1.000 s target) and **all of it has to come out of compute**, because the compute floor at perfect parallelism is 1.152 s and the read floor is 0.754 s below it. E-24 moved the wall clock 6.4% without moving user CPU at all, so it closed none of that gap: it bought parallel efficiency (77% → 82.1%). **E-25 is the first row that did close some of it** — 6.33% of user CPU, taking the floor 1.235 s → 1.152 s — and 152 ms of per-core compute still has to go before 1.000 s is even reachable. **E-27 closes the rest of it: 13.49% of user CPU, floor 1.152 s → 0.9812 s, which is 18.8 ms UNDER the target.** The gap is now 0.233 s (1.233 s minus 1.000 s) and **none of it has to come out of compute** — all of it is the 20.4% of scheduling between the wall and the floor, which inverts this paragraph's ordering rule.

1. **Fuse the name hash into the separator scan** (benhoyt r10's shape, `05-go-techniques.md`): the SWAR scan currently skips 8 bytes at a time and the hash then re-loads the first word. Unmeasured. Prediction: small, because the hash already reads only one word.
2. **The batch tokenizer** (`04-asm-kernels.md`'s biggest win, −40.4%/−40.7% on the official key set against staged SWAR): a whole-buffer dual-needle kernel emitting a token stream, drained into the table. This is the single most load-bearing untested assumption in the study, and `go-v2-kernels` owns it. The measured 1.93 ns Plan 9 call cost (`05-go-techniques.md`) says it must be called once per buffer, never per row.
3. **The 25% of the cores that are idle.** 11.3 of 15 busy. Nothing here measures whether that is I/O wait, the merge, or shard skew. A profile (`sample`, or `xctrace --template 'CPU Counters'`) refills this queue better than a guess; `go-opt-round-2` is defined as profile-driven for this reason.
4. **The correctness tax, measured rather than assumed.** The dual-needle scan (four more integer ops per word, counted from the expression, not measured) and `validTemp` (four to six byte compares per row) exist so this binary rejects exactly what the reference rejects. Neither has been priced against a trusting variant. Prediction: 3-8% of compute; if it is more, the shape of the check should change, not the contract. [**CORRECTED, C7: `validTemp` alone measures 18.2% of CPU (E-21), two to six times this range. The condition in the last clause has fired, and the answer is item 16.**]
5. **`unsafe.Add` pointer walks** in the fold loop: measured ceiling 4-16% on the scan in isolation (`05-go-techniques.md`), with the resliced-versus-unsafe gap there still unexplained.
6. **More workers than cores** (`-workers 20`, `-workers 30`): if part of the idle 25% is I/O wait, oversubscription hides it. Untried, and the cheapest experiment left.
7. **Buffer sizes above 4 MiB** at 1b: 8 and 16 MiB untried at the scale where 4 beat 1.
8. **A 1b 10,000-station file** to settle E-03's parked half and `05-go-techniques.md`'s working-set hypothesis. ~20 s of generation. [CORRECTED, C5: 57.09 GB and ~79 s; see the queue board.]
9. **Go's runtime map for the 10k case**: measured 3.7% FASTER than the best open-addressing arm on 10k names single-threaded (`05-go-techniques.md`). Never tested end-to-end, and would need item 8 first.
10. **The merge and the output.** 10,000 stations means 10,000 `map` inserts per shard at drain time and a sort; at 413 stations it is invisible, at 10k it may not be. Unmeasured.

### Added by `06-cross-disciplinary-transfer.md`

Four hypotheses, each borrowed from a field that solves the same shape of problem. They are numbered H-11 to H-14 so that the hypothesis id and the queue position are the same number; H1-H7 belong to `03-technique-recon.md` and H8-H10 are deliberately never issued, so a reader who meets an H-11 does not go looking for an H8 that was never written. The report also killed three candidates and parked two; those are not queue items and live in `06` and `PARKED.md`.

11. **H-11 — a static split weighted by core class.** It tests `02-baseline.md:15`'s standing, never-measured hypothesis that "a uniform 15-way split is close to optimal on this chip". The classes are 5 "Super" with 16 MiB L2 and 10 "Performance" with 8 MiB (`06-cross-disciplinary-transfer-data.txt` §1), and `reader.go` hands all 15 an equal share. Borrowed from morsel-driven parallelism, minus the dynamic dispatch that E-02 already killed here. **Prediction, derived:** if the fast class runs at `r` times the slow one, weighting is worth `(r/3 + 2/3)x` the equal-split wall clock — 3.3% at `r = 1.1`, 10.0% at `r = 1.3`, 16.7% at `r = 1.5`. `r` is unmeasured; measure it on a resident buffer first, then run the weighted split at 1b. [**SUBSUMED, E-20: worker wall spread is 1.029-1.038, so there are at most 3.8% of imbalance to take and E-17's 20 workers already take it. `PARKED.md` P-05, with the trigger that revives it.**]
12. **H-12 — vectorize the table probe, not only the scan.** Compute a batch of hashes, issue all their bucket loads, then resolve, turning one dependent-load chain into N overlappable misses. Borrowed from vectorized query execution. **Prediction:** under 3% at 413 stations (0.3% load factor, the hot set stays in L2), over 10% at 10,000. The interesting half needs item 8.
13. **[CLOSED — E-28, KILLED-on-numbers: +5.34% of wall against the bracket mean, predicted 0-4% faster.] H-13 — a quotiented 32-byte entry.** Replace the 24-byte `[]byte` key header with an inline 8-byte prefix and a side array for the full name: 48 bytes becomes 32, the probed array shrinks 1.5x, and the hot compare is one word instead of a pointer chase. Borrowed from k-mer count tables. **Prediction:** 0 to 4% at 413. Deliberately close to E-03, which lost 4.6% doing something adjacent; the difference is that this adds no second array in the HIT path, and if it loses too then the neighbourhood is exhausted rather than untried.
14. **H-14 — double-buffer each worker.** `foldRange` calls `ReadAt`, waits, then folds, in one goroutine, alternating; a reader goroutine filling buffer B while the fold runs on buffer A makes the overlap explicit instead of leaving the OS to cover one worker's read with another's compute. Borrowed from DPDK's fill-while-you-process. **Prediction:** 5% up to a ceiling of 25% — 19.657 s of CPU over 15 busy cores is 1.310 s against the measured 1.742 s, which assumes every idle core is idle because of a read stall and is therefore a ceiling, not a forecast. Largest single-mechanism candidate found by the transfer pass. [**E-20 measures both halves: the assumption HOLDS — the idle gap is the read — and the ceiling on the current baseline is 17.6%, 18.53 s of CPU over 15 cores giving 1.235 s against a 1.50 s wall. Still the largest single mechanism, and no longer enough on its own: at its ceiling it lands 235 ms over target.**] [**E-25 supersedes the arithmetic, not the reading: 17.279 s of CPU over 15 cores is 1.152 s against a 1.424 s wall, so the ceiling is 19.1% and it lands 152 ms over target. The ceiling rose as a percentage and improved as a destination only because another arm removed CPU — double-buffering itself can still never go below the compute floor.**] [**E-27 supersedes it again and changes the reading this time: 14.718 s over 15 cores is 0.9812 s against a 1.233 s wall, so the ceiling is 20.4% and it lands 18.8 ms UNDER target. This item is no longer "the largest mechanism that cannot get there" — it is the only one on the board that now can, and it is the top of round 3 for that reason. The clause "can never go below the compute floor" still holds and is now a promise rather than a limit.**] [**E-29 BUILDS IT AND FALSIFIES THE WHOLE CHAIN. Measured: +0.57%/+1.57% of wall, overlapping both bracket slots, and 0.24% from the `sync` arm it differs from by the fill goroutine alone. The ceiling arithmetic was never wrong; the ASSUMPTION under every version of it was — "the OS covering one worker's read with another's compute" is not a fallback this item replaces, it is E-17's oversubscription default already doing the overlap, and stacking a second one on it recovers nothing. Parallel efficiency moved 79.8% → 79.3%. CLOSED, killed-on-numbers; see `CORRECTIONS.md` C10.**]

### Added by the round-2 profile (E-20, E-21)

16. **Validate the temperature's FORMAT from the word the parse already loaded, instead of from four to six dependent byte compares.** `validTemp` is **18.2% of measured CPU** (E-21), the second-largest consumer in the binary. What it checks is the shape `-?\d{1,2}\.\d\n` that `parseTempBranchless` "assumes and cannot itself check" (`kernel.go:121`), by indexing `b[0]`..`b[5]` and testing digit-ness and literal `.` and `\n` positions, one `switch` arm per field length.

    **The obvious version of this idea is wrong, and it is written down here so it is not tried twice.** "Replace the byte checks with an integer range test on the tenths the parse produced" does not work, because that test already exists and already runs on every row: `table.go:134` is `if next == 0 || !validTemp(field, next) || !inRange(v)`, and `inRange` is the range test. The two are not redundant — `inRange` bounds the VALUE, `validTemp` establishes that the bytes were a temperature at all, and the branchless parse will happily return an in-range integer from malformed input like `1x.5` or `--1.5`. Dropping `validTemp` for `inRange` would accept rows the reference rejects, and **no generated corpus can catch it**, because every generated row is well-formed: this is the third appearance of the trap `lessons.md` records under "no corpus can express it".

    **So the hypothesis is a cheaper implementation of the same rejection, not a cheaper rejection.** `parseTempBranchless` already loads the field as one 8-byte word (`binary.LittleEndian.Uint64`) and already derives the `.` position from it with `TrailingZeros64`; the format check re-reads the same bytes individually afterwards. Testing digit-ness of several bytes at once on the word that is already in a register is the standard SWAR form (`(w - 0x30…) | (w + 0x46…)` masked, one AND and one compare against the dot mask the parse computed), which would replace a chain of dependent byte loads and branches with a few word-level ops. **The contract does not move**: the binary must reject byte-for-byte what `gen.Aggregate` rejects.

    **Prediction:** the ceiling is the measured 18.2% of CPU (compute floor 1.235 s → 1.010 s) and it is NOT reachable, because the check is replaced rather than removed; a SWAR form that costs a quarter of the current one would return ~13%, and anything at or above 8% is worth keeping. Unlike most of this board the risk is on the correctness side, so it ships with a rejection test per malformed class — bad digit, doubled sign, missing dot, missing `\n`, two decimals, out-of-range — each asserted against `parseTempScalar`'s independent answer and against the reference, before anything is timed.

    **CLOSED — E-25, KEPT: −4.97% of wall and −6.33% of user CPU, disjoint, new best 1.424 s.** The 8% bar was missed and the arm was kept anyway, with the reasoning in the row. The "quarter of the cost" mechanism is falsified: the new check costs 65.2% of the old. The correctness demand was met by exhaustive differential sweep rather than by a case list — 2,000,000 six-byte fields over the alphabet the shape is built from, two over-read fillers, every verdict compared against both `parseTempBranchless`+`validTemp` and `parseTempScalar` — which contains every named malformed class and ten mutants, all caught.

### Added by the round-3 instrument pass (E-32)

18. **More workers still — sweep `-workers` above the 20 that E-17 made the default.** E-32 decomposes the gap into 8.0 points of system CPU with no live mechanism and 11.5 points of idle cores, and it measures why the cores are idle: the mean number of workers with work to do is **12.85 of 20** against 15 cores, so the binary does not on average have enough runnable goroutines to fill the machine. Blocked-read time is a non-constant in the reader count (5.097 s at 15 readers, 8.673 s at 20), so each added worker buys **0.300** of a runnable worker rather than a whole one, and 15 runnable extrapolates to **27.2 workers** [DERIVED, linear on two points, one of them E-31's `off-w15` arm].

    **The competing reading is named, because it is what the arm distinguishes.** Idle cores are consistent with *too few runnable goroutines* (this item's reading, which more workers fix) and equally with *the scheduler not waking a P fast enough* for goroutines that are already runnable (which more workers do not fix, and which the 0.63 workers' worth descheduled in the same runs is also consistent with). Nothing in E-32 separates them; only building the arm does.

    **Prediction:** `-workers 24` and `-workers 28` recover part of the 11.48 idle points against the 20-worker default; the ceiling is perfect packing at **1.0740 s, −11.48% of wall**, and the realistic figure is well under it because each added reader lengthens the queue it waits in — **anything over 5% would be a surprise, and even the ceiling lands 7.40% over the 1.000 s target.** The second channel is parallel efficiency, which must rise from 80.5% if the mechanism is real. E-12 measured 20 against 30 and could not separate them; that invocation carries the slot bias E-16 and E-18 priced, and its binary is three arms old, so it constrains nothing here.

    **Runnable now, no code:** `-a 'default.a=' -a 'w24=-workers 24' -a 'w28=-workers 28' -a 'default.b='` — four slots at 1b, so it must be backgrounded.

## The queue board — state, cost, and the command

The two lists above hold the hypotheses and their reasoning. This is their **state**, and it is what `go-opt-round-1` dispatches from; the numbering is theirs, never reassigned, because E-10 cites "queue item 2" and a renumbered queue would silently re-point it. Thirteen of the fourteen seeded items are open, which satisfies the harness ticket's floor of ten.

**Re-ordered by the round-2 profile, and the ordering rule changed with it.** Until E-20 the queue was ordered by "the size of the gap each could close" with the gap read as wall clock; the compute floor of 1.235 s against a 1.000 s target makes **share of measured CPU** the ordering key, because a mechanism that does not remove CPU cannot reach the target no matter what it does to scheduling. So the top of the board is now the four items that cut CPU per row, ordered by the share E-21 measured, and H-14 — still the largest single mechanism, at a 17.6% ceiling then and **19.1% against E-25's smaller wall** — sits below them because at its ceiling it lands on the compute floor, 1.24 s then and **1.152 s now**, and never below it. [**SUPERSEDED by E-27, and the ordering rule inverts with it: the compute floor is now 0.9812 s, BELOW the 1.000 s target, so "cuts CPU per row" no longer selects for reaching the target and H-14 — 20.4%, landing at 0.9812 s — moves to the TOP of the board. Round 3 is ordered by wall clock recoverable from scheduling, and the CPU items are worth what they are worth on their own.**]

**The shares this board is ordered by were re-derived once and the re-derivation was wrong; E-26 measures them instead.** [**CORRECTED, C9.**] The falsified paragraph read: E-21 profiled a binary whose total was 18.53 s of user CPU, E-25 removed 6.33% of it without touching the other functions, so every surviving item's share ROSE against a smaller total — `indexDelim` 33.5% → 35.9%, `(*table).update` 15.9% → 17.1%, `memequal` 5.8% → 6.2%, bounded by the two incumbents agreeing to 0.45%. **What that bound could not catch is that the total changed COMPOSITION rather than size:** E-24 had already moved the default read buffer 4 MiB → 1 MiB, and `syscall.rawsyscalln` went 15.7% → 54.03-56.66% with it, so every other share is compressed against a denominator that is mostly not compute. Measured now on the shipped default, `indexDelim` is **17.73-20.36%** of samples, and profiling the same binary at `-buf 4096` puts it back at 31.27%.

**So the board is ordered by share of the NON-syscall samples, which is the only figure that survived the buffer change** — `indexDelim` **40.9-44.3%**, `parseTempWord` 17.7-21.7%, `(*table).update` 12.4% flat, `memequal` 7.2-8.7%, `foldRows` itself 6.6-8.8%, `merge` 2.0%, all agreeing to within 3.4 points across three profiles at two buffer sizes (E-26). Against the 17.283 s of user CPU measured over three runs, `indexDelim` is **7.1-7.7 s**. The syscall bucket is excluded because it cannot be CPU: it holds 7.51 s of samples in a run whose whole measured system time is 1.52 s.

**E-32 re-measures those shares on the shipped default, and what holds is the RANK, not the numbers.** `-fold ptr` removed 13.5% of user CPU between the two profiles; measured now, `indexDelimAt` **41.57-43.64%**, `parseTempWordFrom` 17.84-19.73%, `(*table).update` 12.52-14.05%, `memequal` 8.81-9.87%, `foldRowsPtr`'s own 7.39-9.96%, `merge` 1.92-2.59% — the same six functions in the same order, with the first two inside E-26's brackets and the other four **0.59-1.65 points above** them. Against the **14.6567 s** of user CPU measured over the three unprofiled runs in the same window, `indexDelimAt` is **6.09-6.40 s** — and reaching 1.000 s needs **−17.58%** of user CPU, so it is the only pot on the board large enough to matter and the board has already spent both of its mechanisms on it (item 1 closed not-adopted, item 2 killed).

**Runnable now** means every arm is already a flag on the shipped binary, so the experiment costs only its own timing — about `N × 6 × 1.75 s` for N arms at 1b, so 31.5 s for a three-arm row, plus a correctness pass per arm. **Needs code** means an arm has to be built and gated first, and the ledger row cannot be opened until it is.

| # | hypothesis | state | what it takes |
|---|---|---|---|
| 18 | more workers still, `-workers` above 20 | **PARKED — `PARKED.md` P-06. Still runnable now; the round closed without spending a window on it because its ceiling misses the target (E-32)** | the mean number of workers with work to do is **12.85 of 20** against 15 cores, and each added worker buys **0.300** of a runnable one (blocked read 5.097 s at 15 readers, 8.673 s at 20), extrapolating to 27.2 workers for 15 runnable. Ceiling is perfect packing at **1.0740 s** — −11.48% of wall and still **+7.40% over target** — so it is a candidate for the gap, never for the goal; predicted well under 5%. Four slots, `-a 'default.a=' -a 'w24=-workers 24' -a 'w28=-workers 28' -a 'default.b='` |
| — | the kernel's copy of 13.8 GB into the read buffers | **CLOSED by an existing kill (E-32)** | **7.99 points of the 19.47% gap**, and it is system CPU that the compute floor's definition excludes rather than idleness. It scales with bytes, not calls, so no buffer size touches it; the only mechanism that removes a copy is `-io mmap`, **KILLED at 5.6x** (H7). Listed so the gap's decomposition has a row for each of its parts |
| 16 | establish the same format rejection from the word the parse already loaded | **CLOSED — E-25, KEPT** | −4.97% of wall, −6.33% of user CPU, disjoint in a 0.067% bracket; `-parse word` is the default and the best is 1.424 s. The 8% keep bar was missed and the arm kept on the reasoning in E-25. **This row's own wording was the C7-falsified one ("from the integer, not the bytes") until this cycle rewrote it, which is the standing lesson about corrections not reaching the line a cycle builds from** |
| 15 | re-measure invocation B's six rows with the cooldown on | **CLOSED — E-23 and E-24** | one reversal (`-buf 1024`, −6.34% disjoint, now the default at a swept minimum) and five margins that do not reproduce, including a 21% kill and an 11.4% keep. It took four invocations: two produced no verdict (E-22) and the two that did agree to 0.27%. E-02, E-03 and E-05 are re-opened by it, and E-18's derived corrections are falsified (C8) |
| 1 | fuse the name hash into the separator scan | **CLOSED — E-27, NOT ADOPTED** | [**CORRECTED, C9.**] This row said `indexDelim`'s 33.5% was item 1's pot. It is not: the fuse keeps the scan and removes only the SECOND load of the word, which `-peek` puts at **1.07-1.72% of compute** (`foldRows`'s own inlined `Uint64`, 0.11 s of 6.39 s and 0.10 s of 9.37 s). The item's own prediction — "small, the hash already reads only one word" — was right all along, and the profile never addressed it. Measured in isolation (`both` against `ptr`, the same function and one bool): **+0.81% of wall, −0.11% of user CPU, ranges overlapping, the two channels disagreeing in sign.** The fuse is not in the default; `-fold hash` and `-fold both` stay as the arms it was measured on |
| 6 | more workers than cores | **CLOSED — E-17, KEPT, and E-31 re-prices it against today's baseline** | the default is now `NumCPU()*4/3`; 20 and 30 did not separate, so the optimum is a plateau. E-17 measured the win at 7.5% on a binary three arms ago; **E-31 measures removing it at +14.09% of wall (1.397 s against a 1.2245 s bracket mean, parallel efficiency 69.7% against 80.2%/80.0%)**, so it is worth roughly twice what the board carried, and what it buys is read overlap across workers rather than finish-time balance (skew moves 1.014-1.018 → 1.021-1.025 when it is removed) |
| 7 | buffers above 4 MiB | **CLOSED — E-17, KILLED-on-numbers** | 8 MiB is 3.05% slower slot-corrected; E-13's +8.64% was a slot artifact |
| 4 | the correctness tax, priced | **PRICED — E-21, and its escape clause fired** | measured at 18.2% of CPU for `validTemp` alone against a predicted 3-8%; the trusting arm is no longer needed to price it, and the answer is item 16 rather than a price tag |
| 13 | H-13, quotiented 32-byte entry | **CLOSED — E-28, KILLED-on-numbers** | **+5.86% / +4.83% of wall and +6.98% / +6.80% of user CPU, disjoint from both bracket arms in a 0.98% bracket.** Predicted 0-4% faster, measured 5.3% slower against the bracket mean, and its compute floor is 1.0456 s — the arm puts the floor back ABOVE the 1.000 s target that E-27 took it under. Its target was `memequal` **7.2-8.7% of compute** and `(*table).update` **12.4% flat** (E-26); shrinking the bucket 48 → 32 bytes did not collect either. `-table quot` stays as the arm it was measured on. **With E-03's −4.6%, two independent losses now say the bucket LAYOUT is the exhausted neighbourhood, not an untried one.** **Its escape clause is now RUNNABLE and still untried:** the 10,000-station regime this row named as the only thing that could re-open it is `measurements-10k-stations-242m.txt`, built by `go-opt-round-3-10k`, and `-table quot` on that file is a flag that already ships — **now parked as `PARKED.md` P-09 with the arm written out, because a runnable escape clause recorded only inside a killed row's prose is one nobody will run** |
| 5 | `unsafe.Add` pointer walks in the fold | **CLOSED — E-27, KEPT, and it is the study's biggest single arm** | **−11.80% / −11.87% of wall and −13.49% / −13.67% of user CPU, disjoint from both bracket arms in a 0.072% bracket; new best 1.233 s and `-fold ptr` is the default.** Its own "half reachable in safe Go" clause is measured and slightly under: the slice-walk arm gets **4.79 of the 11.80 points, 40.6%**. The predicted 4-16% ceiling held; the 1-4% this cycle predicted from `foldRows`'s flat share did not, because most of the win came from the rewritten scan the arm needed rather than from the walk |
| 14 | H-14, double-buffer each worker | **CLOSED, KILLED-on-numbers (E-29)** | built as `-fill sync \| ahead` and measured at **+0.57%/+1.57% of wall, overlapping both bracket slots** (E-30 re-measures the repaired reader at +0.82%/−0.08% and confirms it), against a prediction of −11% to −20.4%. Every ceiling this row published (25% → 17.6% at E-20 → 19.1% at E-25 → 20.4% at E-27) was arithmetic on a sound floor and an unsound assumption: the read is already overlapped ACROSS workers by E-17's oversubscription, so there was nothing for a per-worker prefetch to hide. Parallel efficiency 79.8% → 79.3%. The `-buf` re-measure this row demanded was run and **E-24's 1 MiB held** (512 KiB +5.76%, 4 MiB +6.57%, disjoint). `CORRECTIONS.md` C10 |
| 17 | are the two overlap mechanisms substitutes? | **CLOSED — E-31, ANSWERED: yes** | `-fill off -workers 15` costs **+14.09%** of wall against the bracket mean, `ahead-w15` recovers **72.5%** of that (both disjoint from both bracket slots), and the same prefetch stacked on the oversubscribed default recovers **0%** (+0.53%, ranges overlapping, a third reproduction of E-29/E-30). The `-phases` half is what makes it a verdict: sum(worker wall)/(read+fold) is **1.0011** for both `-fill off` arms and **0.7368** for `ahead-w15`, so **67.5% of that arm's read is measurably off the worker's critical path** and it still loses 3.88%. DERIVED: `-fill ahead -workers 15` is 30 runnable goroutines on 15 cores, i.e. oversubscription spent through a dedicated reader per worker, which is why it is a partial substitute and not a duplicate. Removes scheduling from the gap's suspect list; the incumbent is still the best arm at 1.220 s. Also answers `PARKED.md` P-05 |
| 11 | H-11, core-class-weighted split | **SUBSUMED — E-20, see PARKED.md P-05** | there is at most 3.8% of finish-time imbalance to collect (worker wall spread 1.029-1.038) and E-17's oversubscription already collects it; revives only if the default returns to one worker per core |
| 8 | a 1b-row 10,000-station file | **ANSWERED by refusing it — `go-opt-round-3-10k` built the byte-constant file instead** | the 1b-row form is still 57.09 GB, 2.22x RAM, a 3.12 s read floor, and nobody should build it [CORRECTED, C5: priced as "~20 s"]. `measurements-10k-stations-242m.txt` (241,600,000 rows, 13,798,576,606 B, +0.0215% of the 1b file, sha256 `ab9b3483…`) holds the memory regime constant and changes only the key set, which is what items 9, 10 and 12 were asking for. It unblocks them; it is NOT a verdict file (E-09) |
| 9 | Go's runtime map for the 10k case | **CLOSED — E-33, and E-34 prices the other regime** | `-table map` measured **−12.81% of wall and −14.51% of user CPU** on the 242m 10k file, disjoint from both bracket slots in a 0.078% bracket, against a prediction of −1% to −4%. Mechanism-only. **E-34 answers the 413 half and the default does NOT move: the same arm is +52.20% of wall and +58.77% of user CPU at 1b, disjoint, compute floor 1.551 s.** The key set inverts this choice by 65 points. The arm removes more than the probe — 120.0 MiB of tables never allocated, system CPU −5.48% on an identical I/O path |
| 12 | H-12, vectorized table probe | **PARKED — `PARKED.md` P-07, needs code; its file now exists** | predicted under 3% at 413 stations, over 10% at 10,000. Left open by `go-opt-round-3-10k` with this measured state: E-33 shows the 10k regime's cost is NOT concentrated in the probe the way this item assumes — the map arm removed 14.51% of user CPU while allocating 120.0 MiB less, and no profile separates probe from allocation on this file, so the item needs one before the arm |
| 10 | the merge and the output at 10k stations | **CLOSED at 413 stations — E-20**; the 10k half **PARKED — `PARKED.md` P-08**, still runnable with no code | measured at 5.1 ms, 0.34% of wall by `-phases`, so there is nothing here at 413. `go-opt-round-3-10k` left the 10k half open with its file built and no code needed: `-phases` on `measurements-10k-stations-242m.txt` reports the read/fold/merge split directly, and E-33's incumbent slots put an upper bound on it at 1.276 s of wall. Fix: run `-phases` on both `-table` arms of that file and read the merge bucket |
| 3 | the idle 25% of the cores | **CLOSED — E-20, ANSWERED** | it is the read: 29-30% of worker wall blocked in `pread`, 5.75 of 20 workers at any instant, while skew is under 4% and the merge under 0.4%. The answer it produced is that the binary is compute-bound and the target sits below the compute floor |
| 2 | the batch tokenizer | **CLOSED — E-10, KILLED-on-numbers** | `-kernel batch-swar` and `-kernel batch-neon` stay for reproduction |

**The board's FINAL state, set by `go-opt-round-3-park`: no row is OPEN any more, and three are runnable-but-parked.** Of the 18 numbered items, 13 are closed (item 8 among them, its cell reading ANSWERED-by-refusing), item 4 is priced, item 11 is subsumed, items 18 and 12 are parked, and item 10 is closed at 413 stations with its 10k half parked. Parked is not the same as closed and the distinction is the register's whole job: **P-06** (item 18) and **P-08** (item 10's 10k half) need no code and could run in the next quiet window, as could **P-09**, which is not a board row at all but E-28's escape clause; **P-07** (item 12) needs code. Each carries the test that would revive or kill it. The paragraphs below are the round's own history and are kept as written — they record what the runnable-now set looked like at each point, which is how the board's ordering decisions can be audited.

**E-32 puts ONE row back into the runnable-now set: item 18.** It is there because the instrument pass measured a supply of runnable workers (12.85 of 20) below the core count, which is a mechanism rather than a hunch — and it arrives with its own ceiling, 1.0740 s, which does not reach the target. Everything below is what E-31 left, and it still holds for every other open item. The paragraph is kept rather than rewritten because its reasoning is what item 18 had to clear.

**`go-opt-round-3-10k` puts TWO more rows back, and neither needs code.** The clause below that reads "a file that does not exist yet" is answered: `measurements-10k-stations-242m.txt` exists, so item 10's 10k half is a `-phases` run on the shipped binary, and item 13's own escape clause — bucket layout re-opens only in a measured 10,000-station regime — is `-table quot` on that file, a flag that already ships. Both are MECHANISM runs: E-09 stamps them NOT A VERDICT and anything they keep must be re-measured at 1b. Item 12 still needs code, and item 8's 1b-row form is refused rather than pending.

**With E-31 the runnable-now set is EMPTY.** Item 17 was the last row whose every arm was already a flag on the shipped binary, so from here on every open item needs either code (9, 12) or a file that does not exist yet (8, and the 10k halves of 10 and 12) — which is what `go-opt-round-3-gap` and `go-opt-round-3-10k` are for. The block below is the historical item-15 example, kept because it is what the "incumbent first AND last" rule looks like in an invocation, not because it is dispatchable.

The runnable row, verbatim. It is item 15, and it costs eight arms: **the incumbent goes first AND last**, which is what makes the rest of the invocation readable (E-17), and the cooldown is now the default so it need not be passed.

```
bash 1brc/scripts/experiment.sh -i 'queue item 15 - invocation B re-measured with the cooldown on' \
  -p 'E-18 derives -4.49% for buf 1M, -1.86% for bits 14 and -5.35% for the page cache once the slot bias is subtracted; if that extrapolation holds, at least one of the three changes sign here' \
  -a 'default.a=' -a 'buf1M=-buf 1024' -a 'bits14=-bits 14' -a 'pagecache=-nocache=false' \
  -a 'split=-table split' -a 'scalar=-parse scalar' -a 'cursor=-split cursor' -a 'default.b='
```

At 20 s of cooldown and 5 runs that invocation costs about **8 × 6 × 21.7 s = 17 minutes**, which is the price of the fix and is why item 15 is a round of its own rather than a line item. `1brc/scripts/bench.sh` has NOT been given the cooldown; it measures one command at a time, so it has no slots to bias, but a bench.sh number is still not comparable with an experiment.sh arm.

**Item 8 is not the cheap unblocker three other items assume it is, and the reason is a design question, not a cost one.** Derived from the recorded size of `measurements-10k-stations-10m.txt` (570,941,611 B for 10,000,000 rows, `02-baseline-data.txt`): the 10k key set costs **57.09 bytes per row** against the official set's 13.80, because the synthetic names are longer. A *1-billion-row* 10k file is therefore **57.09 GB — 2.22× this machine's RAM**, where `measurements-1b.txt` is 53.5% of it, and its read floor alone is **3.12 s** at the 18.3 GB/s measured in `02-baseline.md`. Every arm would land three times over target before any compute, so nothing measured there could be compared with the 1.742 s headline. Holding **bytes** constant instead gives **241.6M rows** at 13.80 GB, which reproduces the memory regime the 1b file established and is the file items 9, 10 and 12 actually want. The round must pick one knob and say which; generating both is 79 s and 19 s of a generator that reproduces byte-for-byte from its seed, and 329 GB of disk is free.
