### def-understand (4145227)
meant: spec.md:60 (stage 1, "Definition") and the `def-understand` backlog line — write the definition from the official repo, cited, and restate our target for this machine.
did: `1brc/01-definition.md` (rules table, output semantics derived from `CalculateAverage_baseline.java`, eval procedure, leaderboard top-5, our target as a GB/s rate); `1brc/01-definition-data.txt` (fetch hashes + upstream commit `db06419`, `sysctl` machine record, probe test output); `1brc/definition.md` symlink; `1brc/LICENSES.md`; `1brc/code/gen/tenths.go` + `tenths_test.go` — `Tenths`, `RoundToTenths`, `AppendTenths`, `ParseTenths`, `Mean`, the integer-tenths core the whole study aggregates in.
deviations: (1) The report's output semantics are DERIVED from reading the cited Java source, NOT confirmed against a running JVM — this machine has a Java 8 JRE with no `javac` and the baseline needs Java 17+. I originally wrote "cross-checked empirically against a JVM", found no JDK, and corrected the claim rather than dropping the check silently; the revive trigger is recorded in `01-definition-data.txt`. (2) The report cites a discrepancy in upstream's own docs (README says 5 runs/keep 3, `evaluate.sh` does 10 runs/keep 8) and follows the script. (3) `minTenths`/`maxTenths` were moved from `gen.go` into `tenths.go` so this commit is green standing alone.
verified: `cd 1brc/code/gen && gofmt -l . && go vet ./... && go test ./...` — all green with `gen.go`/`reference.go`/`cmd` stashed out, i.e. this commit is green in isolation, not only at the end of the cycle. Green covers the arithmetic and formatting invariants; it does NOT cover agreement with a real JVM (see deviation 1).
threads: two test expectations I wrote were wrong and the code was right — `-0.05` and `-0.1/2` LOOK like rounding ties and are not, because the nearest float64 sits just below the tie. Any future "our mean is off by one tenth" hunt should suspect the float64 representation of the tie before suspecting the rounding rule. Java's UTF-16 name order diverges from Go's byte order only above U+FFFF; pinned in `TestUTF16OrderDivergesAboveBMP` so it is not rediscovered as a sort bug.

### env-data (d22e3dc)
meant: spec.md:26-32 (inputs and data) and the `env-data` backlog line — build the generator, generate the standard files outside the repo, record command/rows/bytes/sha256 per file, and produce the trivially-correct reference plus expected 10m output.
did: `1brc/code/gen/stations.go` (413 stations mechanically extracted from `CreateMeasurements.java`), `gen.go:Write`/`Official413`/`Synthetic10k`, `reference.go:Aggregate`/`WriteResult`, `cmd/measurements`, `cmd/reference`; four files generated into `/Users/noelruault/Downloads/1brc/1brc-assets/` (10m, 100m, 1b, and a 10k-station 10m stressor); `1brc/02-baseline-data.txt` holds every command, row count, byte size and sha256; `1brc/testdata/expected-10m.out` and `expected-10k-stations-10m.out` are committed (10 KB / 669 KB) so the correctness gate is self-contained.
deviations: (1) Seeded PCG instead of upstream's ThreadLocalRandom — upstream's file is not reproducible and this study needs it to be. (2) Values are clamped to [-99.9, 99.9]; upstream does not clamp, the bound is beyond 6 sigma so behaviour is unchanged, but conformance becomes structural. (3) `Synthetic10k` is this repo's own name synthesiser, NOT a port of upstream's `CreateMeasurements3` — same purpose (10,000 keys, names to 100 bytes, multi-byte UTF-8), not byte-compatible; the definition report establishes the 10k set is a test-suite stressor and not what the leaderboard was benchmarked on, so byte-compatibility buys nothing. (4) The reference has NOT been run on the 1b file — spec.md defers that to `final-dod`. (5) `1brc/scripts/check-correctness.sh` is deliberately NOT written here: spec.md:37 assigns it to `go-skeleton`, so this ticket ships the expected outputs it will consume and nothing more.
verified: `cd 1brc/code/gen && gofmt -l . && go vet ./... && go build ./... && go test ./...` green. Beyond the gate: regenerated the 10m file with the recorded command and `cmp`'d it byte-identical to the recorded sha256; re-ran the reference over it and `cmp`'d the output identical to the committed expected file; confirmed 413 distinct stations in the data and 413 entries in the output. Green does NOT cover agreement with upstream's Java output (no JDK, see def-understand) and does NOT cover the 1b reference pass.
threads: two of the 413 official station names contain a comma ("Washington, D.C.", "Flores,  Petén") and the output separator is ", " — so the result line cannot be split on ", " to recover entries, and byte-comparison is the only sound check. Generation runs at ~725 MB/s single-threaded (1b in 18 s), so the parallel generator I was ready to write was not needed; if a future ticket regenerates often, that is the first thing to reach for. sha256 of the 13.8 GB file is 6.2 s via `openssl dgst` and roughly 20x slower via `shasum` — use openssl.

### env-baseline (d5d62fb)
meant: spec.md:46 (measure the physical floor, derive what <1s demands) plus spec.md:35's warm-cache headline rule
did: new `1brc/code/gen/cmd/floor` — `run()` dispatches four modes, `parallelScan()` preads disjoint ranges, `mmapCount()` counts over a MAP_SHARED mapping, `-nocache` sets darwin F_NOCACHE (48) on the fd. Report `1brc/02-baseline.md`, raw output appended to `02-baseline-data.txt` under its own heading. Headline: 754.4 ms ± 8.8 ms to read all 13.8 GB (15 preads, 1 MiB, uncached) = 75% of the 1.0 s budget.
deviations: three, all recorded in the report. (1) spec.md:35 designates the warm page cache as the headline state; that state is unreachable here because the file is 53.5% of RAM, so both numbers are reported and the conflict is flagged rather than the rule being rewritten. (2) No cold-cache number at all: `purge` needs sudo, this loop is unattended, so F_NOCACHE stands in and is labelled "uncached", never "cold". (3) The floor tool went into the `gen` module rather than a new one, to avoid adding a gate target to spec.md for a measurement tool; `gen` is now "support tooling" broader than its name.
verified: `cd 1brc/code/gen && test -z "$(gofmt -l .)" && go vet ./... && go build ./... && go test ./...` green. Four tests cover the tool: mode agreement across chunk sizes, exact byte/line accounting under 1-100000 parallel workers, the F_NOCACHE path, and mmap slice boundaries. Green does NOT cover the timings themselves — those are single-session observations plus hyperfine 5-run means, reproducible only on this machine in this state.
threads: mmap `workers=4` is a reproducible pessimum (18.46 s then 18.00 s, worse than 1 worker) and the cause is NOT established. Buffer size swept only at 1 MiB vs 8 MiB. F_NOCACHE untested against mmap. Nothing here measures read competing with aggregation for bandwidth, which is `go-v1-parallel`'s first job.

### asm-recon (61dcbb6)
meant: spec.md:47 (technique inventory mapped to arm64 with testable hypotheses) under spec.md:38's clean-room rule
did: `1brc/03-technique-recon.md` + data companion, from the top five entries at upstream `db06419` read for technique only. Six numbered hypotheses (H1-H6) each with a test and a prediction, divided between `asm-kernels` (H2, H3, H6) and `go-v1-parallel` (H1, H4, H5). `LICENSES.md` gains the five filenames and flags the two close derivations.
did: verified rather than described — probe 1 (C/NEON) confirms `vceqq_u8` + `vshrn_n_u16(...,4)` + `ctz>>2` at all 16 positions; probe 2 writes the same idiom in Go Plan 9 arm64 asm and passes against a scalar reference, so `asm-kernels` needs no cgo; probe 4 runs merykitty's parse over all 1999 legal temperatures, 1999 correct; probe 3 shows `bytes.IndexByte`/`Count`/`Equal` are already NEON on arm64.
deviations: a draft asserted "88.6% of station names exceed 8 bytes" as H2's rationale. Invented number, wrong by 2.6x; measured 34.1% (probe 5), and H2's prediction was rewritten to say the margin may go either way. The correction is recorded in the data companion rather than tidied away. Also: ranks 4-9 were not read, and gigatoken's module sources are directories that raw-fetch cannot retrieve, so its contribution is a scale reference only.
verified: no repo code changed, so the gate is unaffected; `gen`'s gate was run anyway and is green. The four probes ran in `/tmp` (`clang -O2 -arch arm64`, two throwaway Go modules) with their source and output transcribed into `03-technique-recon-data.txt`. Nothing in the repo re-runs them, which is the honest limit: they are recorded evidence, not a standing check.
threads: +31 vs +1 collision stride is unmeasured by anyone in public. H4 (are 16 bytes of name enough to hash both key sets) is free to answer and gates the hot loop's shape — answer it first. Nobody in the corpus measured on Apple silicon, so every "faster" in their comments is an x86-64 result.

### go-skeleton (ddefd8f)
meant: spec.md:50 item 6's first step (skeleton with correctness gate) and spec.md:22-27's gate definition
did: `1brc/code/go` as its own module (`replace` → `../gen`), `main.go:aggregate` single-goroutine bufio+map, `Makefile:gate` mirroring spec.md exactly, `1brc/scripts/check-correctness.sh` (byte-compare on both 10m files, 413 and 10000 stations, cmp-identical) and `1brc/scripts/bench.sh` (correctness first, then hyperfine, raw output to `1brc/bench/<stamp>-<label>.txt` with commit, dirty flag, go version, machine).
did: `gen` gains exported `MinTenths`/`MaxTenths`, an added member the group needed — the skeleton has to enforce the same range as the reference and restating -999/999 in a second module is the duplication the review gate looks for.
deviations: the skeleton imports `gen.WriteResult` instead of reimplementing rounding and formatting, so `check-correctness.sh` verifies the read/parse/aggregate path and NOT the formatting (which `gen`'s own tests cover). That is a deliberate trade for one definition of the output contract; an independent reimplementation would be stronger verification but two places to publish the same rounding rule. Also edited spec.md:23 to add `test -z "$(gofmt -l .)"` to the code/go gate, matching the gen gate — a contract edit, small, and the gap pre-dated this group.
verified: `cd 1brc/code/go && test -z "$(gofmt -l .)" && go vet ./... && go build -o bin/1brc . && go test ./...` green; `bash 1brc/scripts/check-correctness.sh` exit 0 on both files. Tests assert byte-equality against `gen.Aggregate` on a fixture containing both comma-names, a rounding tie, and both range endpoints, plus six malformed inputs where both implementations must agree to reject. Measured: 260.5 ms ± 1.5 ms at 10m and 2.607 s ± 0.042 s at 100m, i.e. 26.1 ns/row, linear across a 10x size change.
threads: 26.1 ns/row is 26x the 1.00 ns/row budget and the skeleton is single-threaded, so nothing here is a speed result. `bench.sh` defaults to `FILES="10m 100m"`; the 1b file is opt-in because the naive binary needs ~26 s per run. A rounding tie in the test fixture (mean of -24.7 and 0.0 is exactly -12.35 → -12.3, not -12.4) caught a wrong expectation I had written, which is the third time this study has been bitten by a tie.

### asm-kernels (b4be167)
meant: spec.md:50 item 3 (explore N kernel approaches on arm64, microbenchmark each, a measured winner emerges), answering H2/H3/H6 from `03-technique-recon.md`
did: `1brc/code/asm` as its own module (`replace` -> `../gen`), gated by `make test`. Kernels: `kernel.go` scalar references + `swarIndexByte` + merykitty's branchless parse; `neon_arm64.s:neonIndexSemicolon` (16-byte `CMEQ`/`SHRN #4`/`VMOV`) and `neonTransferProbe`; `tokenize.go` with four whole-row shapes — `TokenizeScalar`, `TokenizeStagedSWAR`, `TokenizeStagedNEON`, `TokenizeBatch` (32-byte dual-needle, emits a `Token` stream).
did: `04-asm-kernels.md` declares a winner per sub-problem; `04-asm-kernels-data.txt` carries every number with its command; `1brc/PARKED.md` created (P-01 per-row NEON scan, P-02 NEON parse) and indexed in root `PARKED.md`.
deviations: the NEON temperature parse H3 named was NOT built — parked as P-02 on a measured bound (a per-row NEON kernel pays 1.080 ns/row transfer; the whole spread between the two parses built is 1.7-3.3 ns/row), which is a bound and not a measurement of the kernel, and P-02 says so. H3's `<=2 ns/row` half is unanswered: no benchmark here isolates the parse from the loop around it. Everything is PROVISIONAL (battery, `spec.md:42`). "Transfer" throughout means transfer-plus-non-inlinable-call; Go has no intrinsic to separate them. The 10k corpus is a long-name stress case (51.1-byte mean), not a second realistic input, and no verdict rests on it alone.
deviations: no ASM-vs-C harness — kernels are Go and Go asm, because the study ships a Go binary and `go-v2-kernels` has to price the call anyway. Two commits of this group predate this cycle (54e700d..a4a49df, written by a cycle that was cut off before it could ledger or push them).
verified: `cd 1brc/code/asm && make test` exit 0 at every commit. Correctness is scalar-reference comparison over both corpora, 200k random inputs, adversarial input the corpora cannot produce (high-bit UTF-8, names at every length around both window boundaries, `:`/`<` neighbours of `;`), and all 1999 legal temperatures value-and-offset at every alignment. `TestNEONIndexSemicolonStopsAtTheSliceEnd` `PROT_NONE`s the next page so an over-read segfaults — the one bug class Go cannot catch. Green does NOT cover: any end-to-end behaviour, anything on AC power, or the batch kernel competing with a real hash table for L1.
threads: the batch kernel's win (-40.4% / -40.7% post-repair, see below) is measured with a 4096-token buffer drained immediately; a real aggregator folds into a hash table instead, and whether the win survives that is `go-v1-parallel`'s to measure — the single most load-bearing untested assumption in the report. `batch/413` is also the noisiest loop of the eighteen (15.6% full-range spread), which is why the verdicts are justified by run-range separation rather than by median gaps. One within-run gap is measured and unexplained: `staged-swar` and `ParseTemp/branchless` compute the same tokenization and differ by 1.930 ns/row with the faster one doing strictly more work; the control run rules out closure placement, and no verdict depends on it. Multi-window batching (2 or 4 windows per transfer pair) is untried and would move the long-name crossover.
reviewed: REPAIRED in this cycle, after this block was written. The batch kernel ended a name at the LAST `;` in a row while every other kernel ends it at the first (`gen.Aggregate`'s rule), unguarded because neither key set produces a name containing `;` — so the corpus agreement tests passed with the bug present. Fixed in `730d166` with `TestEveryKernelEndsTheNameAtTheFIRSTSemicolon`, which fails when the guard is reverted while the old tests still pass. The published -42.3% measured that kernel and is superseded by two post-fix runs (-40.4%, -40.7%), corrected at every site with `CORRECTIONS.md` C3. Also repaired before the first push: the report's Method section published 0.604/0.133 for H6 against its own table's 0.513/0.165 (a cross-run mix its own methodology forbids), claimed run-to-run spreads of "1-3%" where the measured range is 0.8-15.6%, cited a control run whose raw output was in no committed file, and pointed at a `PARKED.md` that did not exist.

### skill-performance-assembly (1c3fa1e)
meant: spec.md:51 (stage 4, skill distillation) and spec.md:61's DoD line — the global skill exists at the named path, employer-agnostic, frontmatter like the siblings.
did: `/Users/noelruault/.claude-work-home/.claude/skills/performance-assembly/SKILL.md`, 165 lines, distilled from `04-asm-kernels.md` + `03-technique-recon.md` + `02-baseline.md`. Sections: the vector-to-general transfer model, SWAR-before-NEON, both verified arm64 movemask idioms as Plan 9 asm, branchless-as-misprediction-insurance, shift-vs-LUT with the ILP effect, the seven microbench rules, Go asm quirks, the `PROT_NONE` over-read test, the kernel correctness protocol, darwin memory/I-O, macOS profiling.
did: the artifact is OUTSIDE the repo by design, so this ticket's only repo diff is its ledger. The skill is NOT copied into the repo: that would publish the same numbers at two sites, which `spec.md:41` is written against.
deviations: (1) No `LESSONS.md` companion, unlike `performance-swift`/`performance-zig`, whose self-improvement memory this skill does not copy — the file's own tables are the ledger and a second empty file would be scaffolding; the "Adding a chip" section is the append instruction instead. (2) The backlog line asks for "alignment" coverage and there is none: no kernel in this study measured aligned against unaligned loads, so there was nothing measured to distil and inventing guidance was the alternative. (3) Every number carries the machine line AND the provisional label, because the kernel session ran on battery. (4) The profiling section is the only one not grounded in this study's own measurements — it is grounded in tool invocations verified against this machine (`xcrun xctrace list templates` on Xcode 26.6, `/usr/bin/sample`), and says which tool answers which question rather than claiming a profiling result.
verified: no repo code changed, so no module gate applies. Verified instead: the two code snippets are transcriptions of committed, passing code (`neon_arm64.s:19-24` and `:77-92`, `neon_arm64_test.go:11-23`), `go build -gcflags=-d=ssa/check_bce ./...` was run in `1brc/code/asm` and does print `Found IsInBounds`, `xcrun xctrace list templates` does list `Time Profiler` / `CPU Counters` / `CPU Profiler`, `/usr/bin/sample` and `/usr/bin/xctrace` both exist, and a grep for employer/repo/path references over the file is clean. The skill loaded into this session's skill list, so the frontmatter parses.
threads: `1brc/PARKED.md` P-01 and P-02 are NOT mentioned in the skill — the parked ideas are this study's, not portable guidance. If `go-v2-kernels` finds the batch win does not survive a real hash table, the skill's headline table needs a row saying so; it is currently the strongest claim in the file resting on a microbenchmark.

### go-recon (600e9dc)
meant: spec.md:52 (stage 5, how far raw/unsafe Go goes) and the `go-recon` backlog line — mmap, unsafe walks, BCE, a custom open-addressing hash, sharding, GOGC off, Plan 9 interop, plus the Go and Rust corpus.
did: `1brc/05-go-techniques.md` + `05-go-techniques-data.txt` (853 lines, including the probe sources verbatim). Six probes in a throwaway `/tmp/gorecon` module with a `replace` onto `1brc/code/gen`: prefix-collision counts for both key sets (H4), four table shapes behind one identical scan, three spellings of the SWAR scan with their surviving bounds checks, `NumGC` under a v1-shaped loop, and a bare Plan 9 asm call against an inlinable Go one.
did: the four findings that change v1's design — the prefix-hashed open-addressing table beats a Go map by 15.8% on the official key set but LOSES by 3.7% on the 10k stressor (so the Go map stays reachable by flag); H4 is answered outright (16-byte prefixes separate both key sets, 8 bytes leave exactly one collision in 413, `Alexandra`/`Alexandria`); a Plan 9 asm call costs 1.93 ns, roughly twice the whole 1.0 ns/row headline budget, so assembly may only be called per chunk; and the per-CORE budget is 15.0 ns/row, not 1.0, which reframes the skeleton as 1.74x over target rather than 26x.
did: `LICENSES.md` gains the three implementations read, with their licenses VERIFIED via the GitHub API rather than assumed — `shraddhaag/1brc` has no license file at all, so only its author's published write-up was read and nothing from that repo may ever be used.
deviations: (1) H4 belongs to `go-v1-parallel` in `03-technique-recon.md`; it is answered here because it is a static property of the key sets and the table design could not be recommended without it. `go-v1-parallel` must cite this answer, not re-measure it. (2) H5 is NOT measured — the report sharpens it with a mechanism (a 1<<17 entry array is 4 MiB touched at random; a split hash array is 1 MiB) [CORRECTED, C4: the entry is 48 bytes, so the array is 6.00 MiB and the ratio is 6x] and hands it on. (3) H1 and H7 are untouched, as planned. (4) The probes are not in the repo and nothing re-runs them, following the `asm-recon` precedent; the sources are transcribed into the data companion, which is the only durable record. (5) Everything is PROVISIONAL: battery, per spec.md:42. (6) A first benchmark invocation was DISCARDED after the 10k result turned out to be confounded by table load factor; it is recorded in the data companion with its 2-3% drift against the authoritative run, because that drift is the reason nothing is compared across invocations.
verified: no repo code changed, so no module gate applies. Every table shape is checked against `gen.Aggregate` over the 413 corpus before it is priced (`TestTablesAgree`) and the two SWAR spellings are checked against each other over the whole corpus (`TestSWARScansAgree`); both were run and pass. Every verdict-bearing pair in the report is disjoint over ten runs in ONE invocation, and the two pairs that overlap (the 14-vs-17-bit buckets on 413, resliced-vs-indexed on 10k) are reported as "no difference" rather than as margins. Bounds-check claims come from `go test -c -gcflags=-d=ssa/check_bce`, with the function line ranges recorded so the check lines can be attributed. Green does NOT cover: anything sharded, anything end-to-end, anything on AC power.
threads: the resliced-versus-`unsafe` gap (3.8% / 16%) is measured with ZERO bounds checks on both sides and is unexplained — the only unattributed margin in the report. Every table number is single-goroutine, and the 4 MiB working-set hypothesis is the kind that gets worse under 15-way contention. benhoyt r10 fuses the name hash INTO the separator scan while our SWAR skips 8 bytes and hashes separately; fusing-versus-skipping is unmeasured and `go-v2-kernels`-sized. The 10k set's perfect 8-byte separation is an artifact of the synthesiser, so upstream's `CreateMeasurements3` is the source to port if a realistic 10k set is ever needed.

### go-v1-parallel (f786d4e, then 39990ad and 089924b)
meant: spec.md:53 (stage 6's v1 — parallel I/O + per-core sharding + custom hash map, I/O starting from 02-baseline's measured winner with mmap kept as a measured alternative) and the `go-v1-parallel` backlog line, answering H1/H5/H7 and H3's carried flag.
did: `1brc/code/go` grows three files. `kernel.go` — `indexDelim` (dual-needle SWAR), `parseTempBranchless`/`parseTempScalar`, `validTemp`, `hashWord`/`hashName`, `maxRow`. `table.go` — `table` (per-shard linear-probe open addressing, `hashes` non-nil = H5's split layout), `fold` (fast path while a whole row plus its over-fetch fits, scalar tail otherwise), `drain`. `reader.go` — `aggregateFile` (F_NOCACHE pread or mmap, static split or atomic cursor), `foldRange`, `foldMapped`, `rangeEnd`. `main.go` keeps the same output path through `gen.WriteResult`.
did: **1.742 s ± 0.019 s on the 1b file** (`1brc/bench/2026-09-01T152951Z-v1-parallel.txt`), 161.0 ms at 100m, 27.8 ms at 10m — 16.2x the skeleton at 100m (2.607 s), 9.4x at 10m, 1.74x over the 1.0 s target. `1brc/07-experiment-ledger.md` + data companion created with nine rows: H1 killed (cursor is 21% SLOWER, opposite direction to its prediction), H7 killed (mmap 5.6x slower end-to-end, and `MADV_WILLNEED` makes it worse not better), H5 killed for 413 and parked for 10k, H3 kept (branchless wins 11.4% at 1b having LOST 15.2% in the microbenchmark and 4.3% at 100m), plus buffer size, table size and F_NOCACHE sweeps, plus E-09.
did: E-09 is the result worth carrying: **seven arms, seven disagreements between 100m and 1b** — four invert, three vanish. No verdict may be taken from the 100m file, which is now a rule in the ledger.
deviations: (1) The fast path needed TWO correctness additions the recon's plan had no line for, both found by tests rather than by reading: `indexDelim` scans for `\n` as well as `;` (a row with no separator otherwise runs its name into the next row and folds a station that does not exist), and `validTemp` checks the field's shape (the branchless parse reads `b;1.0` from a name containing the separator, and `100.0`, as plausible temperatures). Both are a real cost on the fast path and both are unpriced — queued as ledger item 4. Without them this binary would have passed the 10m gate while disagreeing with the reference on exactly the rows no byte-comparison sees. (2) The SWAR scan and merykitty's parse are REIMPLEMENTED in `code/go` rather than imported from `code/asm`: the ticket says "no asm yet" and the asm module links Plan 9 assembly. That is the same constants block in two modules, which LICENSES.md documents once; the alternative was linking assembly this ticket is not supposed to ship. (3) H5's 10k half cannot be measured at all: the 10k stressor is a 10m file and there is no 1b 10k-station file, so the wall clock there measures nothing. Parked with the generation command as its revive trigger. (4) The batch tokenizer, `04-asm-kernels.md`'s biggest measured win, is NOT integrated — that is `go-v2-kernels` and it is queue item 2. (5) `-madvise` goes through `syscall.Syscall(SYS_MADVISE,...)` because darwin's `syscall` package has no `Madvise`. (6) Everything is PROVISIONAL: battery, per spec.md:42. (7) The headline (1.742 s) comes from the committed bench file and the ledger's within-arm deltas come from a separate matrix invocation whose default arm measured 1.667 s; the two are never subtracted, and the ledger says so where a reader would otherwise see an inconsistency.
verified: `cd 1brc/code/go && test -z "$(gofmt -l .)" && go vet ./... && go build -o bin/1brc . && go test ./...` green at every commit, and `bash 1brc/scripts/check-correctness.sh` exit 0 on both 10m files (413 and 10,000 stations) before every recorded number. The tests that carry the weight: `TestEveryRangeBoundaryIsFoldedExactlyOnce` sweeps 7 worker counts x 4 buffer sizes x 2 splits x 2 readers against the reference (it found all three boundary bugs below); `TestRunMatchesTheReferenceByteForByte` and `TestFoldRejectsWhatTheReferenceRejects` run all 16 strategy combinations, the second with each malformed row placed BOTH in the scalar tail and in the branchless fast path; `TestHashPathsAgree` pins that the word hash and the byte hash cannot split a station across two buckets; `TestBranchlessParseMatchesTheReference` runs all 1999 legal temperatures through both parses and `gen.ParseTenths`; `TestTableTooSmallErrorsInsteadOfHanging` pins that a full table errors instead of probing forever. Green does NOT cover: AC power, a 1b 10k-station file, or any claim about WHY an arm won.
threads: three real bugs were in this code and the boundary sweep found all three, which is the only reason they are not in the 1b numbers. (a) A range starting exactly ON a row boundary skipped its first row — 23 rows silently lost out of 20,000, one per aligned boundary, and the fix is a one-byte lookback so the existing rule handles both cases. (b) The single-needle scan folded a separator-less row into its successor and then cascaded into a `negative shift amount` panic three rows later. (c) A range whose buffer was no larger than the range itself could not read the straddling row's tail, so `rangeEnd` had to become "read more" rather than "error". Also open: the mechanism behind EVERY verdict in the ledger is unestablished (why cursor loses 21%, why the bigger table wins, why 11.3 of 15 cores are busy) — `go-opt-round-2` is profile-driven for exactly this, and a profile will rank the queue better than the queue's own ordering does.

### x-transfer (5528b58, corrected in b13caec)
meant: spec.md:82 stage 6's "cross-disciplinary transfer pass (`x-transfer`) seeds the queue: the repo's signature move", and the binding rules at spec.md:35 (label measured/derived/hypothesis) and spec.md:39 (nothing discarded without a number or a non-expiring argument).
did: `1brc/06-cross-disciplinary-transfer.md` + its data companion. Six candidates, each landing in one of the three states the ticket allows: C1 morsel-driven parallelism -> H-11, C2 vectorized execution -> H-12 (its tokenizer half is `go-v2-kernels`, measured in this same cycle), C3 k-mer hashing -> two KILLED-on-argument and one -> H-13, C4 perfect hashing -> P-03, C5 poll-mode I/O -> one half KILLED with its number and the other -> H-14, C6 Metal -> P-04. Queue items 11-14 appended to `07-experiment-ledger.md`; P-03 and P-04 written into `1brc/PARKED.md` with all seven fields.
did: also `CORRECTIONS.md` C4 — `unsafe.Sizeof(entry{})` is 48, not the 32 published at six sites, so a 1<<17 array is 6.00 MiB not 4 MiB and H5's ratio is 6x not 4x. Corrected inline at each site; no verdict moves.
deviations: (1) The report's mechanism descriptions come from prior reading, NOT from primary text fetched this session — only two Crossref records and the HTTP status of every cited URL were fetched. Every mechanism claim carries `[unfetched]` at its site and no NUMBER is taken from any of them; the companion says so in §3. That is weaker than `spec.md:44`'s standard for `01-definition.md` and it is labelled rather than hidden. (2) The report FIRST claimed the Super/Performance core split was unrecorded. It is recorded at `01-definition.md:91`, `02-baseline.md:15` and `03-technique-recon.md:25`. Caught while writing E-11, corrected at four sites plus the ledger's H-11 in `b13caec`; what is actually new is the L2 asymmetry (16 MiB against 8 MiB) and that `02-baseline.md:15`'s "a uniform 15-way split is close to optimal" is an untested hypothesis the code treats as a decision. (3) H-11's prediction depends on `r`, the throughput ratio between the two core classes, which is NOT measured — the entry says measure `r` first and the arithmetic is tabulated for r = 1.0 to 2.0 rather than asserted at one value. (4) No new timing was taken for this ticket at all; every number is quoted from an earlier companion or derived in §4 of this one. (5) The C5 "the reader alternates instead of overlapping" claim rests on the SHAPE of `foldRange`'s loop, not on a timing, because the ledger's 0.988 s of compute is defined as 1.742 - 0.754 and adding it back would be circular. The report names that trap explicitly.
verified: docs-only for the report itself; the one code change is a comment in `table.go`, so `cd 1brc/code/go && test -z "$(gofmt -l .)" && go vet ./... && go build -o bin/1brc . && go test ./...` and `bash 1brc/scripts/check-correctness.sh` both exit 0. Green covers nothing about the report's content: a transfer report has no gate, which is why the deviations above are the audit surface.
threads: H-12's interesting half and H-13's both need a 1b 10,000-station file (queue item 8, ~20 s of generation) and neither can be ranked without it. P-04's revive trigger is quantitative (the Go path within ~15% of the 754 ms floor) and nothing checks it automatically. `1brc/PARKED.md:5` still says the ledger is "not yet written; `go-v1-parallel` creates it" — stale, left alone as another group's line.

### go-v2-kernels (4e78d53 build, b13caec measurement)
meant: backlog.md:15 — integrate the asm-kernels winners, Go Plan 9 asm or SWAR-in-Go where the call overhead kills the asm win, measure BOTH, ledger every swap. Also `07-experiment-ledger.md` queue item 2, "the single most load-bearing untested assumption in the study".
did: `1brc/code/go/batch.go` — `foldBatchSWAR` and `foldBatchNEON`, both draining a window's `;` and `\n` syndromes in address order into `foldBatchRow`, behind `-kernel row | batch-swar | batch-neon`. `table.fold` now dispatches once per buffer and the old body is split into `foldRows` + `foldTail`, which all three kernels share. `code/asm` became a real dependency of the binary (`require` + `replace ../asm`) and `neonDelimMask32` is exported as `asm.DelimMask32`, so the binary and the asm module's correctness tests exercise ONE copy of the assembly.
did: measured, one invocation, 1b: `row` 1.684 s, `batch-swar` 1.849 s (+9.8%), `batch-neon` 1.859 s (+10.4%), disjoint. KILLED-on-numbers, default unchanged. `bench/2026-09-01T160211Z-v2-kernels.txt`; ledger E-10 and E-11; `04-asm-kernels.md`'s hands-forward row annotated; `PARKED.md` P-01 and P-02 both updated because E-10 fired one of their triggers and spent the other.
deviations: (1) NEITHER batch arm materialises a token stream. `04-asm-kernels.md` benchmarked `TokenizeBatch` writing 12 bytes per row into an out array; folding straight into the table is less memory traffic, so the end-to-end arm is a BETTER shape than the benchmarked one rather than a faithful port. That makes the -40.4% -> +10.4% swing an understatement of the gap, not an artifact of a worse port. (2) `foldBatchSWAR` uses `zeroByteMask`, an exact zero-byte mask, not `indexDelim`'s cheaper borrow form: the cheap mask can set a lane above a real match, which is harmless when only the lowest bit is read and is a wrong row boundary when every bit is drained. Two extra ops per word on the arm that lost anyway. (3) One check was DELETED, not added: `pendingSep+next != end` survived every mutant because `validTemp` already implies it. (4) `-parse scalar` with a batch kernel is now a hard error rather than a silently ignored flag. (5) The batch loops are duplicated rather than parameterised by a mask function, because an indirect call per window would have charged the SWAR arm for a call the whole ticket exists to price. (6) PROVISIONAL: battery, 38%, load 2.08, per spec.md:42. (7) The `row` arm measured 1.684 s here against v1's published 1.742 s in its own invocation; the two are never subtracted, per the ledger's rule, and the refactor into `foldRows`/`foldTail` adds one non-inlined call per BUFFER, not per row.
verified: three gates green at every commit — `cd 1brc/code/go && test -z "$(gofmt -l .)" && go vet ./... && go build -o bin/1brc . && go test ./...`, `cd 1brc/code/asm && make test`, and `bash 1brc/scripts/check-correctness.sh` (exit 0 on both 10m files). Additionally each kernel was byte-compared against `testdata/expected-10m.out` and `expected-10k-stations-10m.out` INDIVIDUALLY, because the gate script only runs the default. Ten mutants, all ten caught: the exact mask, both drains' first-`;` guard, the no-separator guard, `validTemp`, the over-read slack, the batch/tail handoff, the NEON lane shift, the `-parse scalar` rejection. Green does NOT cover: AC power, a 1b 10k-station file, or WHY the batch shape degrades four times as fast as the row shape between 100m and 1b (E-11 labels that a hypothesis).
threads: the no-separator guard survived its first two mutation rounds because a redundant sibling check masked it; deleting the sibling and adding a "bare temperature at the START of the buffer" case is what pinned it, and the same masking pattern will hide any guard whose only test asserts "an error came back" rather than which error. `04-asm-kernels.md`'s "two or four windows per transfer pair, untried" is now the only untried batch variant and E-10 records it as the revive trigger. The 100m file would have shipped `batch-neon` as a tie; it is the second time in this study that the 1b file inverted a 100m reading (E-09 was the first, seven arms).

### go-autoresearch-harness (42b5b3d, 4646edd)

meant: `spec.md:53` stage 6 — the optimization rounds run the autoresearch way (hypothesis queue → one experiment → measure → keep/kill → ledger), adapted from the repo's own prior art with karpathy/autoresearch secondary; the backlog line adds `1brc/scripts/experiment.sh`, a queue section in the ledger, the adaptation note in its header, and ≥10 seeded hypotheses.
did: `1brc/scripts/experiment.sh` (new) runs ONE experiment: `parse_args`/`validate` enforce four rules, `arm_command` builds the timed command from the same `ARM_FLAGS` entry the correctness gate checked, `run` gates every arm then times all of them in one hyperfine invocation and appends a ledger row pre-filled from `--export-markdown`; `self_test` is the runnable check. `check-correctness.sh` gains `ARM` so a variant's flags are byte-compared before they are timed — it previously only ever ran the default binary. `lib-provenance.sh` (new) holds the power/load/commit block, sourced by both `bench.sh` and `experiment.sh`. `07-experiment-ledger.md` gains "The loop this ledger runs" (header) and "The queue board" (after item 14); `CORRECTIONS.md` gains C5.
deviations: (1) The ticket says "seed >=10 hypotheses" and the queue was ALREADY seeded with fourteen by `go-v1-parallel` and `x-transfer`; this group added none and boarded them instead. Thirteen are open (item 2 is E-10's kill), so the floor is met, but the seeding is not this group's work and the ledger says so. (2) `bench.sh` and `check-correctness.sh` belong to `go-skeleton`, and this group edited both. The `ARM` variable is load-bearing — without it no experiment arm is ever correctness-checked, which is `spec.md:36` — and `lib-provenance.sh` exists so two scripts cannot publish two different provenance formats for numbers in the same study. (3) C5 corrects `f786d4e` and `5528b58`, i.e. two other groups' published claims, because `spec.md:41` makes correction site-wide and mandatory. The `docs/1brc/handoff.md` site is corrected HERE rather than by editing the historical block, following C4's precedent: **the ~20 s figure in the `x-transfer` threads line above (`handoff.md:80`) is wrong; a 1b 10k-station file is 57.09 GB and ~79 s, and the equal-bytes 241.6M-row file is what H-12, H-13 and items 9/10 actually want.** (4) No delegation: the deliverable is a judgement call about which of two methodologies' rules survive contact with this study, and the whole group is three files — writing a router plan would have cost more than it saved. (5) An `EXIT`-trap `unbound variable` bug in the first draft was found by running the harness rather than by reading it; fixed before the first commit, which is why the smoke file in `1brc/bench/` is stamped `2026-09-01T161727Z` and not the earlier run's timestamp. (6) `1brc/bench/2026-09-01T161727Z-harness-smoke.txt` is committed and is NOT a result — it is 10m, `--mechanism-only`, and the script stamped `NOT A VERDICT` on it itself.
verified: `bash 1brc/scripts/experiment.sh --self-test` (14 checks, green), `cd 1brc/code/go && test -z "$(gofmt -l .)" && go vet ./... && go build -o bin/1brc . && go test ./...` and `bash 1brc/scripts/check-correctness.sh` green at both commits. Three behaviours were proved by running rather than by reading: `ARM='-nosuchflag'` makes the correctness gate exit 2 (so `ARM` reaches the binary and is not dropped); an arm that fails the byte-compare aborts the experiment with hyperfine never invoked and no bench file written; and `bench.sh` still emits a byte-compatible header after the provenance refactor. Raw output in `07-experiment-ledger-data.txt` §2-4. Green does NOT cover: any 1b run (this group measured nothing at 1b and claims no timing), AC power, or whether the boarded predictions are any good.
threads: `experiment.sh` writes the ledger row to the bench file and prints the path; nothing appends it to `07-experiment-ledger.md` automatically, and that is deliberate — the verdict is the human step — but it means a round can measure and forget to ledger. Queue items 6 and 7 are runnable with no new code and are the cheapest two rows in the study; item 11 needs `r` measured before its own prediction can be evaluated at all. `1brc/PARKED.md:5` still says the ledger is "not yet written" — stale, still another group's line, still left alone.
