# Built ledger — 1brc

One line per shipped ticket, appended by the builder: `- <id> <sha> — summary`.
- def-understand 4145227 — 01-definition.md written from gunnarmorling/1brc@db06419 with cited rules, derived output arithmetic, and the integer-tenths core it requires
- env-data d22e3dc — seeded generator (413 + synthetic 10k key sets), 10m/100m/1b/10k-stations files generated outside the repo with recorded rows+bytes+sha256, trivially-correct reference and its committed expected outputs
- env-baseline d5d62fb — physical floor measured: 754.4 ms +/- 8.8 ms to read 13.8 GB (15 uncached preads), mmap 5-9x slower than read(), page cache slower than F_NOCACHE; also closed `asm-recon` 61dcbb6 (top-five technique inventory, six hypotheses, NEON movemask and merykitty's parse both verified in Go) and `go-skeleton` ddefd8f (1brc/code/go module, byte-compare gate green on 413 and 10k stations, hyperfine harness, 26.1 ns/row)
- asm-kernels b4be167 — four tokenizer shapes measured on arm64: 8-byte SWAR beats a per-row 16-byte NEON scan (H2 falsified, 16.3-23.1%, cause measured as a 1.080 ns/row vector-to-general transfer), gigatoken's batch shape wins biggest (-40.4% / -40.7% on the official key set across two post-fix runs; the -42.3% first published measured a kernel the review gate then repaired, see CORRECTIONS.md C3), H3 splits by input so both parses survive behind a flag, H6 shift beats the lookup table 22.6% single / 11.7% interleaved; every verdict-bearing pair disjoint over ten runs, PARKED.md created with P-01 and P-02
- skill-performance-assembly 1c3fa1e — global `performance-assembly` skill written at the DoD path (165 lines, employer-agnostic): the arm64 vector-to-general transfer model with its measured floor, both movemask idioms as verified Plan 9 asm, branchless-parse-as-misprediction-insurance, the seven microbench rules that made this study's numbers usable, the PROT_NONE over-read test, and darwin's page-cache/mmap behaviour; artifact lives outside the repo by design, so this id's repo diff is its ledger
- go-recon 600e9dc — `05-go-techniques.md` + 853-line data companion from six probes: a prefix-hashed open-addressing table beats a Go map by 15.8% on the official key set and LOSES by 3.7% on the 10k stressor (Go map stays behind a flag), H4 answered outright (16-byte prefixes separate both key sets, 8 bytes leave one collision in 413), full-name FNV is 2.44x slower than a prefix hash on long names, `unsafe` is worth only 4-16% on the scan and reslicing gets half of it in safe Go, zero GC collections in a v1-shaped loop, a Plan 9 asm call costs 1.93 ns so assembly may only be called per chunk, and the per-CORE budget is 15.0 ns/row rather than 1.0; LICENSES.md gains the three implementations read with licenses verified via the API
- go-v1-parallel f786d4e — v1 lands at **1.742 s ± 0.019 s on 1,000,000,000 rows** (161.0 ms at 100m, 27.8 ms at 10m; 16.2x the skeleton at 100m, 1.74x over target): one F_NOCACHE pread reader per core over disjoint byte ranges, each folding into its own prefix-hashed linear-probe table, merged once; `07-experiment-ledger.md` opened with nine rows — H1 cursor KILLED (21% slower, opposite direction to its prediction), H7 mmap KILLED (5.6x slower end-to-end and MADV_WILLNEED makes it worse), H5 killed for 413 and parked for 10k, H3 branchless KEPT (wins 11.4% at 1b having lost 15.2% in the microbenchmark), and E-09: seven arms, seven disagreements between the 100m and 1b files, so no verdict may come from 100m; also `39990ad` (a full table errors instead of probing forever) and `089924b` (the ledger)
- x-transfer 5528b58 — `06-cross-disciplinary-transfer.md`: six mechanisms borrowed from databases, bioinformatics, networking, compilers and Metal, each landing as a queued hypothesis (H-11 core-class-weighted split, H-12 vectorized probe, H-13 quotiented 32-byte entry, H-14 double-buffered workers), a non-expiring kill (rolling hashes, minimizers, and syscall batching with its number: 3,289 reads against a 754 ms floor) or a seven-field park (P-03 learned perfect hashing, P-04 Metal, unkillable because its ceiling is the read floor and that clears the 1.0 s target); the pass's own findings are that `foldRange` alternates read-then-fold instead of overlapping and that `02-baseline.md:15`'s "a uniform 15-way split is close to optimal" is an untested hypothesis; also `CORRECTIONS.md` C4, the table entry measured at 48 bytes against the 32 published at six sites
- go-v2-kernels 4e78d53 — the batch tokenizer integrated both ways (`-kernel batch-swar` pure Go, `-kernel batch-neon` calling `asm.DelimMask32`, `code/asm` now a real dependency so one copy of the assembly is both shipped and tested) and then **KILLED end to end**: one invocation at 1b gives row **1.684 s**, batch-swar **1.849 s (+9.8%)**, batch-neon **1.859 s (+10.4%)**, disjoint, with user CPU confirming the batch arms execute more work; `04-asm-kernels.md`'s −40.4% was a true measurement against a baseline this binary never had, which is E-11's rule and E-09's twin; ten mutants all caught, one redundant check deleted; measurement and consequences in `b13caec` (04's hands-forward row annotated, P-01's trigger fired and re-read, P-02's only trigger spent)
- go-autoresearch-harness 42b5b3d — the autoresearch loop adapted into `1brc/scripts/experiment.sh`: N named arms of one binary, **each byte-compared with its own flags before any of them is timed** (`check-correctness.sh` gained `ARM`; it had only ever checked the default binary), then all arms in ONE hyperfine invocation, written with the shared provenance block (`lib-provenance.sh`, now also `bench.sh`'s) and a ledger row pre-filled from `--export-markdown`; four rules are refusals with teeth — a prediction must carry a number or be labelled `sweep:` (E-06), two arms minimum, unique arm names, and any file but 1b needs `--mechanism-only` and gets stamped `NOT A VERDICT` (E-09) — all 14 covered by `--self-test`, plus a proof by running that a failing arm aborts with hyperfine never invoked; `4646edd` writes the adaptation note into the ledger header (what transplants from karpathy/autoresearch and this repo's own AUTORESEARCH/METHODOLOGY/07-method-what-worked/quantization prior art, and the two things that deliberately do not: automatic keep/discard, and iterating on the 100m proxy), boards the fourteen already-seeded hypotheses with state, cost and copy-pasteable commands for the two that need no new code, and adds `CORRECTIONS.md` C5 — a 1b 10k-station file is **57.09 GB and ~79 s**, not the published ~20 s, and at 2.22× RAM with a 3.12 s read floor it answers a different question than the three queue items blocked on it are asking
- go-opt-round-1 4498b8b — the autoresearch round measured its own harness: queue items 6 and 7 each won in their own invocation (`-workers 20` −10.02%, `-buf 8192` −8.64%), then the combination run scored `-buf 8192` at **+12.01%**, reversing the arm order swapped the winner and the loser, and a null experiment of **eight IDENTICAL arms rose monotonically 1.660 → 2.010 s (+21.08%)** with user CPU +16.65% for identical work; the fix is `experiment.sh -c`, a 20 s `--prepare` sleep before every timed run (default at 1b, `NOT SLOT-CORRECTED` stamped without it) plus the incumbent named first AND last, after which the two bracket arms agree to 2.54% and user CPU to 0.69% (`ff99119`); under it item 6 is KEPT (**−7.49%**, disjoint, default now `NumCPU()*4/3` = 20 here, pinned by `TestTheDefaultOversubscribesTheCores`) and item 7 KILLED (+3.05%), giving a new best **1.613 s ± 0.047 s at 1,000,000,000 rows**, PROVISIONAL on battery; `d879b70` prices the damage — invocation B, the source of six earlier rows, carries the same signature (+14.53% user CPU across its slots), so E-03's kill falls to 0.94% and E-06/E-07/E-08 are marked NOT DEFENSIBLE with a re-measure queued as item 15, and `CORRECTIONS.md` C6 retires "one invocation makes arms comparable" at every site that published it
- go-opt-round-2-profile 8901488 — the round's profile half, after machine state voided two attempts at it: an orphaned item-15 invocation at PPID 1 collided with four new 1b runs and then the AC-power housekeeping storm (`backupd`, Spotlight, a WorkflowKit burst, load **12.79**) tripled the same binary to 4.54-5.06 s, so `lib-provenance.sh` stopped merely recording the machine's state and started gating on it (`ae2b753`: an exclusive lock that refuses a second measurement with status 3 and clears a dead owner's lock, `require_quiet` waiting 15 s x 12 then refusing with status 4, `NOT QUIET` stamped on a forced run, 13 mutants all caught, and a proof by running that a held lock stops `experiment.sh` before hyperfine); then the measurement itself on AC at load 4.20 with the preflight green, the study's first non-provisional number, answering queue item 3: the idle quarter of the machine is the **read** (29-30% of worker wall blocked in `pread`, 5.75 of 20 workers at any instant), NOT shard skew (1.029-1.038) and NOT the merge (0.34% of wall), and the finding that outranks it - **user CPU 18.53 s over 15 cores is a 1.235 s compute floor ABOVE the 1.000 s target**, with the 0.754 s read floor below it, so the binary is compute-bound at 1b and 19.1% of CPU has to go; the profile says where (`indexDelim` 33.5%, `validTemp` 18.2%, the `pread` syscall 15.7%, `(*table).update` 15.9%, parse 5.8%, `memequal` 5.8% and per-row `(*table).merge` 1.7% nested inside `update` rather than added to it, pprof's own total 24.6% short of measured user CPU so its shares rank and its seconds do not), which closes item 3 and item 10-at-413, prices item 4 at 18.2% against a predicted 3-8% (`CORRECTIONS.md` C7) and boards **item 16** in its place (a cheaper way to establish the SAME format rejection, not a range test standing in for it: the review gate caught that the range test already exists as `inRange` on the same row, so the obvious version would have accepted malformed input no generated corpus can produce), subsumes H-11 into `PARKED.md` P-05 with its revive trigger, re-derives H-14's ceiling at **17.6%** at all three sites that published 25%, and re-orders the whole board by measured CPU share because a mechanism that removes no CPU cannot reach a target below the compute floor (`028f640`)
- go-opt-round-2-item15 2c389c5 — the owed re-measure, which took FOUR invocations to produce two readable ones: one (`dcf8572`) aborted when its cooldown `sleep` died with the cycle that owned it, one timed everything and **failed its own bracket at 27.37%** with user CPU flat and unordered and the whole excess in system time (E-22, void — not E-16 drift, and no per-slot constant describes it), and the two that count agree to 0.27%; against a 0.62% bracket **five of invocation B's six verdicts do not reproduce** — a 21.00% kill (E-02 cursor) lands at +0.06%, an 11.40% keep (E-05 scalar) at −1.12% to −1.73%, split at +1.49%, bits14 at −0.44%, pagecache at +0.19%, all overlapping — and the sixth REVERSES: `-buf 1024` is −6.34% disjoint, swept in E-24 to a true minimum (512 KiB +3.97%, 2 MiB +4.44%, 4 MiB +6.56% against it, 1 MiB disjoint from every arm), so `defaultBufKiB` is now 1024 (`2beb9e1`, pinned by `TestTheDefaultReadBufferIsTheSweptMinimum`) and the study's best is **1.510 s ± 0.005 s at 1,000,000,000 rows on AC with the preflight green, no longer provisional** (12.32 of 15 cores busy, 82.1% parallel efficiency, 49.9% of the wall now the read floor); `CORRECTIONS.md` C8 retires the six margins AND E-18's derived repair of them, which predicted cursor at +19.18% and measured +0.06% — a contaminated invocation carries no information about its own contamination, so it is re-run, never repaired by arithmetic; E-02, E-03 and E-05 are re-opened rather than re-decided, and `F_NOCACHE` and 1<<17 keep their defaults on other evidence
- go-opt-round-2-item16 bde92b1 — board item 16 shipped and measured: `parseTempWord` folds `validTemp`'s whole format rejection into the 8-byte word `parseTempBranchless` already loads (the `.` and `\n` at fixed offsets from the dot lane, a SWAR digit test on the lanes bounding the digit run, the sign byte, and the digit COUNT, the only guard that rejects `100.0`), one accumulator and no branch inside the check — the `dot > 28` guard stays and is never taken on well-formed input, what left is the UNPREDICTABLE three-way switch on `next` — contract unchanged because `inRange` and the byte-level establishment both stay; E-25 measures it at **1.424 s ± 0.009 s at 1,000,000,000 rows** against 1.498 s and 1.499 s for the two incumbent bracket slots — **−4.97% of wall, −6.33% of user CPU, disjoint, bracket spread 0.067%, on AC with the preflight green and not provisional** — which makes it the first arm on the board to remove CPU rather than buy parallel efficiency (which FELL, 82.1% → 80.9%, because the unchanged 754 ms read floor now hides under less compute) and takes the compute floor 1.235 s → **1.152 s**, still above the 1.000 s target with 152 ms per core left to find; the pre-registered 8% keep bar was MISSED and the arm kept on stated reasoning, the prediction's "a quarter of the cost" mechanism is falsified at **65.2%**, `defaultParse` is now `"word"` (`a7e61dc`, pinned by `TestTheDefaultParseFoldsTheCheckIntoTheWord`), the batch kernels now need `-parse branchless` explicitly, and correctness rests on an exhaustive differential sweep of 2,000,000 six-byte fields against both the incumbent pair and the independent `parseTempScalar` plus ten mutants all caught, one per digit-lane mask
- go-opt-round-2-items1and5 080f332 — board items 1 and 5 built as three arms behind one `-fold` flag and measured in one invocation (E-27, bracket 0.072%): **`-fold ptr` is 1.233 s ± 0.010 s at 1,000,000,000 rows** against 1.398 s and 1.399 s for the two incumbent slots — **−11.80% / −11.87% of wall, −13.49% / −13.67% of user CPU, disjoint from both** — and is now the default (`dd70725`, pinned by `TestTheDefaultFoldIsThePointerWalk`); **the compute floor goes 1.152 s → 0.9812 s, 18.8 ms UNDER the 1.000 s target for the first time in the study**, per-row CPU 14.72 ns against the 15.00 ns budget, parallel efficiency 80.9% → 79.6%, so the board's ordering key inverts from CPU per row to wall clock recoverable from scheduling and item 14 becomes the only item that can reach the goal (ceiling re-derived to 20.4% landing at 0.9812 s at all four sites); **item 1 is CLOSED as not-adopted** — isolated exactly by `both` against `ptr`, the same function and one bool, at +0.81% of wall against −0.11% of user CPU with overlapping ranges and the two channels disagreeing in sign, which confirms both its own original prediction and E-26's 1.07-1.72% ceiling; **item 5 is CLOSED as kept** and its "half reachable in safe Go" clause measures 40.6% (4.79 of 11.80 points on the slice walk); the prediction was falsified upward by 3x on every arm because most of the win is the rewritten separator scan that item 1's arm needed and no board item named, an attribution left DERIVED rather than measured; en route the re-profile the ticket demanded falsified the board's own headline share (E-26, `CORRECTIONS.md` C9: `indexDelim` 35.9% → 17.73-20.36% of samples, because E-24's buffer change moved the syscall bucket 15.7% → 54-57%, and the ordering key becomes share of NON-syscall samples where it is 40.9-44.3%); 12 valid mutants counted rather than remembered, 11 caught, three survivors closed by two test additions (the bucket-count assertion kills both hash-mask mutants, a 200-byte name at a chunk's end kills the slice arm's guard mutant) and the last unkillable and measured rather than argued; three further mutants were discarded as equivalent and three failed to apply and were re-run
- go-opt-round-2-item13 ba1c6e7 — board item 13 built as `-table quot` and measured in one invocation (E-28, bracket 0.98%): H-13's quotiented 32-byte bucket — the name's first eight bytes inline (`qentry.word`, masked exactly as the hash masks them), the full names in a dense side array reached only for the 34.1% of rows whose names exceed eight bytes, `nlen` = len+1 so a zeroed bucket reads as empty and a NUL-padded name cannot alias a shorter one, min/max narrowed to int16 to buy the 32 bytes with two blank constant conversions failing the build if `inRange`'s window ever outgrows them — and it **LOSES: 1.301 s at 1,000,000,000 rows against 1.229 s and 1.241 s for the two incumbent slots, +5.86% / +4.83% of wall and +6.98% / +6.80% of user CPU, disjoint from both and in the wrong direction against a prediction of 0-4% faster**, so item 13 is **CLOSED, KILLED-on-numbers** and `-table quot` stays only as the arm it was measured on; the sharper statement is the floor — 15.684 s of user CPU over 15 cores is a **1.0456 s compute floor, back ABOVE the 1.000 s target** E-27 had taken it 18.8 ms under; the mechanism is DERIVED and the row says so, no profile was taken: at 413 stations in 131,072 buckets the load factor is 0.32%, so a probe touches one cache line whatever the bucket size and 48 → 32 bytes removes none, while the side-array indirection is paid for real; with E-03's −4.6% that is **two independent losses, so bucket layout is a closed neighbourhood at 413 stations** and `PARKED.md` P-03's third revive trigger ("or H-13 wins") is marked fired-negative; the invocation's two incumbent slots also reproduce E-27's headline ACROSS invocations at −0.32% / +0.65% with user CPU within 0.39%, which is the control that says the arm's refactor (the masked word carried into `update`, one layout dispatch branch, `hashWord` split into inlined `maskWord`+`mixWord`) costs the incumbent nothing measurable; `-table` is validated now rather than falling back to the incumbent on an unknown string, which would have published the incumbent's time under an arm's name
- go-opt-round-2-item14 6936b9a — board item 14 built as `-fill off | sync | ahead` (`foldRangeFill`: fixed-size reads at fixed offsets into a buffer reserving a chunk at the front for the carry, `ahead` running them in a per-worker goroutine, `sync` running the identical loop inline so the pair differs by that goroutine alone) and **KILLED-on-numbers in two bracketed invocations**: E-29 (8 arms, bracket 0.989%) gives `ahead` **1.232 s against 1.225 s and 1.213 s**, +0.57%/+1.57% of wall with the ranges covering both slots and user CPU flat at −0.08%/+0.04%, and E-30 (4 arms, bracket 0.905%, run after the repair below) gives **1.225 s against 1.215 s and 1.226 s**, +0.82%/−0.08%; the isolating pair measures the overlap itself at +0.24% and −0.81% in the two invocations, both inside their brackets, so **the mechanism did not underperform, it did nothing**, and parallel efficiency — predicted to go 79.6% toward 95% — measured 79.3-80.2% against incumbents at 79.8-80.5%; the ceiling was never bad arithmetic but the assumption under it was, because E-20's "29-30% of worker wall blocked in `pread`" is per-worker wall on a machine running 20 workers on 15 cores and E-17 bought that oversubscription precisely to cover one worker's read with another's fold, so the overlap was already happening at saturation (`CORRECTIONS.md` C10 retires the 25%/17.6%/19.1%/20.4% chain and the "only item that can reach the target" verdict at all five sites, and board item 17 records the `-fill` x `-workers` 2x2 that would test the substitution reading directly); the `-buf` re-measure the ticket demanded was run and **E-24's 1 MiB held** (512 KiB +5.76%, 4 MiB +6.57% against it, disjoint, 2 MiB indeterminate at σ 0.050 s); the review gate then found a defect in the arm this cycle had just shipped — a `maxRow`-sized carry prefix made the staged reader reject rows between `maxRow` and the buffer size ALIGNMENT-DEPENDENTLY, the same file passing at one `-workers` and failing at another — repaired in `6936b9a` with a chunk-sized prefix, the previous buffer released only after its carry is copied out of it, and one `IndexByte` per chunk rejecting an over-long row deterministically, pinned by `TestTheReadersAgreeOnOverlongRows` whose last two row lengths are the split itself (exactly `chunk`, which every reader must accept, and `chunk+1`, which every reader must reject); the gate gains `go test -race` because the mutant that releases a buffer before its carry is copied passes every byte-compare in the suite and only the race detector catches it, and of 15 mutants 13 are caught with the two survivors proved EQUIVALENT rather than scored; also `92669cb` (the reader and the 449-subtest boundary sweep) and `17b7c31` (E-29, C10 and the five corrected sites)
