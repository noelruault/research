# 1brc-perf backlog — append-only ids, priority order

Ids are stable and append-only; never renumber or delete. `[dN]` = difficulty. `HUGE` = dispatched alone. `(needs: `id`)` gates dispatch. Every timed ticket runs through `1brc/scripts/measure-when-idle.sh` — see spec.md.

- [ ] `lanes-requiet` HUGE — re-measure `-fold lanes` on a genuinely idle machine at `-r 10`, incumbent first AND last, and settle the wall clock E-37 could not. E-37 measured user CPU −5.28% (control bracket 0.034%) and wall −2.28% with overlapping ranges, on a machine where WebKit held 71-97% of a core. Prediction to pre-register: if the wall win is real it lands 2-5% and disjoint; if it was noise the arms overlap again at `-r 10`. Ledger the row either way; promote `lanes` to the `-fold` default ONLY on a disjoint wall win. [d3] PARTLY MEASURED, promotion still open. E-38 (`857911b`, ledger:492) answered the mechanism on a BUSY machine: wall -3.57% inside the predicted 2-5% band against a 0.000% incumbent bracket, user CPU -5.16% reproducing E-37's -5.28%, and NO promotion because the ranges are not disjoint (`lanes` max 1.309 s vs incumbent minima 1.227/1.220 s). What is still owed is the disjointness test on a QUIET machine, which is the only thing that can move the `-fold` default. IN FLIGHT since 2026-09-04T213210Z: a detached invocation (pid 68175, own process group, log `1brc/bench/2026-09-04T213210Z-lanes-requiet-detached.log`) passed all 12 correctness cases including the 1b byte-compare for both arms and is parked in `require_quiet` waiting for 600 s of operator idle at load under 4.2, with an 8 h budget. A later cycle must POLL that log and the newest `1brc/bench/*-lanes-requiet.txt` for the `Summary` block, NOT launch a second run: check `/tmp/1brc-measure.lock/pid` first. Close this ticket only when a quiet-machine bracket has been ledgered as its own row; do NOT close it on E-38, whose own verdict defers promotion.
- [ ] `lanes-stacked` HUGE — measure `-fold lanes` stacked with more workers, since lanes cuts compute and therefore ENLARGES the idle share P-06 attacks: arms `default`, `-fold lanes`, `-fold lanes -workers 24`, `-fold lanes -workers 28`, incumbent bracketed. E-37 took wall-above-floor from 33.7% to 37.9%, so the two mechanisms are predicted to be complements rather than substitutes (contrast E-31, where fill-ahead and oversubscription proved substitutes). State the prediction as a number before running. (needs: `lanes-requiet`) [d4]
- [ ] `lanes-four` — extend `foldRowsLanes` from two cursors to four behind `-fold lanes4`, gate it (including `ARM` correctness and a mutated differential test), and measure 1 vs 2 vs 4 cursors in one bracketed invocation. Prediction: if two cursors bought 5.28% of CPU by filling stalls, four buys less than double and may lose to register pressure and cache pressure on four live rows. A measured plateau is the useful answer here, not a win. (needs: `lanes-requiet`) [d4]
- [ ] `gigatoken-license` — add the one-line `1brc/LICENSES.md` entry for marcelroed/gigatoken: studied for the tokenization angle (`03-technique-recon.md:5,78`), contributed the batch SHAPE that became `TokenizeBatch`, nothing copied and the module sources were never fetchable, so no derivation to attribute. The file lists other studied implementations and this omission is an inconsistency, not a licence risk. Docs-only, no gate. [d1]
- [ ] `p06-workers` HUGE — `PARKED.md` P-06, queue item 18: sweep `-workers` above 20 on an idle machine, using the arm and keep-rule P-06 already quotes rather than a new one. Its stated ceiling is perfect packing at 1.0740 s, still +7.40% over target, so this is a candidate for the gap and never for the goal — say that in the ledger row whatever it measures. (needs: `lanes-stacked`) [d4]
- [ ] `final-perf` — verify every DoD item in spec.md; confirm the ledger, `09-result.md` and `README.md` agree at every site that publishes a number; park anything unmeasured in `1brc/PARKED.md` with all seven fields. Only this ticket may emit "backlog empty". (needs: `p06-workers`) [d3]
- [ ] `stdlib-contribution-survey` — answer, with evidence, whether anything this study measured is worth proposing UPSTREAM to Go itself, and write `1brc/10-upstream-candidates.md`. This is a survey ticket: the deliverable is a ranked list with a verdict per candidate, and "nothing qualifies" is a valid and expected outcome. Method: for each candidate, (a) state the measurement that motivates it and the machine it was taken on, (b) search the Go issue tracker and `golang-dev` for prior art before writing anything — most of these have been proposed and rejected before, and the rejection reason is the finding, (c) check whether it is a *stdlib* change at all or just user code we happened to write, and (d) apply the honest test: would this help programs other than a 1BRC entry? Candidates the study actually generated, none of them pre-judged: darwin `F_NOCACHE` has no exposure in `os`/`syscall` beyond a raw `fcntl` constant, and 02-baseline measured it beating the page cache 754 ms vs 1.126 s on a file at 53.5% of RAM, so a documented way to request uncached reads is a real gap — but check `x/sys/unix` first, and check whether the Go team considers this a `golang.org/x/sys` matter rather than stdlib. `bufio.Scanner`'s allocation and copy behaviour on multi-GB single-line-per-record input against what a hand-rolled reader achieved. Whether anything in `05-go-techniques.md`'s bounds-check-elimination findings is a compiler issue worth a reproducer rather than a stdlib change. Whether the `GOEXPERIMENT=simd` arm64 gap this study hit (mask-to-general-register `ToBits()` present on amd64, absent on arm64 — probed and pinned in `07-experiment-ledger-data.txt` §U) is already tracked upstream, and if not, whether a minimal reproducer is worth filing: that one is the strongest candidate because it is a concrete portability asymmetry in an experimental package, not a performance opinion. Every entry ends KILLED (with the reason and any upstream issue number), PARKED (seven fields), or PROPOSE (with the reproducer written and the issue text drafted, but NOTHING filed — filing is the human's call). [d4]
- [ ] `idle-gate-sigpipe` — added mid-cycle as a member of `lanes-requiet`, because it blocks every timed ticket here. `hid_idle` in `1brc/scripts/lib-provenance.sh` piped ioreg's 438,837 B into an `awk … exit`, so the early close SIGPIPE'd ioreg and the pipeline returned 141 under `set -euo pipefail` — measured 296/300 calls — which killed two unattended `lanes-requiet` waits at `require_quiet` rather than letting them wait. Fix: let awk drain and print at END. Pinned by a 40-call case in `experiment.sh --self-test`. [d2]
- [ ] `busiest-stamp` — added mid-cycle as a member of `lanes-requiet`, because the operator's 2026-09-04 override (`measure-when-idle.sh` now `IDLE_MIN=0 QUIET_LOAD=6.0 QUIET_FORCE=1`) means a run happens on whatever machine it finds, and `busiest` was printed only on the `NOT QUIET` path. At load 4.50 — under the 6.0 line, so unstamped — three Microsoft Defender daemons held ~150% of a core, which is E-37's exact confound arriving unrecorded. Fix: `provenance_header` in `1brc/scripts/lib-provenance.sh` stamps `busiest:` on EVERY header; the `NOT QUIET` line drops its now-duplicated copy. Pinned by a `QUIET_LOAD=999` case in `experiment.sh --self-test`, mutated to prove it fails. [d2]
