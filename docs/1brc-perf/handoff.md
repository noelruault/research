### idle-gate-sigpipe (9e2fbcc)
meant: not a spec ticket — spec.md:13 makes `measure-when-idle.sh` the only path to a timed run, and the wait it promises did not work unattended. Added as a member of `lanes-requiet` mid-cycle.
did: `1brc/scripts/lib-provenance.sh:hid_idle` — awk no longer `exit`s on the match, it sets `seen` and prints at END, so ioreg's 438,837 B are drained and never SIGPIPE. `1brc/scripts/experiment.sh:self_test` gains a 40-call case before the defaults check.
deviations: the diagnosis loop that produced 296/300 is not committed — it is reproduced in the commit body, and the self-test asserts the property rather than the rate. `busiest`/`load1` were checked for the same shape (0/50 each) and deliberately left alone.
verified: `experiment.sh --self-test` all checks passed; mutated back to `awk … exit` and it fails with `FAIL (status 141 on a call within 40)`. Go gate (gofmt/vet/build/test) and `check-correctness.sh` green, both unaffected by a shell-only change.
threads: `scripts/loop-next-selftest.sh` fails PRE-EXISTING on two ids (`skill-performance-assembly`, `x-transfer`) already in `docs/1brc/built.md` — the OLD loop's selector fixture, untouched here and not in spec.md:19-24's gate. `main.go:31`'s `-fold` usage string still reads `slice | hash | ptr | both` and omits `lanes`, which `reader.go:299` accepts — that is `lanes-four`'s group, left alone. A background task whose command exits takes the process group with it: the first attempt was launched as `nohup … & sleep 5` and died on task cleanup, so a wait this long must BE the task's own command.

### busiest-stamp (32e3e60)
meant: not a spec ticket either — spec.md:15 forbids publishing numbers from a busy machine, and after the operator's 2026-09-04 override (`measure-when-idle.sh:11-15`, `QUIET_FORCE=1`) the header became the only record of what the machine was doing. Added as a member of `lanes-requiet`.
did: `1brc/scripts/lib-provenance.sh:provenance_header` prints `busiest:` unconditionally instead of only inside the `NOT QUIET` branch, which also drops its now-duplicated copy. `1brc/scripts/experiment.sh:self_test` gains a `QUIET_LOAD=999` case.
deviations: the code landed in 32e3e60 during the previous cycle but was never ledgered — no `built.md` line, no handoff block, no review line — so this cycle closed the bookkeeping rather than the code. Nothing was re-implemented; the diff was audited and both self-test cases were mutated (see verified).
verified: `experiment.sh --self-test` all 7 checks passed. Mutated by deleting the `busiest:` echo → `FAIL: provenance_header named no busiest process on a quiet machine`; mutated `hid_idle` back to `awk … exit` → `FAIL (status 141 on a call within 40)`, so `idle-gate-sigpipe`'s test is real too. Go gate (gofmt/vet/build/test), `check-correctness.sh` and `ARM="-fold lanes" check-correctness.sh` all green — the last one because `lanes` is byte-compared before it is timed (spec.md:22).
threads: `busiest()` reads `ps -Ao pcpu,comm -r | sed -n '2,4p'` — a fixed top-3, so a fourth core-stealer is still invisible; adequate for a stamp, not for attribution. The header records the machine at the START of the invocation only, and a 12-minute bracket can be quiet at minute 0 and busy at minute 9 — that is the gap `lanes-requiet` still has to live with, and the reason this cycle opts the idle wait back on per-invocation rather than trusting one stamp.
