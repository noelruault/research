# Review log — 1brc

One line per group, written by the cycle that built it, after auditing its own diff. Read this before building: it is this codebase's memory of what it gets wrong.

- reviewed def-understand 4145227 + env-data d22e3dc: REPAIRED — four defects found by auditing the diff against the handoff. (1) `AppendTenths` assumed the whole part fitted in two digits and silently emitted garbage above it (`1000` tenths printed as `":0.0"`), while `ParseTenths` happily accepted `"500.0"`; replaced with `strconv.AppendInt` (also shorter) and added `TestAppendTenthsBeyondLegalRange`. (2) `Aggregate` accepted out-of-range temperatures and folded them into a plausible-looking result; it now fails with the line number, pinned by `TestAggregateRejectsOutOfRangeTemperatures`. (3) The `Tenths` doc comment justified the whole integer-grid design with a *derived* claim — "float64 drifts by ~1e-2 per station" — that measurement disproved by four orders of magnitude (actual drift 9.2e-07, and float64 recovers the exact sum). The design is right, the stated reason was not; the comment now cites `TestFloat64AccumulationDriftIsSmall`, which measures it. (4) `01-definition.md` cited `semantics_probe_test.go`, a file that does not exist (it is `tenths_test.go`) — a claim-vs-diff mismatch found exactly where the handoff protocol says to look. Also closed a contract gap the group created: `1brc/code/gen` had no green gate in `spec.md`, so the new code was ungated; added one, with `test -z "$(gofmt -l .)"` because `gofmt -l` exits 0 even when it lists files. Mutation-tested the suite over 17 mutants across both files: all 17 caught after the fixes, and the one survivor in the first round (a separator-less line that is itself a valid temperature, which panics if the check is removed) is now covered. Expected outputs re-derived after every fix and confirmed byte-identical, so none of the repairs moved the correctness target.
- reviewed env-baseline d5d62fb + asm-recon 61dcbb6 + go-skeleton ddefd8f: REPAIRED (ebbac7f) — five defects. (1) Mutation testing over the two new files caught 17 of 18 mutants; the survivor was `bytes.IndexByte` → `bytes.LastIndexByte` in the skeleton's separator scan, i.e. the first-vs-last-`;` divergence from `gen.Aggregate` that this same cycle had deliberately fixed and then left unguarded. A case with `;` inside the name pins it (first separator ⇒ both reject, last ⇒ they diverge) and the mutant is now caught. (2) `03-technique-recon.md` listed mmap under "techniques that do not survive the port" and `02-baseline.md` handed forward three I/O "constraints" — both violate the amended `spec.md:35`/`:37`, because every baseline number is a *scan* with no aggregation running and nothing may be discarded without an end-to-end measurement. mmap is now hypothesis H7 with `madvise(MADV_WILLNEED)` named as its untried rescue, and the constraints became a starting point plus three stated hypotheses. (3) No measurement recorded power or load (`spec.md:42`): the session was on BATTERY at load 4.29, so every 02-baseline figure is labelled provisional with the strategy *ordering* flagged as the trustworthy part, and `bench.sh` now captures `pmset`/`uptime` and stamps PROVISIONAL itself. (4) `CORRECTIONS.md` did not exist; created with the two due entries, C1 (the invented 88.6% name-length figure, measured 34.1%) and C2 (the "warm page cache" target, corrected at its site in `01-definition.md:91`). (5) Reconciliation, not a correction: the `go-skeleton` commit message quotes 265.6 ms ± 4.5 ms for 10m from a 3-run dirty-tree bench, while the authoritative figure is the committed bench file's clean 5-run 260.5 ms ± 1.5 ms (26.1 ns/row, linear against 2.607 s at 100m). Both are real runs; the bench file is the record. (6) Every sha this cycle recorded in built.md, handoff.md, review.md, CORRECTIONS.md and the bench-file header was orphaned by the mid-cycle rebase onto the operator amendment, so the ledger cited commits that no longer exist on the branch; all sites are repointed (env-baseline d5d62fb, asm-recon 61dcbb6, go-skeleton ddefd8f) and the bench header names the rewrite explicitly. Claim-vs-diff check on the handoff found no mismatches beyond these; every "verified" line's gate command was re-run and the 754.4 ms / 34.1% / 26.1 ns/row figures agree at every site that publishes them.
- reviewed asm-kernels b4be167 (repairs 730d166, 0956159): REPAIRED — five defects, one of them a correctness bug in the kernel the report recommends. (1) `TokenizeBatch` ended a name at the LAST `;` in a row while `TokenizeScalar`, `TokenizeStagedSWAR` and `TokenizeStagedNEON` all end it at the first, which is `gen.Aggregate`'s rule: on `Ab;cd;1.0` it reported a 5-byte name against the others' 2-byte one, so it would have attributed the row to a different station. Neither key set produces a name containing `;`, so `TestTokenizersMatchTheScalarReference` and `TestBatchBenchLoopCoversEveryRow` both passed over both corpora with the bug present — the benchmark was pricing a kernel that was not doing the job correctly. This is the THIRD appearance of the first-vs-last-`;` divergence in this study (lessons.md records it in `go-skeleton`, one group earlier). Fixed with a one-line guard plus `TestEveryKernelEndsTheNameAtTheFIRSTSemicolon`, mutation-tested: reverting the guard fails the new test while every pre-existing test still passes, which is exactly the gap. The test asserts only the name BOUNDARY, because a `;` in a name makes that row's temperature unparseable by construction and the kernels then resynchronise differently by design (staged from the parse's output, batch from the newline mask). (2) The repair changed the kernel the largest published number measures, so it was re-measured: `-42.3%` becomes `-40.4%` and `-40.7%` across two post-fix runs, corrected at all six sites that published it (report x4, data companion, built.md, handoff.md) with `CORRECTIONS.md` C3 recording the superseded claim. Both post-fix runs are published because they disagree about the 10k pair's separation and quoting only the agreeing one would be choosing the answer; the 10k crossover is now stated as a direction that reproduced twice, not a separated margin. (3) The report's Method section published H6 margins of `0.604` and `0.133 ns/row` while its own table and the data companion give `0.513` and `0.165` — a cross-invocation mix its own methodology forbids (`0.604` does turn up, in a different run). Corrected, and the "clears the floor by 60x to 600x" that followed from the wrong pair is now 83x to 513x, derived from the published floor. (4) The report claimed run-to-run spreads of "1-3%" as the reason its orderings are safe; measured full-range spread is 0.8% to 15.6%, and the noisiest loop of the eighteen is `batch/413`, the one the largest headline rests on. Replaced with the check that is both true and stronger: every verdict-bearing pair is disjoint over its ten runs, the winner's slowest beating the loser's fastest, with the one exception labelled. (5) The report cited a control run and a `PARKED.md` that existed in neither the repo nor any committed file. The control benchmarks were uncommitted; they are now committed, re-run, and their raw output is in the data companion, where the byte-identical twins agree to 0.001-0.002 ns/row. `1brc/PARKED.md` is created with P-01 (per-row NEON scan) and P-02 (NEON parse, never built, parked on a bound and recorded as a bound), all seven fields each, and indexed in root `PARKED.md`. Also verified rather than assumed: the page-guard test's claim that it catches a weakened over-read guard — narrowing the 16-byte guard to 8 makes `TestNEONIndexSemicolonStopsAtTheSliceEnd` segfault, as advertised.
- reviewed skill-performance-assembly 1c3fa1e + go-recon 600e9dc + go-v1-parallel f786d4e (repairs in this cycle): REPAIRED — one correctness defect and four published-number defects. (1) A byte range SHORTER than one row folded the next range's first row, so with more workers than rows a row was counted twice; ranges now own only rows that START inside them, pinned by `TestARangeShorterThanARowOwnsNothing` (11-1000 workers over a 10-row file, both splits, both readers). This is the fourth distinct boundary trap in this reader: the other three (a range starting exactly ON a boundary dropping its first row, a separator-less row folded into its successor and then panicking, a buffer no larger than its range unable to read the straddling row) were caught during the build by the same worker x buffer x split x reader sweep, which is the only test that found any of them. (2) `15.0x the skeleton at 100m` was arithmetic that never happened: 2.607 s against 161.0 ms is 16.2x, corrected at all three sites that published it (built.md, handoff.md, the ledger). (3) `branchless is 11.4% faster` inverted its own denominator; the number is the scalar arm costing 11.4% MORE than the default, and the report now states the convention (percentages are relative to the arm being replaced) because two reports in this cycle use it. (4) The 10k table gap was published as 3.5% (denominator: the loser) next to a 15.8% that used the winner; both now use the arm being replaced, so 3.7%, corrected at five sites. (5) `five extra integer ops per word` for the dual-needle scan is four, counted from the expression, and `six byte compares` in `validTemp` is four to six depending on the shape; both are labelled derived-by-counting rather than measured. Mutation-tested every guard this cycle decided, seven mutants, all seven caught: the short-range guard (8 subtests), the one-byte lookback (29), the dual-needle scan (the no-separator case, after a first attempt that only failed to COMPILE and therefore proved nothing), `validTemp` (4), `hashWord`'s mask (a new assertion on buckets-used, `418 buckets used for 413 stations`, because the output stayed correct through drain() and only the table filled), `rangeEnd`'s hi-1 (120), and the full-table guard, which is caught by the test HANGING rather than failing - honest limit, Go's own 10-minute test timeout is what would fail CI. Re-ran the whole gate after every repair; the 1b headline was NOT re-measured because no repair touches a path the 1b file exercises (its ranges are 920 MB and its rows all start inside them), and saying so is cheaper than a number that would drift 3% for reasons unrelated to the fix.
- reviewed x-transfer 5528b58 + go-v2-kernels 4e78d53 (measurement b13caec, repairs 0f5df22): REPAIRED — one false claim and three published-number defects, no correctness defect in the code. (1) The x-transfer report claimed the Super/Performance core split was unrecorded and made that its headline machine finding. It is recorded at `01-definition.md:91`, `02-baseline.md:15` and `03-technique-recon.md:25`, and `03`'s H1 names the asymmetry as its motivation — so the claim was false at three sites the report itself cites elsewhere. Caught while writing E-11, before the push, and corrected at four sites plus the ledger's H-11: what is actually new is the L2 asymmetry (16 MiB against 8 MiB) and the observation that `02-baseline.md:15`'s "a uniform 15-way split is close to optimal on this chip" is an untested *hypothesis* the code has been treating as a decision. That reframing is stronger than the false one it replaces, which is the tell that the original was reaching. (2) `swarMostSignificant` named a `0x7F7F…` constant that masks the most significant bit OFF; renamed `swarLow7Bits`. A reader trusting the name would have mis-derived the exactness argument the whole batch-SWAR kernel rests on. (3) `docs/1brc/handoff.md:61` still published the pre-C4 "a 1<<17 entry array is 4 MiB" with no marker, while `CORRECTIONS.md` C4 listed it as a corrected site; marked inline, so the correction claim and the file now agree. (4) `06`'s C2 said the batch tokenizer was "measured in this same cycle" and never said how it came out, which would have left the report reading as an endorsement of a shape the same cycle killed; it now carries the result and the boundary of what E-10 does and does not falsify (a vectorized probe is a different mechanism from a vectorized scan). Also verified rather than assumed: the C5 syscall arithmetic is now derived in the data companion rather than only inline, and it is stated in the direction generous to the idea it kills. Mutation-tested twelve mutants across `batch.go`, `table.go` and `reader.go`, all twelve caught — the exact zero-byte mask, both drains' first-`;` guard, the no-separator guard, `validTemp`, the over-read slack, the batch/tail handoff both ways, the NEON lane shift, the NEON window stride, `foldRows`' return position, and the `-parse scalar` rejection. Two survivors were fixed rather than accepted: `pendingSep+next != end` survived every mutant because `validTemp` already implies it and was DELETED, and the no-separator guard survived until its sibling was gone and a "bare temperature at the START of the buffer" case was added, which now panics on the mutant. Each of the three kernels was byte-compared against both committed expected outputs individually, because the gate script only runs the default.
- reviewed go-autoresearch-harness 42b5b3d + 4646edd (repairs 4e1dc6d): REPAIRED — one unpinned rule and two published-number defects, no correctness defect in the shipped Go binary (this group ships no Go). (1) The mutation round left ONE survivor: deleting `[[ -n $PRED ]]` kept `--self-test` green, because the digit rule rejects an empty prediction too. That is `lessons.md`'s masking pattern, its second appearance in this study, and it means neither guard was pinned. Per the recorded lesson the redundant guard was DELETED rather than double-tested, its message folded into the surviving rule, and the two prediction cases now assert the failure MESSAGE via a new `expect_msg` — status alone cannot distinguish which rule fired. Re-run widened from five mutants to twelve, one per guard in the script: all twelve caught, no survivors. (2) The experiment budget shipped as `(N+1) × 6 × 1.75 s` at two sites; hyperfine runs warmup-plus-runs per NAMED command, so it is `N × 6 × 1.75 s`, and the worked figure that followed it said 53 s where three arms cost 31.5. Both the formula and the figure were wrong in the same direction, which is what a formula nobody evaluates looks like. (3) "Four of the eleven rows here are kills" is three `KILLED-on-numbers` plus one `SPLIT` with a killed half — counted with `grep -o 'Verdict: [A-Za-z-]*' | sort | uniq -c` rather than remembered. Also verified rather than assumed: the data companion's `--self-test` block is `diff`ed against a live run byte for byte instead of transcribed; the queue board's two published commands are shown reaching the missing-file check with `ASSETS` pointed at nothing, which proves they parse and pass every rule; `ARM='-nosuchflag'` makes the correctness gate exit 2, proving the new variable reaches the binary rather than being dropped; and a failing arm aborts the experiment with hyperfine never invoked and no bench file written. Not repaired, and named because a reader of the new `CORRECTIONS.md` C5 will trip on it: `06-cross-disciplinary-transfer.md` numbers its candidates C1-C6 while `CORRECTIONS.md` numbers corrections C1-C5, so "C5" now means two things in this study. That collision predates this group and renaming either sequence would break citations in files this group does not own.
- reviewed go-opt-round-1 4498b8b + ff99119 + d879b70 (repairs in this cycle): REPAIRED — one stale claim in the code, three published-number defects and one gap in the correction itself, no correctness defect in the binary. (1) `main.go`'s package comment still described the shipped shape as "one goroutine per core" in the same commit that stopped it being one; it now says more goroutines than cores and cites E-17. That is the claim-vs-diff mismatch the protocol says to look for, and it was in the one file the cycle actually changed. (2) A number disagreed with itself across sites: E-13's row said the combination run measured `-buf 8192` at 12.98% slower while E-14's row said 12.02%, and the live evaluation says **12.01** — the swing is 20.65 points, not 20.66. Corrected at all four sites (both rows, `built.md`, `handoff.md`), and every derived figure in the round is now pasted into the data companion as the command AND its output, per the standing lesson, which is how the 12.02-against-12.01 rounding was caught at all. (3) E-16's row claimed "slots 1-7 separate cleanly"; they do not — adjacent slots overlap, because the 0.030 s step sits inside the 0.012-0.036 s σ. The true and stronger statement is that slot 1 is disjoint from every slot from 3 onward and the result is the monotone trend over eight slots. (4) The handoff claimed seven 1b experiments and 28 arm-checks; it is six experiments, 26 arm passes and 52 file byte-compares, counted rather than remembered. (5) C6 listed `docs/1brc/handoff.md` as a site that published the falsified rule but its "Corrected in" line did not account for it; it now enumerates all eight marked sites and follows C4/C5 in correcting the historical handoff block from this cycle's own block rather than editing history. Also verified rather than assumed: the ledger table originally called the 1.742 s v1 headline "slot-biased in an unknown direction", which is **wrong** — `bench.sh` runs one command per hyperfine invocation (checked, `bench.sh:42`), so it has no arm slots at all; the row now says what it does carry, its own 5 runs' 2.8% spread. Mutation-tested every guard this cycle decided: reverting `defaultWorkers` to `runtime.NumCPU()` fails `TestTheDefaultOversubscribesTheCores`; deleting the numeric-cooldown rule fails the self-test on the MESSAGE, not just the status; flipping the 1b cooldown default from 20 to 0 fails its own assertion. One mutant SURVIVED and was not accepted: rewriting `if ((COOLDOWN)); then args+=(...); fi` as `((COOLDOWN)) && args+=(...)` ran fine at cooldown 0, because bash exempts a failing left-hand side of `&&` from `set -e` — so the comment that justified the `if` was itself false and was deleted rather than left as an untested claim about the shell.
