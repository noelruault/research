# Review log — 1brc

One line per group, written by the cycle that built it, after auditing its own diff. Read this before building: it is this codebase's memory of what it gets wrong.

- reviewed def-understand 4145227 + env-data d22e3dc: REPAIRED — four defects found by auditing the diff against the handoff. (1) `AppendTenths` assumed the whole part fitted in two digits and silently emitted garbage above it (`1000` tenths printed as `":0.0"`), while `ParseTenths` happily accepted `"500.0"`; replaced with `strconv.AppendInt` (also shorter) and added `TestAppendTenthsBeyondLegalRange`. (2) `Aggregate` accepted out-of-range temperatures and folded them into a plausible-looking result; it now fails with the line number, pinned by `TestAggregateRejectsOutOfRangeTemperatures`. (3) The `Tenths` doc comment justified the whole integer-grid design with a *derived* claim — "float64 drifts by ~1e-2 per station" — that measurement disproved by four orders of magnitude (actual drift 9.2e-07, and float64 recovers the exact sum). The design is right, the stated reason was not; the comment now cites `TestFloat64AccumulationDriftIsSmall`, which measures it. (4) `01-definition.md` cited `semantics_probe_test.go`, a file that does not exist (it is `tenths_test.go`) — a claim-vs-diff mismatch found exactly where the handoff protocol says to look. Also closed a contract gap the group created: `1brc/code/gen` had no green gate in `spec.md`, so the new code was ungated; added one, with `test -z "$(gofmt -l .)"` because `gofmt -l` exits 0 even when it lists files. Mutation-tested the suite over 17 mutants across both files: all 17 caught after the fixes, and the one survivor in the first round (a separator-less line that is itself a valid temperature, which panics if the check is removed) is now covered. Expected outputs re-derived after every fix and confirmed byte-identical, so none of the repairs moved the correctness target.
- reviewed env-baseline d5d62fb + asm-recon 61dcbb6 + go-skeleton ddefd8f: REPAIRED (ebbac7f) — five defects. (1) Mutation testing over the two new files caught 17 of 18 mutants; the survivor was `bytes.IndexByte` → `bytes.LastIndexByte` in the skeleton's separator scan, i.e. the first-vs-last-`;` divergence from `gen.Aggregate` that this same cycle had deliberately fixed and then left unguarded. A case with `;` inside the name pins it (first separator ⇒ both reject, last ⇒ they diverge) and the mutant is now caught. (2) `03-technique-recon.md` listed mmap under "techniques that do not survive the port" and `02-baseline.md` handed forward three I/O "constraints" — both violate the amended `spec.md:35`/`:37`, because every baseline number is a *scan* with no aggregation running and nothing may be discarded without an end-to-end measurement. mmap is now hypothesis H7 with `madvise(MADV_WILLNEED)` named as its untried rescue, and the constraints became a starting point plus three stated hypotheses. (3) No measurement recorded power or load (`spec.md:42`): the session was on BATTERY at load 4.29, so every 02-baseline figure is labelled provisional with the strategy *ordering* flagged as the trustworthy part, and `bench.sh` now captures `pmset`/`uptime` and stamps PROVISIONAL itself. (4) `CORRECTIONS.md` did not exist; created with the two due entries, C1 (the invented 88.6% name-length figure, measured 34.1%) and C2 (the "warm page cache" target, corrected at its site in `01-definition.md:91`). (5) Reconciliation, not a correction: the `go-skeleton` commit message quotes 265.6 ms ± 4.5 ms for 10m from a 3-run dirty-tree bench, while the authoritative figure is the committed bench file's clean 5-run 260.5 ms ± 1.5 ms (26.1 ns/row, linear against 2.607 s at 100m). Both are real runs; the bench file is the record. (6) Every sha this cycle recorded in built.md, handoff.md, review.md, CORRECTIONS.md and the bench-file header was orphaned by the mid-cycle rebase onto the operator amendment, so the ledger cited commits that no longer exist on the branch; all sites are repointed (env-baseline d5d62fb, asm-recon 61dcbb6, go-skeleton ddefd8f) and the bench header names the rewrite explicitly. Claim-vs-diff check on the handoff found no mismatches beyond these; every "verified" line's gate command was re-run and the 754.4 ms / 34.1% / 26.1 ns/row figures agree at every site that publishes them.
- reviewed asm-kernels b4be167 (repairs 730d166, 0956159): REPAIRED — five defects, one of them a correctness bug in the kernel the report recommends. (1) `TokenizeBatch` ended a name at the LAST `;` in a row while `TokenizeScalar`, `TokenizeStagedSWAR` and `TokenizeStagedNEON` all end it at the first, which is `gen.Aggregate`'s rule: on `Ab;cd;1.0` it reported a 5-byte name against the others' 2-byte one, so it would have attributed the row to a different station. Neither key set produces a name containing `;`, so `TestTokenizersMatchTheScalarReference` and `TestBatchBenchLoopCoversEveryRow` both passed over both corpora with the bug present — the benchmark was pricing a kernel that was not doing the job correctly. This is the THIRD appearance of the first-vs-last-`;` divergence in this study (lessons.md records it in `go-skeleton`, one group earlier). Fixed with a one-line guard plus `TestEveryKernelEndsTheNameAtTheFIRSTSemicolon`, mutation-tested: reverting the guard fails the new test while every pre-existing test still passes, which is exactly the gap. The test asserts only the name BOUNDARY, because a `;` in a name makes that row's temperature unparseable by construction and the kernels then resynchronise differently by design (staged from the parse's output, batch from the newline mask). (2) The repair changed the kernel the largest published number measures, so it was re-measured: `-42.3%` becomes `-40.4%` and `-40.7%` across two post-fix runs, corrected at all six sites that published it (report x4, data companion, built.md, handoff.md) with `CORRECTIONS.md` C3 recording the superseded claim. Both post-fix runs are published because they disagree about the 10k pair's separation and quoting only the agreeing one would be choosing the answer; the 10k crossover is now stated as a direction that reproduced twice, not a separated margin. (3) The report's Method section published H6 margins of `0.604` and `0.133 ns/row` while its own table and the data companion give `0.513` and `0.165` — a cross-invocation mix its own methodology forbids (`0.604` does turn up, in a different run). Corrected, and the "clears the floor by 60x to 600x" that followed from the wrong pair is now 83x to 513x, derived from the published floor. (4) The report claimed run-to-run spreads of "1-3%" as the reason its orderings are safe; measured full-range spread is 0.8% to 15.6%, and the noisiest loop of the eighteen is `batch/413`, the one the largest headline rests on. Replaced with the check that is both true and stronger: every verdict-bearing pair is disjoint over its ten runs, the winner's slowest beating the loser's fastest, with the one exception labelled. (5) The report cited a control run and a `PARKED.md` that existed in neither the repo nor any committed file. The control benchmarks were uncommitted; they are now committed, re-run, and their raw output is in the data companion, where the byte-identical twins agree to 0.001-0.002 ns/row. `1brc/PARKED.md` is created with P-01 (per-row NEON scan) and P-02 (NEON parse, never built, parked on a bound and recorded as a bound), all seven fields each, and indexed in root `PARKED.md`. Also verified rather than assumed: the page-guard test's claim that it catches a weakened over-read guard — narrowing the 16-byte guard to 8 makes `TestNEONIndexSemicolonStopsAtTheSliceEnd` segfault, as advertised.
- reviewed skill-performance-assembly 1c3fa1e + go-recon 600e9dc + go-v1-parallel f786d4e (repairs in this cycle): REPAIRED — one correctness defect and four published-number defects. (1) A byte range SHORTER than one row folded the next range's first row, so with more workers than rows a row was counted twice; ranges now own only rows that START inside them, pinned by `TestARangeShorterThanARowOwnsNothing` (11-1000 workers over a 10-row file, both splits, both readers). This is the fourth distinct boundary trap in this reader: the other three (a range starting exactly ON a boundary dropping its first row, a separator-less row folded into its successor and then panicking, a buffer no larger than its range unable to read the straddling row) were caught during the build by the same worker x buffer x split x reader sweep, which is the only test that found any of them. (2) `15.0x the skeleton at 100m` was arithmetic that never happened: 2.607 s against 161.0 ms is 16.2x, corrected at all three sites that published it (built.md, handoff.md, the ledger). (3) `branchless is 11.4% faster` inverted its own denominator; the number is the scalar arm costing 11.4% MORE than the default, and the report now states the convention (percentages are relative to the arm being replaced) because two reports in this cycle use it. (4) The 10k table gap was published as 3.5% (denominator: the loser) next to a 15.8% that used the winner; both now use the arm being replaced, so 3.7%, corrected at five sites. (5) `five extra integer ops per word` for the dual-needle scan is four, counted from the expression, and `six byte compares` in `validTemp` is four to six depending on the shape; both are labelled derived-by-counting rather than measured. Mutation-tested every guard this cycle decided, seven mutants, all seven caught: the short-range guard (8 subtests), the one-byte lookback (29), the dual-needle scan (the no-separator case, after a first attempt that only failed to COMPILE and therefore proved nothing), `validTemp` (4), `hashWord`'s mask (a new assertion on buckets-used, `418 buckets used for 413 stations`, because the output stayed correct through drain() and only the table filled), `rangeEnd`'s hi-1 (120), and the full-table guard, which is caught by the test HANGING rather than failing - honest limit, Go's own 10-minute test timeout is what would fail CI. Re-ran the whole gate after every repair; the 1b headline was NOT re-measured because no repair touches a path the 1b file exercises (its ranges are 920 MB and its rows all start inside them), and saying so is cheaper than a number that would drift 3% for reasons unrelated to the fix.
- reviewed x-transfer 5528b58 + go-v2-kernels 4e78d53 (measurement b13caec, repairs 0f5df22): REPAIRED — one false claim and three published-number defects, no correctness defect in the code. (1) The x-transfer report claimed the Super/Performance core split was unrecorded and made that its headline machine finding. It is recorded at `01-definition.md:91`, `02-baseline.md:15` and `03-technique-recon.md:25`, and `03`'s H1 names the asymmetry as its motivation — so the claim was false at three sites the report itself cites elsewhere. Caught while writing E-11, before the push, and corrected at four sites plus the ledger's H-11: what is actually new is the L2 asymmetry (16 MiB against 8 MiB) and the observation that `02-baseline.md:15`'s "a uniform 15-way split is close to optimal on this chip" is an untested *hypothesis* the code has been treating as a decision. That reframing is stronger than the false one it replaces, which is the tell that the original was reaching. (2) `swarMostSignificant` named a `0x7F7F…` constant that masks the most significant bit OFF; renamed `swarLow7Bits`. A reader trusting the name would have mis-derived the exactness argument the whole batch-SWAR kernel rests on. (3) `docs/1brc/handoff.md:61` still published the pre-C4 "a 1<<17 entry array is 4 MiB" with no marker, while `CORRECTIONS.md` C4 listed it as a corrected site; marked inline, so the correction claim and the file now agree. (4) `06`'s C2 said the batch tokenizer was "measured in this same cycle" and never said how it came out, which would have left the report reading as an endorsement of a shape the same cycle killed; it now carries the result and the boundary of what E-10 does and does not falsify (a vectorized probe is a different mechanism from a vectorized scan). Also verified rather than assumed: the C5 syscall arithmetic is now derived in the data companion rather than only inline, and it is stated in the direction generous to the idea it kills. Mutation-tested twelve mutants across `batch.go`, `table.go` and `reader.go`, all twelve caught — the exact zero-byte mask, both drains' first-`;` guard, the no-separator guard, `validTemp`, the over-read slack, the batch/tail handoff both ways, the NEON lane shift, the NEON window stride, `foldRows`' return position, and the `-parse scalar` rejection. Two survivors were fixed rather than accepted: `pendingSep+next != end` survived every mutant because `validTemp` already implies it and was DELETED, and the no-separator guard survived until its sibling was gone and a "bare temperature at the START of the buffer" case was added, which now panics on the mutant. Each of the three kernels was byte-compared against both committed expected outputs individually, because the gate script only runs the default.
- reviewed go-autoresearch-harness 42b5b3d + 4646edd (repairs 4e1dc6d): REPAIRED — one unpinned rule and two published-number defects, no correctness defect in the shipped Go binary (this group ships no Go). (1) The mutation round left ONE survivor: deleting `[[ -n $PRED ]]` kept `--self-test` green, because the digit rule rejects an empty prediction too. That is `lessons.md`'s masking pattern, its second appearance in this study, and it means neither guard was pinned. Per the recorded lesson the redundant guard was DELETED rather than double-tested, its message folded into the surviving rule, and the two prediction cases now assert the failure MESSAGE via a new `expect_msg` — status alone cannot distinguish which rule fired. Re-run widened from five mutants to twelve, one per guard in the script: all twelve caught, no survivors. (2) The experiment budget shipped as `(N+1) × 6 × 1.75 s` at two sites; hyperfine runs warmup-plus-runs per NAMED command, so it is `N × 6 × 1.75 s`, and the worked figure that followed it said 53 s where three arms cost 31.5. Both the formula and the figure were wrong in the same direction, which is what a formula nobody evaluates looks like. (3) "Four of the eleven rows here are kills" is three `KILLED-on-numbers` plus one `SPLIT` with a killed half — counted with `grep -o 'Verdict: [A-Za-z-]*' | sort | uniq -c` rather than remembered. Also verified rather than assumed: the data companion's `--self-test` block is `diff`ed against a live run byte for byte instead of transcribed; the queue board's two published commands are shown reaching the missing-file check with `ASSETS` pointed at nothing, which proves they parse and pass every rule; `ARM='-nosuchflag'` makes the correctness gate exit 2, proving the new variable reaches the binary rather than being dropped; and a failing arm aborts the experiment with hyperfine never invoked and no bench file written. Not repaired, and named because a reader of the new `CORRECTIONS.md` C5 will trip on it: `06-cross-disciplinary-transfer.md` numbers its candidates C1-C6 while `CORRECTIONS.md` numbers corrections C1-C5, so "C5" now means two things in this study. That collision predates this group and renaming either sequence would break citations in files this group does not own.
- reviewed go-opt-round-1 4498b8b + ff99119 + d879b70 (repairs in this cycle): REPAIRED — one stale claim in the code, three published-number defects and one gap in the correction itself, no correctness defect in the binary. (1) `main.go`'s package comment still described the shipped shape as "one goroutine per core" in the same commit that stopped it being one; it now says more goroutines than cores and cites E-17. That is the claim-vs-diff mismatch the protocol says to look for, and it was in the one file the cycle actually changed. (2) A number disagreed with itself across sites: E-13's row said the combination run measured `-buf 8192` at 12.98% slower while E-14's row said 12.02%, and the live evaluation says **12.01** — the swing is 20.65 points, not 20.66. Corrected at all four sites (both rows, `built.md`, `handoff.md`), and every derived figure in the round is now pasted into the data companion as the command AND its output, per the standing lesson, which is how the 12.02-against-12.01 rounding was caught at all. (3) E-16's row claimed "slots 1-7 separate cleanly"; they do not — adjacent slots overlap, because the 0.030 s step sits inside the 0.012-0.036 s σ. The true and stronger statement is that slot 1 is disjoint from every slot from 3 onward and the result is the monotone trend over eight slots. (4) The handoff claimed seven 1b experiments and 28 arm-checks; it is six experiments, 26 arm passes and 52 file byte-compares, counted rather than remembered. (5) C6 listed `docs/1brc/handoff.md` as a site that published the falsified rule but its "Corrected in" line did not account for it; it now enumerates all eight marked sites and follows C4/C5 in correcting the historical handoff block from this cycle's own block rather than editing history. Also verified rather than assumed: the ledger table originally called the 1.742 s v1 headline "slot-biased in an unknown direction", which is **wrong** — `bench.sh` runs one command per hyperfine invocation (checked, `bench.sh:42`), so it has no arm slots at all; the row now says what it does carry, its own 5 runs' 2.8% spread. Mutation-tested every guard this cycle decided: reverting `defaultWorkers` to `runtime.NumCPU()` fails `TestTheDefaultOversubscribesTheCores`; deleting the numeric-cooldown rule fails the self-test on the MESSAGE, not just the status; flipping the 1b cooldown default from 20 to 0 fails its own assertion. One mutant SURVIVED and was not accepted: rewriting `if ((COOLDOWN)); then args+=(...); fi` as `((COOLDOWN)) && args+=(...)` ran fine at cooldown 0, because bash exempts a failing left-hand side of `&&` from `set -e` — so the comment that justified the `if` was itself false and was deleted rather than left as an untested claim about the shell.
- reviewed go-opt-round-2-profile 8901488 + ae2b753 + 028f640 (repairs in this cycle): REPAIRED — three defects, two of them in numbers this cycle published and one of them a FALSE CLAIM sitting at the top of the queue. (1) **Item 16, the next thing a cycle would build, was wrong.** It said the correctness tax could be paid by "one integer range test on the value the parse already produced" instead of `validTemp`'s byte compares. `table.go:134` is `if next == 0 || !validTemp(field, next) || !inRange(v)`: the integer range test is ALREADY there on every row, and `validTemp` establishes something it cannot — that the bytes were a temperature at all, which `kernel.go:121` says in its own doc comment. `parseTempBranchless` returns an in-range integer from `1x.5` or `--1.5`, so the swap would have accepted rows the reference rejects, and **no generated corpus can catch it** because every generated row is well-formed. That is the third appearance of the "no corpus can express it" trap in `lessons.md`. Item 16 is rewritten to be a cheaper way to establish the SAME format rejection (SWAR digit test on the 8-byte word the parse already loads), it now records the wrong version and why it fails so it is not tried twice, and its prediction says the 18.2% ceiling is NOT reachable because the check is replaced rather than removed. (2) **A number was misattributed at three sites.** `(*table).merge` at 1.7% of CPU was published beside "the merge is 0.34% of wall" as though they were the same thing, and used as evidence for closing queue item 10. `-peek` shows `merge` is inlined into `update` and called per row: `table.go:81` folds one measurement into min/max/sum/count, while item 10's merge is the cross-shard drain at `reader.go:208` that `-phases` times separately. Two functions named `merge` at two scales. Item 10 still closes at 413 stations, on the 0.34% figure alone; the 1.7% is now labelled per-row aggregation in E-21, the board row and `built.md`, with the nesting spelled out (`memequal` 5.8% and `merge` 1.7% sit INSIDE `update`'s 15.9%, so the shares do not add). (3) **A novelty claim was too strong**: "the study's first non-provisional number" — the AC-storm file is also on AC power. Now stated as the first number neither on battery nor otherwise confounded, with the count that backs it (12 of 14 files in `bench/` carry PROVISIONAL, and the other one is stamped void). Also hardened in review: `measure_lock_release` was an unconditional `rm -rf` on an env-overridable path, so it now removes only a directory holding this process's own pid, pinned by a check that a lock owned by pid 4242 survives release. Mutation: 15 mutants over the new shell across two rounds, all caught. The first round left two survivors and both were real gaps — nothing pinned the preflight's defaults (a mutant setting QUIET_LOAD=999 disabled the gate in production while every test passed, since the tests override it) and nothing pinned the wait-then-refuse path (every fast test set QUIET_WAIT=0), so the sleep became a seam and both are now asserted. What the gate still does not cover: no 1b timing runs inside it, so nothing proves the preflight's threshold is the right threshold — 6.0 is a judgement from two observations, not a measured optimum.
- reviewed go-opt-round-2-item15 2c389c5 + 2beb9e1 (repairs in this cycle): REPAIRED — three defects in numbers this cycle published and four stale sites the change itself created, no correctness defect in the binary. (1) **A count was remembered rather than counted**: the handoff claimed the harness gated "20 arms across the three completed invocations"; it is 8 + 9 + 6 = **23**. (2) **A disagreement was rounded in the flattering direction**: M against N was published as "up to 12 points" at two sites when the largest is **11.85** (pagecache, +12.04% against +0.19%); both now carry the pair the figure comes from. (3) **A load-bearing sentence said the opposite of what it meant**: E-24's verdict read "1 MiB is a minimum: it loses in both directions", which asserts the winner loses; it is *moving off it* that loses in both directions. (4) The reversal made four figures elsewhere stale and every one is marked at its own site: `06-cross-disciplinary-transfer.md`'s syscall-count kill (13,795,610,267 / 4 MiB = 3,289 reads becomes **13,157**, evaluated not estimated — and the kill holds a fortiori, because E-24 measured the smaller buffer FASTER while making four times as many syscalls, which is the same claim the kill rests on), `05-go-techniques.md`'s "one reusable 4 MiB buffer per worker" describing what it calls "our I/O strategy", and that report's open recommendation to "try 4 MiB alongside 1 MiB", now answered. (5) The most valuable thing the audit found is not a defect: **`02-baseline.md`'s H-io-3 predicted this result** — "1 MiB is at or near the optimum" — and E-06's contaminated invocation overrode it for six ledger rows. It is marked CONFIRMED rather than corrected, and it is independent support for E-24 from a different harness. Also verified rather than assumed: the pinning test asserts a constant and therefore cannot tell a wired constant from an unused one, so `./bin/1brc -h` was run and shows `(default 1024)`, recorded in the data companion; 1 MiB's range [1.504-1.519] was checked disjoint from all four other arms individually rather than read off hyperfine's summary; and the aborted invocation's `sleep` failure was reproduced-as-absent (`hyperfine --prepare "sleep 1"` exits 0 foregrounded and backgrounded) rather than diagnosed from the error text. Mutation: reverting `defaultBufKiB` to 4096 fails `TestTheDefaultReadBufferIsTheSweptMinimum` on its message, and the whole gate plus both correctness key sets were re-run after every repair. What the gate still does not cover, and cannot: nothing in it re-runs a 1b invocation, so the bracket is the only guard that an invocation is readable — this cycle is the first time it refused one (E-22), and it refused the only invocation of the four whose numbers would otherwise have been believed.

- reviewed go-opt-round-2-item16 bde92b1..0c2b7f9: REPAIRED — the code and the measurement held; every defect was a published EXAMPLE written from reasoning instead of measured, which is this file's most repeated failure. (1) `nonDigitMask`'s comment claimed `0x2F` passes the low-nibble test; it fails BOTH halves (low nibble 0xF + 6 carries out of the nibble), and the byte that actually passes it is `'b'` (0x62) — the same wrong byte had also reached the data companion's M4 line. (2) The mutation table named `"b;1.0"` as the sign guard's case; measured, TWO guards reject it (the sign byte and the digit lanes), so dropping either alone still rejects it, and the sign guard's exclusive case is `"\x005.0"`. (3) `parseTempWord`'s comment named `"-.5"` as a digit-count case; the digit lanes reject it too. (4) "no data-dependent branch" overstated at two sites: the `dot > 28` guard stays, and what left the loop is the UNPREDICTABLE branch — re-worded in E-25 and `built.md`. (5) The audit that found 1-3 also produced the missing evidence: each of the five guards was dropped one at a time and its exclusive input measured, which is now a table in the data companion and is what "load-bearing" should have meant instead of "ten mutants caught". No new test was needed — the exhaustive sweep already contains all five classes over its alphabet (`100.0`, `1b.9`, `1.9b`, `1-9`, `\x001.0`), which is why every mutant died. Verified the pinning test by mutation (`defaultParse = "branchless"` fails it), and re-read the threading diff line by line: mechanical, and the 9-byte over-read guard in `foldRows` already covers `parseTempWord`'s 8-byte load. Consequence handled outside the group's code, because this change moved its input: H-14's ceiling was published as 17.6% derived from a baseline that no longer exists, so all six forward-looking sites (board item, board row, the ordering paragraph, two in `06`, and item14's backlog line) now carry 19.1% landing on the 1.152 s floor.

- reviewed go-opt-round-2-items1and5 080f332..3803169: REPAIRED — the measurement and the arms held; four defects, and the worst of them was a test default that had been lying since the previous cycle. (1) **`main_test.go:defaults()` restated the parse instead of reading `defaultParse`**, so it still said `branchless` after E-25 shipped `word`, and the two range-boundary sweeps — which `lessons.md` names as the only tests that find a byte-offset off-by-one — had been running a configuration the binary does not. Both the parse and the new fold now read the shipped constants, and the boundary sweep runs BOTH row loops, 224 subtests. Honest negative recorded rather than hidden: no mutant was found that only the fold dimension catches, because `foldRange`/`rangeEnd` are shared by every arm, so that dimension is coverage of the shipped shape and not a demonstrated kill. (2) **A count was remembered rather than counted**, which is this file's most repeated failure and it happened again: the handoff claimed "15 mutants, 12 caught"; counted, it is **12 valid mutants and 11 caught**, with three more discarded as EQUIVALENT (a rotated-loop bound, a stricter over-read guard, the fuse forced always-on — each a no-op or a slowdown no behavioural test can see) and three that failed to apply. Corrected at both sites. (3) **A comment asserted a memory-safety guarantee it does not provide**: `foldRowsPtr` said taking `data` by value keeps the buffer reachable. It does not — what keeps it alive is the caller owning `buf` across the call, and the comment now says so, with the condition under which a future caller would need a `KeepAlive`. (4) **E-25's verdict still published "the study's best is 1.424 s"** in the present tense two rows above E-27's 1.233 s; marked superseded at its own site rather than rewritten. Also verified rather than assumed: the id in `built.md` matches `backlog.md` byte for byte and the selector's `^- ?[a-z0-9-]+` extraction resolves it; every derived figure in E-26, E-27 and C9 is the output of a script pasted into the data companion, not prose arithmetic; and the three surviving mutants from the first round were each traced to a real gap or proved unkillable by measurement (`TestParseTempWordIgnoresTheBytesPastTheField` randomises every lane at or past the field over all 1,999 legal temperatures), never argued away. The claim-vs-diff check found no mismatch in what the group said it built.
- reviewed go-opt-round-2-item13 ba1c6e7..3b6c325: REPAIRED — the kill stands and its numbers reproduce across every site, but the arm shipped with its central guard unmutated and the review found a real hole. (1) **The long/short boundary was untested at the only length that decides it.** Mutating `updateQuot`'s `long := len(name) > 8` to `> 9` passed the whole suite. It is a real bug, not an equivalent mutant: `hashWord` masks only the first eight bytes, so two nine-byte names sharing a prefix land in the same bucket with the same `nlen`, and skipping the key compare merges two stations into one. **No generated corpus can express it** — the 413 set's single 8-byte-prefix collision is `Alexandra` (9) / `Alexandria` (10), which `nlen` separates by length, so the key compare is never exercised at nine bytes in real data. `TestQuotSeparatesNamesOneWordCannotTell` gained a nine-byte pair; under the mutant it now reports 1 station against the reference's 2. Two other mutants were caught by the existing tests (dropping the `nlen` length compare, dropping the `+1` that makes a zeroed bucket mean empty), so the score for the group is 3 run, 2 caught, 1 gap, 0 equivalent. (2) **The `-table` guard had no test**, which is the one guard whose absence is a measurement hazard rather than a correctness one: before it, `-table quto` ran the incumbent and would have published the incumbent's wall clock under the arm's name. Pinned, and the old fall-through fails it. (3) **`qentry`'s doc comment stated a row share as a property of the name set** — "34.1% of the 413 official rows" — when it is 141 of 413 NAMES and the row share follows only from `gen.Write` picking its station with a uniform `IntN`. Counted: 141, 34.1404%. (4) **The handoff's threads line published both bucket-layout losses with a negative sign**, `E-03 −4.6%` and `E-28 −5.3%`, which reads as an improvement; the ledger, the board row and built.md all publish them as `+` of wall, meaning slower. Corrected to the published sign. The verdict itself is untouched: the arm is still KILLED-on-numbers at +5.34% against the bracket mean.
- reviewed go-opt-round-2-item14 92669cb..6936b9a: REPAIRED — the measurement held twice and the code did not. (1) **The arm shipped with an alignment-dependent contract.** `foldRangeFill` reserved `maxRow` at the front of each buffer for the carry, so a row between `maxRow` and the buffer size was rejected only when the chunk boundary happened to leave more than `maxRow` of it trailing: a 306-byte row passed at `-workers 1` and failed at `-workers 7`, while `-fill off` accepted it at every worker count. Byte-comparing the 10m and 10k corpora could never see it — no generated row exceeds 107 bytes — which is this repo's oldest failure mode (the first-vs-last-`;` divergence, three times) and the reason the standing lesson demands a sibling-agreement test on input the corpus cannot express. Repaired with a chunk-sized prefix, and pinned by `TestTheReadersAgreeOnOverlongRows`. (2) **The repair touched the timed path, so E-29 stopped describing the binary.** Re-run as E-30 rather than quoted forward (E-11): `ahead` +0.82%/−0.08% in a 0.905% bracket, verdict unchanged, and the arm moved 0.57% across the two invocations, which is what says the repair cost it nothing. (3) **A mutant that no byte-compare can catch.** Releasing a buffer to the reader before its carry is copied out is the double buffer's characteristic bug and it survived the whole suite; `go test -race` catches it, so the gate in `spec.md` gained a `-race` run — a contract edit, made because the alternative was an unpinned invariant in shipped code. (4) **The over-long bound was pinned one past the split, not at it.** A mutant moving `nl >= chunk` to `nl > chunk` survived until the sweep gained rows of exactly `chunk` and `chunk+1` bytes; the same lesson `nlen` taught at nine bytes, in a different guard, one cycle later. (5) Two of 15 mutants survive and are EQUIVALENT by proof rather than gaps — `nl >= from-1` is invariant so weakening that guard cannot panic, and `ReadAt` never reads short without an error so the producer's advance is the same either way. Also checked and clean: every number published in E-29, E-30, C10 and the board was evaluated in a shell rather than written in prose; the five sites publishing H-14's ceiling all carry the falsification; `built.md` and `handoff.md` history left as written per C4-C9.
- reviewed go-opt-round-3 4cecf8d..HEAD: REPAIRED — a docs-only SPLIT, so the audit is of the split itself, and it found two classes of defect before the commit and one after. (1) **Two of four line-number citations did not resolve**: `07-experiment-ledger.md:41` for the 19.85%-above-the-floor row that lives at `:44`, and `spec.md:81` for the honest-miss clause that lives at `:61`. Both were checked with `sed -n '<n>p' | grep -c` and corrected before committing; a backlog line is read as the contract by the cycle that builds it, so an unresolvable citation there costs a re-derivation. (2) **The dependency re-point was the load-bearing part and it nearly went unwritten.** `method-retro` and `final-dod` both read `(needs: `go-opt-round-3`)`, and `is_built` (`scripts/loop-next.sh:37`) matches built.md with `grep -qxF` while a SPLIT parent never gets a built.md line — left alone, both terminal tickets would have been masked permanently. Re-pointed onto `go-opt-round-3-park` and verified by running the selector and its selftest (40 runs agree on `go-opt-round-3-item17`, and nothing dispatched appears in built.md). (3) After the commit, the review caught the split disagreeing with itself: `go-opt-round-3-item17` said "Four arms" beside an invocation listing five `-a` slots, because the board row it inherited counts configurations and the cost estimate counts slots. Corrected to "four configurations in five slots"; the `~5 x 6 x 21.7 s = 11 min` estimate was already the five-slot one and re-evaluated at 651 s. Also verified rather than assumed: the split is purely additive (24 ids to 28, `diff` of the sorted id lists shows four additions and no deletion or renumber), every flag named in the new lines exists in `main.go` and `experiment.sh` (`-fill`, `-workers`, `-phases`, `-cpuprofile`, `--mechanism-only`, and `-f`'s plain substitution into `measurements-$FILE.txt`, which is why the 242m file needs no harness change), the generator invocation matches `cmd/measurements`'s real flags, and every derived figure in the split — 57.0942 B/row, 241.6M rows at 13.794 GB against the 1b file's 13.796 GB, 57.09 GB at 2.22x RAM, the 3.12 s read floor — was evaluated in `python3` and not written in prose.
- reviewed go-opt-round-3-item17 62dba55..5b1327c: REPAIRED — the measurement is sound and the write-up of it was not, which is the failure mode this gate exists for: no code changed, so every defect was a published claim. **Four were wrong.** (1) *"eight points below the 1.15 trigger"* is **12.3 points** — 2.7% of measured imbalance against the 15% the trigger asks for — and it was wrong at four sites (the ledger row, `PARKED.md` P-05, the backlog line, `built.md`), which is the corrections rule biting inside a single cycle rather than across two. (2) *"its range covering `default.b`'s"* is false: `ahead-w20`'s 1.219-1.245 **overlaps** 1.216-1.240, it does not cover it, and "covers" is the stronger claim. (3) *"the only arm whose parallel efficiency moves"* is false — `ahead-w15` moves too, by three points, and that is exactly why it lands 3.88% short instead of level; the repair replaces the assertion with the whole ladder (80.2/80.0 · 69.7 · 77.0 · 79.8) and turns the second channel into a real cross-check, because the prefetch buys back 7.3 of the 10.4 points, **70.2% of the drop against the wall channel's 72.5%**. (4) *"the study's best measured wall clock is unchanged at 1.220 s"* misstates the standing figure, which is **E-27's 1.233 s**: 1.220 s is a bracket slot, a bracket slot is a control and not a headline, and by the same rule E-29's 1.213 s was never a best either. **Two more were weaker than the measurement supports, which the audit counts as defects because the row is the evidence:** (5) `ahead-w15` is **disjoint from both bracket slots** (1.269 min against maxima of 1.228 and 1.240) and the row never said so, leaving a measured loss reading like a slot artifact; (6) *26.3% of read+fold* is the weak form of the mechanism claim — the unstaged identity (sum(worker wall)/(read+fold) = 1.0011 for both `-fill off` arms) licenses subtracting the fold, which leaves **3.257 s of read still on the critical path out of 10.025 s, so 67.5% of it is hidden and a third is not**. Every repaired figure now has its evaluation in `07-experiment-ledger-data.txt` §S instead of being asserted in prose, and the `-split static` claim the substitution reading rests on is pinned at its call site (`reader.go:162`, one range per worker, so 30 goroutines on 15 cores is exact rather than estimated). **No test to mutate:** this group added no code, so the mutation analog is the discriminator itself, and it has a control — the 1.0011 identity reproduces on two independent unstaged configurations while the staged one reads 0.7368, across three rounds each.
- reviewed go-opt-round-3-gap c565dda..HEAD: REPAIRED — no code changed, so every defect was a published claim, and the sharpest one was a check that could not fail. **(1) The handoff called the decomposition identity its evidence.** `idle := capacity − user − sys` makes `(sys + idle)/capacity ≡ (wall − user/15)/wall` algebraically, so "reproduces to the digit in every round" is a tautology that agrees whatever the inputs are. Replaced with the checks that CAN fail and did not: `sum(worker wall)/(read + fold)` = 1.0008/1.0017/1.0018 (the `-phases` clocks close on worker wall, so no fourth component hides inside a worker), `idle ≥ 0`, and 0.153% agreement on instructions retired across all eight runs. The closure line is now printed by `e32-decompose.py` rather than argued, and **mutating one round's read by +10% takes it to 0.9666**, which is the proof it discriminates. **(2) "`-fold ptr` moved no share outside E-26's brackets" is FALSE for four of the six functions** — `update` +1.65, `memequal` +1.17, `foldRows` +1.16, `merge` +0.59 points above E-26's top, all in the same direction; only `indexDelim` and `parseTempWord` are contained. Checked with a six-line script instead of by eye, corrected at all four sites that published it (E-32's bullet, the board's ordering paragraph, `backlog.md`, `built.md`), and the surviving claim is the stronger one anyway: **the RANK of all six is identical**, and rank is what the board is ordered by. **(3) "E-31's rounds agree with these" overstated a disjoint measurement** — E-31's `default-w20` burns 14.77-14.87 s of user CPU against this pass's 14.64-14.69 s, **disjoint, 1.07% higher on the same binary and flags**; restated as the cross-invocation difference it is, which changes no conclusion and is exactly what E-11/E-18 forbid quoting across. **(4) "an eighth reproduction" undercounted its own three rounds** (floors 0.9793/0.9760/0.9760 s are the eighth, ninth and tenth on E-31's counting). **(5) `e32-ceilings.py` restated five means transcribed from another script's output** — the drift pattern `lessons.md` already records for a test helper; it now parses the same committed log, and its output is byte-identical to what was published, so the transcription was right and can no longer rot. **(6) Item 18 named one reading of the idle cores and not its rival** — too few runnable goroutines (which more workers fix) versus scheduler wake latency (which they do not); nothing in E-32 separates them and only the arm does, so the board now says so. One mutant beyond (1): removing the round-0 first-touch exclusion moves the headline gap 19.47% → 24.40%, so the exclusion is load-bearing — and the SHAPE of that damage became a finding, because the compute floor moves 0.9771 → 0.9772 s while idle goes 11.48% → 16.79%, i.e. the outlier costs 0.31 s of wall and 0.001 s of work and is therefore I/O, corroborating the verdict from a direction the decomposition does not use.
- reviewed go-opt-round-3-10k f3f3223..HEAD: REPAIRED — the code held and the WRITE-UP did not, twice, which is this file's most repeated failure. (1) **A derived read floor was published with the wrong rate beside it**: the 57.09 GB figure was written as "3.12 s at the measured 4.42 GB/s", and 4.42 GB/s does not produce 3.12 s — the measured rate is 18.29 GB/s (13.795610267 GB / 0.7544 s, `02-baseline-data.txt`), which does. Corrected at its only site before commit; the 3.12 s itself was right. (2) **An extrapolation was taken from the wrong key set**: the note explaining why no 1b reference output exists said "15 s here extrapolates to about 62 s there", applying the 10k file's 62.4 ns/row to the 413-station 1b file, whose recorded rate is 39.8 ns/row and ~40 s. Corrected. (3) **A generation-rate explanation was written from reasoning and was wrong**: 1599 MB/s was attributed to "the per-row cost is dominated by the PRNG draw, not the copy", which is a mechanism nothing here measured; replaced with what is actually true of the file — the same bytes cost fewer rows because the names are longer. (4) **An insertion broke the sentence it was inserted into**: the item 13 board row's new escape-clause note landed mid-clause between "(E-26);" and "shrinking the bucket"; moved to the end of the row. Found by re-reading the row rather than the diff, which is the only way that class shows up. **Mutation testing, four mutants on the decisions this cycle made, all four CAUGHT**: aliasing the map key to the fold buffer (caught by TestMapKeysDoNotAliasTheFoldBuffer, the test written for it), dropping `t.size++` (TestTableLayoutsAgree's bucket-count assertion), moving the map dispatch below the array's full check so it returns false on the first row (three tests), and `v < e.min` → `v > e.min` (three tests). No surviving mutant, and the two new tests each killed a mutant nothing else caught. The arm's numbers reproduce across two independent invocations with brackets of 0.078% and 0.33%.
- reviewed go-opt-round-3-park 57ba731..a181c8b: REPAIRED — six defects in a docs-only group, and the two worst were both an assertion made over a set that was never enumerated. **(1) A pre-registered test was silently re-specified.** The queue's item 18 already carries a four-slot arm, a bar ("anything over 5% would be a surprise"), a second channel (parallel efficiency must rise from 80.5%) and the competing reading the arm exists to distinguish; P-06 published its own five-slot arm and its own keep rule and dropped the rest. A parked entry that restates a registered test instead of quoting it is how a bar gets moved without anyone deciding to move it — P-06 now quotes the arm, labels its fifth slot as an addition with its reason, and carries the competing reading and the `-phases` mechanism check. **(2) The verdict's own reproduction claim was wrong in two numbers because E-31 was left out of the set the numbers were asserted over**: nine wall readings, not eight, and a user-CPU spread of **0.68%** (14.647-14.747 s), not 0.48%, because E-31's `default.b` is 14.747 s. The handoff had already claimed "every derived figure evaluated in `python3 -c` and pasted" — true of each figure computed, and no defence at all against computing the right statistic over the wrong set, which is the more expensive mistake. **(3) C12 misquoted its own evidence command**, listing lines from a broader grep than the one written down and wrong by three lines; it now carries the exact pinned `git grep … ca81148` and its real output (14 hits in 7 files, two publishing the claim, one borrowing it, eleven not making it). **(4) §U.1's probe had one control where it needed two** — a missing method and a package that does not build on the architecture at all fail identically, so the probe was mutated with `Not().String()`, which arm64 does have, and it builds clean, pinning the failure to the method rather than the import. **(5) A `String` method was cited to a five-line range that does not contain it** (`maskmerge_gen_arm64.go:142`, not `ops_arm64.go:2140-2144`). **(6) Two count defects:** "compute floor 0.9771-0.9812 s" presented a three-round mean and a single reading as a range's endpoints (both kept, attributed, 0.42% apart), and the 18-item tally folded item 8's ANSWERED-by-refusing into "closed" without saying so. Also removed: P-06 pricing its own non-existence as "a sixteenth arm", a number with no source. What held: all four new entries carry seven fields and a status, every one of the twelve line citations written this cycle resolves (checked with `sed -n '<n>p' | grep -F`), the board carries a final state for all 18 numbered items with nothing reading OPEN, and no arm was invented for the separator-scan pot the round could not close.
