.DS_Store

# Apple's Vision model files. Legal to read locally; NOT redistributable.
# This repo pushes to GitHub, so committing them would publish them. See 41-vision-recon.md.
shapes-image-file-format/weights/
*.espresso.weights
*.espresso.net
*.espresso.shape
# build artefacts
shapes-image-file-format/code/lab/lab
# Local-only research notes. Kept on disk, never pushed: this repo is public.
shapes-image-file-format/private-notes/
.backups/
1brc/code/go/bin/
# `go build ./...` in a main package writes the binary next to the source, named after the directory.
1brc/code/go/go
1brc/code/gen/gen
# A detached measurement appends a waiting line every 15 s for up to 8 hours; bench/ keeps results, not launcher logs.
1brc/bench/*-detached.log
