# 1brc backlog — append-only ids, priority order

Ids are stable and append-only; never renumber or delete. `[dN]` = difficulty for router delegation. `HUGE` = dispatched alone. `FANOUT` = Workflow-authorized design-space search (see spec.md). `(needs: `id`)` gates dispatch on a built dependency.

- [ ] `def-understand` — fetch and study https://github.com/gunnarmorling/1brc (README, rules, eval method, top leaderboard entries and their write-ups); write `1brc/01-definition.md` + `1brc/01-definition-data.txt` (cited sources, fetch dates); symlink `1brc/definition.md` -> `01-definition.md` (the handoff name). State OUR target: <1s, 1B rows, this M5 Pro, warm cache. [d3]
- [ ] `env-data` — build the generator (port official logic or extend SOURCE's `cmd/synthetic_generator.go`; keep generator code in `1brc/code/gen/`); generate `measurements-10m/100m/1b.txt` into `/Users/noelruault/Downloads/1brc/1brc-assets/`; record command+rows+bytes+sha256 per file in `1brc/02-baseline-data.txt`. Also produce the reference (trivially correct) implementation + expected outputs for 10m. [d3]
- [ ] `env-baseline` — measure the physical floor on this machine: dd/read bandwidth on the 1b file (cold+warm), `wc -l` time, naive Go line-scan time; install hyperfine if missing; write `1brc/02-baseline.md` (+ data companion) deriving what <1s implies in GB/s and ns/row. (needs: `env-data`) [d2]
- [ ] `asm-recon` — technique inventory from top 1BRC solutions (thomaswue, artsiomkorzun, jerrinot, ...), marcelroed/gigatoken (tokenization angle), and arm64/NEON literature: semicolon/newline finding (SWAR vs NEON cmeq), branchless fixed-point temp parse, name hashing, mmap strategies. Write `1brc/03-technique-recon.md` mapping each technique to an arm64 equivalent and a testable hypothesis. (needs: `def-understand`) [d3]
- [ ] `asm-kernels` HUGE FANOUT — build N competing arm64 assembly tokenizer kernels under `1brc/code/asm/` (at minimum: SWAR 8-byte, NEON 16B+ vector scan, gigatoken-inspired tokenizer, branchless temp parse; each with scalar-reference correctness check in `make test` and a GB/s microbenchmark); write `1brc/04-asm-kernels.md` (+ data) declaring a measured winner per sub-problem. (needs: `asm-recon`) [d5]
- [ ] `skill-performance-assembly` — distill the kernel work into the global skill `/Users/noelruault/.claude-work-home/.claude/skills/performance-assembly/SKILL.md` (macOS arm64: NEON idioms, SWAR, alignment, page-fault/mmap behavior, Instruments/sample profiling, Go Plan9-asm quirks, microbench discipline). Employer-agnostic, generalized, frontmatter like sibling skills. (needs: `asm-kernels`) [d4]
- [ ] `go-recon` — raw/unsafe Go study: mmap via syscall, unsafe.Pointer walks, bounds-check elimination (verify with `-gcflags=-d=ssa/check_bce`), custom open-addressing hash, per-core sharding, GOGC off, //go:noescape + Plan9 asm interop; recon automataIA/1brc-rs and top Go 1BRC solutions for transplantable ideas. Write `1brc/05-go-techniques.md`. (needs: `asm-kernels`) [d3]
- [ ] `x-transfer` — cross-disciplinary transfer pass, the repo's signature move (see `quantization/01-cross-disciplinary-transfer.md` and `nearest-color-scaling/05-cross-disciplinary-transfer.md` for the shape): enumerate mechanisms OTHER fields use for the same shapes of work — DB vectorized scans and SIMD predicate filters, bioinformatics k-mer hashing, DPDK/io_uring-style batched I/O, compiler perfect hashing, GPU/Metal offload feasibility; each candidate ends as a testable hypothesis with a prediction, appended to the ledger's queue; no candidate discarded without a number or a non-expiring argument. Write `1brc/06-cross-disciplinary-transfer.md`. (needs: `go-recon`) [d4]
- [ ] `go-skeleton` — scaffold `1brc/code/go` (module, flag parsing, Makefile, bin/ gitignored), `1brc/scripts/check-correctness.sh` (byte-compare vs reference on 10m), hyperfine bench script `1brc/scripts/bench.sh` writing raw output to a dated file. Gate green end-to-end with a naive implementation. (needs: `env-data`) [d3]
- [ ] `go-v1-parallel` — parallel I/O + per-core chunk sharding + custom hash map, no asm yet; I/O strategy starts from 02-baseline's measured winner, with mmap kept as a measured alternative (nothing assumed away without a number); correctness gate then hyperfine on 100m and 1b; record in `1brc/07-experiment-ledger.md` (create it: append-only; idea, prediction, measured, verdict). (needs: `go-skeleton`) [d4]
- [ ] `go-v2-kernels` — integrate the asm-kernels winners (Go Plan9 asm or SWAR-in-Go where the call overhead kills the asm win — measure both); ledger every swap. (needs: `go-v1-parallel`) [d4]
- [ ] `go-autoresearch-harness` — adapt the autoresearch loop (hypothesis queue -> experiment -> metric -> keep/kill -> ledger) into `1brc/scripts/experiment.sh` + a hypothesis queue section in the ledger, starting from the repo's OWN prior art (`shapes-image-file-format/AUTORESEARCH.md`, `METHODOLOGY.md`, `07-method-what-worked.md`; `quantization/00-methodology.md`) with https://github.com/karpathy/autoresearch as the secondary source; seed >=10 hypotheses from the recon + x-transfer reports. Write the adaptation note in the ledger header. (needs: `go-v2-kernels`) [d3]
- [x] `go-opt-round-1` HUGE FANOUT — run the autoresearch loop: take the top hypotheses, one experiment each (parallel attempts allowed), measure, keep/kill, ledger; end with the new best binary + number. (needs: `go-autoresearch-harness`) [d5]
- [x] `go-opt-round-2` HUGE FANOUT — next hypothesis batch; profile-driven (sample/pprof the current best first, let the profile refill the queue). (needs: `go-opt-round-1`) [d5] — SPLIT: the ticket is two units and the first one changed what the second should be. Profiling was supposed to refill the queue and instead it re-ordered it, by measuring that the target sits BELOW the compute floor (E-20), so the batch that follows is a different batch from the one this line imagined. Split into `go-opt-round-2-profile` (shipped) and `go-opt-round-2-batch`.
- [x] `go-opt-round-2-profile` — profile the current best and refill the queue from what it measures: `-phases` for the read/fold/skew/merge split, `-cpuprofile` for the CPU shares, then re-order the board and re-price every prediction the profile falsifies. (needs: `go-opt-round-1`) [d4]
- [x] `go-opt-round-2-batch` HUGE FANOUT — the hypothesis batch the profile selected, in board order: item 16 (validate the temperature from the integer, ceiling 18.2% of CPU) first because it is the largest cut with the contract unchanged, then items 1 and 5 in ONE invocation (they attack the same `indexDelim`/`foldRows` path, 33.5% of CPU, and overlap), then item 13, then item 14 (double-buffering, measured ceiling 17.6%). Item 15's re-measure is owed and unrun: its first invocation was orphaned and contaminated (E-19). Every arm needs the preflight green and a quiet AC window; a 17-minute eight-arm invocation is a cycle of its own. (needs: `go-opt-round-2-profile`) [d5] — SPLIT: this line asks for five separate quiet-window invocations and its own last sentence prices each one at a cycle, so it cannot ship green in one. Split into `go-opt-round-2-item15`, `go-opt-round-2-item16`, `go-opt-round-2-items1and5`, `go-opt-round-2-item13` and `go-opt-round-2-item14`, chained on `needs:` so exactly one arm is dispatched per cycle. Two corrections carried into the split rather than left in this line: item 16's mechanism here ("validate the temperature from the integer") is the version `CORRECTIONS.md` C7 records as FALSIFIED, and item 15 is promoted ahead of the new arms because it defines the incumbent they are measured against.
- [x] `go-opt-round-2-item15` — the OWED re-measure (board item 15): invocation B's six rows — `-buf 1024`, `-bits 14`, `-nocache=false`, `-table split`, `-parse scalar`, `-split cursor` — against an incumbent named FIRST AND LAST, with `experiment.sh -c` on. Runs BEFORE any new arm: these six verdicts define the incumbent's own flags, two of them change sign under the slot bias (C6), and E-11's rule is that a delta only transfers if its baseline is the shape production runs — so an arm measured against a mis-configured incumbent is a number that does not transfer. No code, flags only. Verify: preflight green (`pgrep -fl 'hyperfine|experiment.sh'` clear, AC power, `require_quiet` satisfied), bracket spread under 3% with user CPU flat across slots, raw output in `07-experiment-ledger-data.txt`, one ledger row per verdict confirmed or reversed, and every reversal corrected at its own site plus a `CORRECTIONS.md` entry. (needs: `go-opt-round-2-profile`) [d4]
- [x] `go-opt-round-2-item16` — board item 16, the largest cut with the contract unchanged: establish the SAME format rejection `validTemp` performs from the 8-byte word `parseTempBranchless` already loaded, branchlessly, instead of the four to six dependent byte compares it costs today; ceiling 18.2% of CPU (E-21). It is NOT "validate from the integer" — C7 records that version as falsified, because `inRange` already runs on the same row and the byte check is what establishes that the bytes were a temperature at all. Verify: a differential test asserting the new check accepts and rejects byte-for-byte what `validTemp` does over the whole `-?\d{1,2}\.\d\n` shape plus adversarial non-temperatures (`b;1.0` from a name containing the separator, `100.0`, a truncated field, a field with no `\n`), mutation-tested; `check-correctness.sh` green on 10m and on the 10k key set; then one arm against the item-15 incumbent. (needs: `go-opt-round-2-item15`) [d5]
- [x] `go-opt-round-2-items1and5` — board items 1 and 5 in ONE invocation, because they attack the same `indexDelim`/`foldRows` path (33.5% of CPU, the biggest pot on the board, E-21) and overlap: fuse the name hash into the separator scan (item 1, whose own prediction predates the profile and is to be re-stated against it before anything is built) and `unsafe.Add` pointer walks in the fold (item 5, ceiling 4-16% in isolation with half of that reachable in safe Go). Verify: a `;`-in-name case against a sibling implementation before it ships (the standing lesson — that divergence has shipped three times), correctness gate green, and both arms plus their combination in one invocation so the overlap is measured and not assumed additive. **The incumbent these arms are measured against is now `-parse word` at 1.424 s (E-25).** [**CORRECTED, C9: this line also said `indexDelim`'s share "rose to ~35.9% of a smaller total". It did not — the re-profile it asked for measures 17.73-20.36%, because E-24's buffer change moved the syscall bucket from 15.7% to 54-57% and compressed every other share. Ordering is by share of the NON-syscall samples, where `indexDelim` is 40.9-44.3%, and item 1's own reachable pot is 1.07-1.72% of compute, not 36%.**] (needs: `go-opt-round-2-item16`) [d5]
- [x] `go-opt-round-2-item13` — board item 13, H-13 quotiented 32-byte entry: inline 8-byte prefix in `entry`, full names in a side array. Adjacent to E-03, whose 4.6% loss C6 cut to 0.94%, and E-21 gives it a real target: `memequal` is 5.8% of CPU and `(*table).update` 15.9%. Verify: `TestHashPathsAgree` extended to the quotiented layout so `hashWord` and `hashName` cannot split a station across two buckets, correctness gate green on the 10k key set specifically (that is where the collisions are), then one arm. (needs: `go-opt-round-2-items1and5`) [d5]
- [x] `go-opt-round-2-item14` — board item 14, H-14 double-buffer each worker: a fill-ahead goroutine per worker behind a flag. Still the largest single mechanism on the board and it can no longer reach the target alone — E-20 measures the ceiling at 17.6%, not 25%, which lands at 1.24 s. **Superseded by E-25: the ceiling is 19.1% of a 1.424 s wall and it lands on the 1.152 s compute floor, 152 ms over target; the incumbent is `-parse word`.** **Superseded again by E-27, and this time the verdict flips: the ceiling is 20.4% of a 1.233 s wall and it lands on a 0.9812 s compute floor, 18.8 ms UNDER target. This is now the only item on the board that can reach the goal, the incumbent is `-parse word -fold ptr`, and 1 MiB is a buffer size this item has to re-measure rather than inherit (E-24).** Verify: the sweep of worker count x buffer size x split x reader against the reference, because a single-configuration test finds none of the four byte-offset off-by-one traps, then one arm. (needs: `go-opt-round-2-item13`) [d5] — **BUILT AND KILLED (E-29). [CORRECTED, C10: every ceiling this line published — 25%, 17.6%, 19.1%, 20.4% — and its "only item on the board that can reach the goal" verdict are FALSIFIED.** `-fill ahead` measures +0.57%/+1.57% of wall against the two bracket slots, ranges overlapping both, user CPU flat at −0.08%/+0.04%, and 0.24% from the `-fill sync` arm it differs from by the fill goroutine alone. The arithmetic was sound; the assumption under it was not — E-17's oversubscription is already overlapping the read across workers, so a per-worker prefetch has nothing to hide. The `-buf` re-measure the line demanded WAS run and E-24's 1 MiB held (512 KiB +5.76%, 4 MiB +6.57%, disjoint).**] The board has no derived path to 1.000 s: 19.85% of wall sits above the compute floor with no named mechanism, and `go-opt-round-3` inherits that question, plus board item 17 (the 2x2 that tests the substitution reading) rather than a candidate.
- [x] `go-opt-round-3` HUGE FANOUT — final round: chase the remaining gap to <1s or exhaust the queue; park survivors in `1brc/PARKED.md` with revive triggers. (needs: `go-opt-round-2-item14`) [d5] — SPLIT: this line is a ROUND, and its two verbs cost separate quiet windows. "Chase the remaining gap" has no named mechanism left — E-29 killed the last one and `07-experiment-ledger.md:44` records **19.85% of wall above the compute floor** (20.42% against E-27's 0.9812 s floor; both are published, neither is a new number) with nothing on the board claiming it — so the gap needs a measurement pass before an arm can exist, exactly as `go-opt-round-2-profile` did. "Exhaust the queue" is a different unit again: item 17 is runnable now with no code, and items 8/9/10-at-10k/12 are all blocked on generating a file and on a design decision about WHICH file. A single cycle that tried all three would either contaminate its own benchmark (`lessons.md`: two invocations already lost to that) or ship a docs-only round with no measurement. Split into `go-opt-round-3-item17`, `go-opt-round-3-gap`, `go-opt-round-3-10k` and `go-opt-round-3-park`, chained on `needs:` so exactly one quiet-window invocation is dispatched per cycle. `method-retro` and `final-dod` are re-pointed onto `go-opt-round-3-park`, because `is_built` (`scripts/loop-next.sh:37`) matches built.md exactly and `go-opt-round-3` will never appear there — left as written, the two terminal tickets would have been masked forever.
- [ ] `go-opt-round-3-item17` — board item 17, the only runnable-now row left: the `-fill` x `-workers` 2x2 that tests E-29's DERIVED reading that E-17's oversubscription was already doing the overlap a per-worker prefetch was built for. Four configurations in five slots, no code, incumbent first AND last: `-a 'default.a=' -a 'w15=-workers 15' -a 'ahead-w15=-fill ahead -workers 15' -a 'ahead-w20=-fill ahead -workers 20' -a 'default.b='` — five slots at 1b is ~5 x 6 x 21.7 s = 11 min, so it MUST be backgrounded (a 600 s foreground cap kills the run on hyperfine's `--prepare` sleep and aborts the whole invocation). The substitution reading predicts `ahead-w15` recovers most of what `w15` loses against the 20-worker default, and the mechanism check is `-phases` on BOTH 15-worker arms in the same window: if the reading holds, `ahead-w15` shows the read off its critical path and a worker wall spread near E-20's 1.029-1.038, which is also `PARKED.md` P-05's revive test. This cannot reach the target on its own — E-29's best arm was 1.212 s — so it is a mechanism verdict, not a candidate. Verify: preflight green (`pgrep -fl 'hyperfine|experiment.sh'` clear, AC, `require_quiet` satisfied), bracket spread under 3% with user CPU flat across slots or NO arm quoted, `check-correctness.sh` per arm, raw output in `07-experiment-ledger-data.txt`, one ledger row, and P-05 answered either way. (needs: `go-opt-round-2-item14`) [d4]
- [ ] `go-opt-round-3-gap` — the round's actual research question, and it is an instrument pass, not an arm: WHERE does the 19.85% of wall above the compute floor go, measured on the binary that is shipped today? Every reading the board rests on predates two defaults — E-20 profiled a 1.50 s binary at 4 MiB and E-26 re-profiled before `-fold ptr` landed — so `-phases` (read/fold/skew/merge) and `-cpuprofile` are re-run on `-workers 20 -buf 1024 -parse word -fold ptr`, ranked by share of the NON-syscall samples (E-26's rule: the syscall bucket holds 7.51 s of samples against 1.52 s of measured system time and is not CPU), with the pprof-under-counts correction applied (shares rank, seconds do not). Then the round is HONEST about what the measurement admits: name the mechanisms it supports and board them with predictions, or record that it names none and that the study's remaining gap has no candidate — an unexplained 19.85% recorded as unexplained is a result; a mechanism invented to fill it is not. No arm is built in this ticket. Verify: preflight green, `-phases` and profile output in `07-experiment-ledger-data.txt` with the commands that produced them, one ledger row, the board re-ordered from what it measured, and every derived figure evaluated in a shell rather than written in prose. (needs: `go-opt-round-3-item17`) [d4]
- [ ] `go-opt-round-3-10k` — the 10,000-station regime, which is where queue items 8, 9 and 12 and item 10's open half all wait, and the decision that has cost three cycles is made HERE and made once: hold **BYTES** constant, not rows. Derived from the recorded 570,941,611 B / 10,000,000 rows of `measurements-10k-stations-10m.txt` (`02-baseline-data.txt`), the 10k key set is 57.0942 B/row, so a 1b-row 10k file is **57.09 GB at 2.22x this machine's RAM with a 3.12 s read floor** and every arm on it lands three times over target before any compute — it answers a different question than the items asking for it. **241,600,000 rows reproduces the 1b file's 13.80 GB and its 53.5%-of-RAM memory regime**, which is the file items 9, 10 and 12 actually want. Generate with `go run ./cmd/measurements -rows 241600000 -out $ASSETS/measurements-10k-stations-242m.txt -keyset 10k -seed 1` (~19 s, byte-for-byte reproducible from the seed) and record rows/bytes/sha256 in the data companion (`openssl dgst -sha256`, not `shasum`). `experiment.sh -f` needs no change — it substitutes the token into `measurements-$FILE.txt` — but it is not the 1b file, so the harness demands `--mechanism-only` and stamps `NOT A VERDICT`, which is correct and load-bearing: this file ranks MECHANISMS in the 10k regime and re-ranks nothing about the 413-station defaults, and anything it keeps must be re-measured at 1b before it becomes a default. Scope: generate the file and close item 9 (Go's runtime map, measured 3.7% faster than open addressing on 10k names single-threaded and never tested end-to-end, needs an arm built) or say why it was not; items 10 and 12 may be left open with their measured state written into their board rows. Verify: correctness gate green on the new file (the reference must produce its expected output first), gate green, one ledger row per arm. (needs: `go-opt-round-3-gap`) [d5]
- [ ] `go-opt-round-3-park` — close the round and leave the record a reader can act on. Park every survivor in `1brc/PARKED.md` with ALL seven fields and a revive trigger that is a TEST rather than a wish — at minimum whatever `go-opt-round-3-gap` names but does not build, and whichever of items 10 and 12 `go-opt-round-3-10k` leaves open. Write the round's verdict into `07-experiment-ledger.md`: the best measured wall clock with its storage state and reader configuration, the gap to 1.000 s, and the bottleneck analysis that gap rests on, profile-backed. `spec.md:61` makes an honest miss with the ledger intact a valid outcome and an unmeasured claim not one, so if the target is not reached this ticket says so in those terms rather than hedging. Verify: seven fields present in every new `PARKED.md` entry, every number in the round verdict traceable to a recorded command, and the queue board carrying a final state for every one of the 17 items. (needs: `go-opt-round-3-10k`) [d3]
- [ ] `method-retro` — the study's method retrospective, the analog of `shapes-image-file-format/07-method-what-worked.md`: distill `docs/1brc/lessons.md`, `1brc/CORRECTIONS.md`, `docs/1brc/review.md`'s REPAIRED lines and the ledger's meta-verdicts (E-09 scale, E-11 baseline shape, E-17 slot bias and the null experiment, E-19/E-20 quiet gating and the compute floor, the round-2 split, cycle 9's audit) into what worked, what failed and was repaired, and what transfers to the next study; write `1brc/NN-method-what-worked.md` (next free NN) + a data companion of pointers into the ledgers; propose — never apply — any convention promotions for the human as a "Proposed" note appended to this backlog. (needs: `go-opt-round-3-park`) [d3]
- [ ] `final-dod` — verify EVERY item in spec.md's Definition of Done (including `1brc/CORRECTIONS.md`, the quiet-window AC-power headline re-measure, and the root `README.md`/`PARKED.md`/`CLAUDE.md` index rows staged in this branch); run the 1b correctness pass; write `1brc/README.md` + final report with headline numbers; only this ticket may emit "backlog empty", and only with all checks passing. (needs: `go-opt-round-3-park`) [d4]
